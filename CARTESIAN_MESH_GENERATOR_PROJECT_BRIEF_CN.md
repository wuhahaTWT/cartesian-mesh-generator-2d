# 面向航空航天复杂结构的快速 Cartesian 网格生成器

## 新项目总纲、技术路线与验收规范

文档状态：新项目立项基线  
编写日期：2026-08-07  
建议用途：把本文件复制到新项目根目录，作为项目目标、架构决策、开发顺序和验收标准的唯一初始依据。

---

## 1. 文档目的

本文件用于启动一个全新的三维 CFD 网格生成项目。新项目继承现有 CFD 演示软件中“结果必须真实、性能必须实测、不能用界面掩盖算法缺失”的原则，但不继承原有 D2Q9 LBM 求解器及其浏览器内计算架构。

新项目的主要任务不是继续开发流场求解器，而是研究和实现：

> 面向航空航天复杂结构的自动化、自适应、可增量更新、可扩展至千万级单元的三维 Cartesian 网格生成方法与软件。

这里的“Cartesian 网格生成器”不能只是把计算域均匀切成立方体。最终生成的网格必须能够表达真实复杂几何、保留边界分区、形成可靠的体网格拓扑，并能够导出到真实 CFD 工作流中。

本文件同时承担以下作用：

1. 说明为什么选择 Cartesian 网格路线；
2. 规定第一版必须解决和暂时不解决的问题；
3. 给出从几何导入到网格导出的完整算法流水线；
4. 规定千万级网格所需的数据结构和软件架构；
5. 规定每个阶段的真实性、正确性和性能验收方式；
6. 防止项目过早发展成求解器、通用 CAD 或只有界面的演示软件；
7. 为论文、软件著作权和后续工程化保留清晰的研究主线。

---

## 2. 项目背景与问题来源

在燃气轮机、航空发动机和其他大型复杂机械的 CFD 工作中，网格生成通常是整个分析流程中最耗人工、最依赖经验的环节之一。

贴体网格能够很好地描述壁面和边界层，但存在以下实际困难：

- CAD 几何需要大量清理、封闭和简化；
- 冷却孔、狭缝、薄壁、尖角和小圆角会显著增加划分难度；
- 网格拓扑往往依赖工程师手工设计；
- 几何结构发生变化后，经常需要重新生成相当大范围的网格；
- 重新划分后单元编号和拓扑变化，使旧解映射与参数对比更加困难；
- 对大量设计方案进行优化时，前处理成本可能超过求解设置本身。

如果只修改入口流量、温度、压力、冷却气比例等边界条件，而几何不变，原网格通常可以复用；但一旦修改孔径、孔位、叶片轮廓、流道局部结构或壁厚，贴体网格往往需要重新处理。

Cartesian 网格的核心价值是把大部分网格拓扑从几何形状中解耦：先建立规则背景网格，再对几何附近进行局部细化、分类和边界切割。几何局部变化时，理论上只需更新受影响的空间区域。因此，本项目不仅研究“自动生成”，还应重点研究“几何变化后的增量式局部重构”。

---

## 3. 工业方法定位

### 3.1 贴体网格为什么仍然常用

航空发动机和燃气轮机中的高雷诺数黏性流动具有很薄的近壁边界层。贴体六面体或棱柱层可以在壁面法向使用很小的间距，同时在切向保持较大尺寸，用相对较少的单元解析速度和温度梯度。这对壁面剪切、传热、分离、转捩以及明确的 `y+` 控制非常重要。

因此，贴体网格在高精度近壁计算中仍具有明显优势。

### 3.2 Cartesian 网格并非只用于教学

Cartesian 方法已经存在于多种科研和工业工作流中：

- NASA Cart3D：自适应 Cartesian 网格、Cut-cell、复杂航空器几何、并行分析；
- OpenFOAM snappyHexMesh：六面体背景网格、八叉树式细分、表面吸附和棱柱层；
- Ansys Fluent Poly-Hexcore：Cartesian/八叉树六面体核心、近壁多面体和棱柱层；
- Simcenter STAR-CCM+ Trimmed Cell：自动化修剪体网格与棱柱边界层；
- CONVERGE：运行时 Cartesian Cut-cell，应用于发动机和燃气轮机复杂流道；
- FLOW-3D FAVOR：在规则 Cartesian 网格上使用面积分数和体积分数表示固体。

这表明工程上的主流方向往往不是“纯贴体”与“纯 Cartesian”二选一，而是：

> 用 Cartesian/八叉树网格实现主体区域的自动化和可扩展性，用 Cut-cell、表面吸附、棱柱层或壁面模型处理边界。

### 3.3 本项目的路线

本项目采用逐级演进路线：

1. 三维规则 Cartesian 背景网格；
2. 自适应线性八叉树；
3. 几何内外与相交分类；
4. 真实 Cut-cell 几何和拓扑；
5. 小单元检测与处理；
6. 增量式局部重构；
7. 必要时扩展近壁混合层。

近壁棱柱层不是第一阶段内容，但架构必须允许未来增加。不得为了追求“纯 Cartesian”而否认高雷诺数近壁计算的实际需求。

---

## 4. 项目定位

### 4.1 软件定位

第一阶段产品定位为：

> 一个面向封闭三角曲面和航空航天复杂结构的三维自适应 Cartesian/Cut-cell 体网格生成核心，带有命令行接口、网格检查、切片预览和标准格式导出能力。

它首先是网格算法软件，其次才是可视化软件。它不在第一阶段求解 Navier–Stokes 方程。

### 4.2 研究定位

建议将研究创新点聚焦为：

> 面向复杂结构局部几何变更的增量式自适应 Cartesian Cut-cell 网格生成方法。

应重点衡量：

- 几何只改变一小部分时，多少原网格能够保留；
- 未变化区域的单元 ID 和拓扑能否稳定保持；
- 局部重构相对全量重构的时间加速比；
- 局部变化后是否仍满足 2:1 平衡和全局拓扑一致性；
- 旧解向新网格映射所需的数据是否可以自动生成。

### 4.3 项目优先级

优先级严格按以下顺序排列：

1. 几何分类和网格拓扑必须正确；
2. 输出网格必须能被真实工具读取和检查；
3. 算法必须可重复、可验证、失败可见；
4. 数据结构必须支持百万至千万级扩展；
5. 增量重构必须真实保留未变化区域；
6. 性能必须来自实际墙钟和峰值内存测量；
7. 界面应当清晰，但不得先于核心算法扩展。

任何界面功能、AI 功能或附加格式都不能牺牲前五项。

---

## 5. 术语定义

为避免讨论混乱，项目统一使用以下术语：

- **Cartesian 单元**：边与全局坐标轴平行的正交六面体单元；
- **背景网格**：尚未根据实体几何裁切的规则或分层 Cartesian 网格；
- **八叉树叶单元**：八叉树中不再细分、实际参与网格输出的单元；
- **线性八叉树**：以 Morton 编码排序并用紧凑数组存储叶单元的八叉树；
- **Cut-cell**：被几何表面切割后形成一个或多个流体多面体区域的背景单元；
- **嵌入边界**：几何边界不需要与背景网格节点预先重合的表示方法；
- **贴体网格**：网格边界面与物体曲面显式一致的网格；
- **体积分数**：Cut-cell 中流体体积占原背景单元体积的比例；
- **开口面积分数**：原 Cartesian 面上可供流体通过的面积比例；
- **2:1 平衡**：共享面、边或顶点的相邻叶单元层级差不超过一个规定值，第一版至少保证共享面相邻单元层级差不超过 1；
- **脏几何**：包含孔洞、重叠、自交、重复面、退化三角形、法向不一致或非流形连接的输入几何；
- **求解器可用网格**：不仅能显示，还具有闭合单元、正体积、一致法向、完整邻接和边界标签，并能通过目标求解器或独立检查器验证的网格。

---

## 6. 第一阶段范围

### 6.1 必须实现

第一阶段限定为：

- 三维矩形计算域；
- 封闭、定向、流形 STL 三角曲面输入；
- 基本几何诊断；
- 均匀 Cartesian 网格；
- 自适应八叉树细化；
- 表面、距离、曲率、狭缝和用户区域细化；
- 2:1 层级平衡；
- 单元内部、外部和相交分类；
- 可靠的几何内外判断；
- Cut-cell 体积、质心、面、面积和法向计算；
- 流体区域连通性选择；
- 边界分区名称保留；
- 小 Cut-cell 检测；
- 网格质量与拓扑检查；
- VTK 可视化导出；
- 至少一种真实 CFD 交换格式，优先 CGNS 或 OpenFOAM；
- 命令行批处理；
- 切片和层级可视化；
- 确定性结果哈希；
- 真实墙钟、峰值内存和网格规模报告。

### 6.2 第二阶段候选

第一阶段验收后才能考虑：

- STEP/IGES 直接导入；
- CAD 缝合和有限程度的自动修复；
- 增量式局部网格重构；
- 旧网格到新网格的映射；
- 小 Cut-cell 合并或守恒聚合；
- 多流体区域和共轭传热区域；
- 分布式并行生成；
- 近壁棱柱层或各向异性 Cartesian 层；
- Fluent、SU2 等更多格式；
- 桌面图形界面。

### 6.3 当前明确不做

在第一阶段完成前，不得加入：

- CFD 流场求解器；
- 湍流模型、燃烧模型或传热求解；
- 通用 CAD 建模器；
- 三维参数化几何编辑器；
- 结构有限元网格；
- 移动端界面；
- 云端任务系统；
- 用户账户、数据库或协作平台；
- AI 自动生成网格结果；
- 只展示体素而没有真实体网格拓扑的“工业网格”宣传；
- 在没有真实求解器验证前宣称“工业级”或“科研级通用网格器”。

---

## 7. 输入与输出

### 7.1 第一阶段输入

必需支持：

- 二进制 STL；
- ASCII STL；
- 矩形计算域定义；
- 保留区域种子点；
- 基础网格尺寸；
- 最大和最小细化层级；
- 表面细化层级；
- 距离细化规则；
- 用户指定 box、sphere、cylinder 细化区；
- 边界分组及名称。

后续支持：

- STEP；
- IGES；
- OBJ；
- 多部件装配；
- CAD 面名称和工程边界标签。

STEP/IGES 可考虑使用 Open CASCADE 读取与三角化，但 CAD 内核和网格核心必须解耦，确保 STL 流程可以独立测试。

### 7.2 第一阶段输出

输出必须包括：

- 网格元数据 JSON；
- 网格统计报告；
- 八叉树层级和单元分类；
- VTK/VTU 调试与可视化文件；
- CGNS 或 OpenFOAM 网格；
- 边界 patch 名称；
- 质量检查报告；
- 构建日志与失败原因；
- 可选的增量映射文件。

不得只输出截图、点云或表面三角形并称为体网格。

---

## 8. 总体技术架构

### 8.1 推荐技术栈

网格核心推荐使用：

- **C++20**：核心算法与数据结构；
- **CMake**：构建系统；
- **CTest + Catch2 或 GoogleTest**：测试；
- **OpenMP 或 oneTBB**：第一阶段共享内存并行；
- **MPI**：千万级和分布式阶段再加入；
- **Open CASCADE**：后续 STEP/IGES 和 B-Rep 支持；
- **Embree 或自研 BVH**：三角面求交加速；
- **CGNS/HDF5**：CFD 标准数据输出；
- **VTK**：调试、检查和可视化输出；
- **Qt 6 + VTK**：需要桌面界面时再引入。

选择 C++ 而不是继续使用浏览器 TypeScript 作为核心，主要原因是：

- CAD 与 CFD 生态成熟；
- 更容易控制内存布局；
- 便于使用双精度和鲁棒几何谓词；
- 更适合多线程、MPI 和流式文件输出；
- 千万级网格不能依赖浏览器对象和前端状态管理。

前端可以后续使用 Qt，或者让 React 只作为外壳调用原生命令行/服务，但核心不得依赖前端才能运行。

第三方库加入前必须记录版本、许可证、用途和替换方案。研究原型可以评估 `p4est`，但其 GPL 许可证可能影响未来闭源发布，必须在采用前明确。

### 8.2 推荐模块边界

```text
cartesian-mesher/
├── CMakeLists.txt
├── AGENTS.md
├── PROJECT_BRIEF.md
├── README.md
├── cmake/
├── apps/
│   ├── cli/                 # 命令行入口
│   └── viewer/              # 后期桌面查看器
├── include/cartmesh/
│   ├── geometry/
│   ├── spatial/
│   ├── octree/
│   ├── classify/
│   ├── cutcell/
│   ├── topology/
│   ├── quality/
│   ├── incremental/
│   └── io/
├── src/
│   ├── geometry/
│   ├── spatial/
│   ├── octree/
│   ├── classify/
│   ├── cutcell/
│   ├── topology/
│   ├── quality/
│   ├── incremental/
│   └── io/
├── tests/
│   ├── unit/
│   ├── geometry/
│   ├── topology/
│   ├── regression/
│   └── fuzz/
├── benchmarks/
│   ├── analytic/
│   ├── public/
│   ├── configs/
│   └── baselines/
├── examples/
├── docs/
└── tools/
```

### 8.3 核心数据原则

必须遵守：

- 几何计算默认使用 `Float64/double`；
- 单元 ID 使用 64 位整数；
- 八叉树叶节点使用 Morton 编码排序；
- 优先采用结构数组 SoA，而不是每单元一个复杂 C++ 对象；
- 普通 Cartesian 单元邻接尽量隐式推导；
- 只为 Cut-cell 和复杂过渡区域存储显式多面体拓扑；
- 大数组能够分块处理和流式写出；
- 调试模式可以保存更多证据，生产模式保持紧凑；
- 所有并行算法在相同输入和配置下必须产生确定性结果。

禁止把几何鲁棒性完全建立在单精度浮点数或固定容差上。

---

## 9. 网格生成流水线

整个生成过程应当保持阶段清晰，每一阶段都能输出诊断结果：

```text
导入表面
  ↓
几何诊断与尺度归一化
  ↓
建立三角面 BVH
  ↓
建立根计算域
  ↓
按尺寸场生成/细化八叉树
  ↓
执行 2:1 平衡
  ↓
单元内部/外部/相交分类
  ↓
选择需要保留的流体连通区域
  ↓
重建 Cut-cell 几何
  ↓
建立面、邻接和边界 patch
  ↓
检测小单元与拓扑错误
  ↓
质量检查
  ↓
导出与独立验证
```

任何阶段失败都必须返回明确错误，不得静默删除问题区域后继续输出“成功网格”。允许提供明确命名的容错模式，但必须报告其修改了什么。

---

## 10. 关键算法要求

### 10.1 几何诊断

至少检查：

- 三角形数量和包围盒；
- 零面积及近零面积三角形；
- 重复三角形；
- 边界边；
- 非流形边；
- 法向一致性；
- 连通分量；
- 自交候选；
- 封闭性；
- 几何尺度与容差建议；
- 极小特征和潜在狭缝。

第一阶段遇到非封闭或非流形输入时可以拒绝生成，但必须指出问题位置并导出可视化标记。

### 10.2 空间加速结构

输入三角面必须进入 BVH 或等价加速结构，用于：

- 单元包围盒与三角形候选查询；
- 射线内外判断；
- 最近表面距离；
- 局部曲率/法向变化估计；
- 几何变化区域查询。

不得对每个网格单元遍历全部三角面。

### 10.3 自适应八叉树

八叉树至少支持：

- 基础层级；
- 最大层级；
- 叶单元 Morton 编码；
- 细化与粗化；
- 表面相交细化；
- 到表面距离细化；
- 用户区域细化；
- 狭缝保护；
- 2:1 平衡；
- 邻居查找；
- 层级统计；
- 确定性遍历顺序。

细化条件必须来自明确的尺寸场或几何规则，不能依靠不可解释的随机策略。

### 10.4 内外分类

不能只用单条固定方向射线完成所有分类。第一阶段可结合：

- BVH 加速的多方向奇偶射线；
- 三角形朝向和符号距离；
- 连通区域 flood fill；
- 对退化命中进行确定性扰动；
- 对分类冲突进行显式标记。

对于解析几何测试，分类结果必须与解析真值比较。无法可靠分类的单元不得被默认当成流体或固体。

### 10.5 Cut-cell 重建

Cut-cell 不是简单给单元贴一个 `intersected=true` 标签。至少需要得到：

- 流体子体积；
- 体积分数；
- 流体质心；
- 每个开口面的多边形；
- 每个面的面积、质心和外法向；
- 几何边界切面；
- 边界 patch ID；
- 相邻流体单元；
- 闭合性和正体积检查。

必须验证离散几何守恒关系，例如：

- 单元各面有向面积向量之和接近零；
- 多面体面片闭合；
- 子体积位于 `[0, 原单元体积]`；
- 流体体积加固体体积与原单元体积一致；
- 相邻单元共享面的面积和法向相互匹配。

几何切割必须考虑同一背景单元中存在多个三角片或多个不连通流体片的情况。第一版如果不支持多流体片，必须检测并拒绝，而不是合并成错误拓扑。

### 10.6 小 Cut-cell 问题

非常小的 Cut-cell 可能给显式 CFD 求解带来严重时间步限制。网格器至少要：

- 统计最小体积分数；
- 标记低于阈值的单元；
- 输出位置和所属边界；
- 支持按阈值筛选和可视化；
- 为未来的单元合并、通量重分配或稳定化保留信息。

第一阶段可以只检测而不自动修复，但不得忽略。

### 10.7 狭缝和薄壁

复杂机械中最危险的几何特征之一是冷却孔、薄壁和窄通道。尺寸场必须能检测：

- 两相对表面距离；
- 通道局部厚度；
- 一个通道宽度内的单元数量；
- 表面法向快速变化；
- 小孔是否会被背景网格完全跳过。

应允许用户设置“每个最小间隙至少 N 个单元”。无法满足时必须报告，而不是悄悄封死通道。

### 10.8 增量式局部重构

第二阶段的核心研究功能包括：

1. 计算新旧几何变化区域；
2. 用膨胀包围盒覆盖可能受影响的单元；
3. 使受影响区域重新分类和细化；
4. 扩展影响区域直到满足 2:1 平衡；
5. 保持外部叶单元 Morton ID 和拓扑稳定；
6. 只重建必要的 Cut-cell 和邻接；
7. 输出旧单元到新单元的保留、细分、合并和删除映射；
8. 与全量重构结果做等价性比较。

增量结果必须在给定容差和拓扑规则下与全量重新生成结果一致。速度快但结果不同不能算通过。

---

## 11. 千万级网格设计

### 11.1 正确认识“千万网格”

生成一千万个没有几何含义的规则立方体很容易，不构成项目成果。千万级验收必须指：

- 已完成自适应细化和平衡；
- 已完成内部、外部和相交分类；
- Cut-cell 具有几何与拓扑信息；
- 边界标签完整；
- 通过质量检查；
- 能够写出标准格式；
- 峰值内存和墙钟时间已经测量。

### 11.2 内存策略

必须从第一天记录每种数据结构的单元字节数。推荐把内存拆成：

- 八叉树叶记录；
- 分类和标签；
- BVH；
- 三角面数据；
- Cut-cell 顶点；
- Cut-cell 面；
- 邻接；
- 导出临时缓冲；
- 可视化缓存。

不得用操作系统虚拟内存掩盖峰值内存，也不得只报告最终文件大小。

长期参考目标而非第一阶段承诺：

- 一千万个求解器可用叶单元；
- 单机峰值内存不超过 16 GB；
- 在明确记录的参考工作站上，完整生成和检查控制在数分钟量级；
- 导出时间单独报告；
- 相同输入重复运行得到相同网格哈希。

目标只能在真实测量后宣称达到。

### 11.3 并行策略

推荐顺序：

1. 串行正确版本；
2. 多线程 BVH 查询和初始分类；
3. 多线程局部细化；
4. 确定性并行排序和去重；
5. 分块 Cut-cell 重建；
6. 流式输出；
7. MPI 空间分区；
8. 分布式平衡和幽灵层。

不得在串行算法尚未有正确性基线时直接加入 GPU 或 MPI。

---

## 12. 可视化与交互要求

图形界面不是第一阶段前置条件。第一版可以通过命令行生成 VTK 文件并使用 ParaView 检查。

后续查看器至少应支持：

- 原始三角曲面；
- 计算域；
- 八叉树层级切片；
- 内部、外部、相交单元分类；
- Cut-cell 表面；
- 边界 patch；
- 体积分数；
- 小单元；
- 狭缝警告；
- 网格质量；
- 局部重构前后差异；
- 单元查询和 ID 显示。

千万级网格不能把所有单元面一次性提交给界面。查看器需要：

- 只绘制可见外表面；
- 切片；
- 层级抽样；
- 视锥裁剪；
- LOD；
- 后台流式加载。

界面显示的单元数、内存、耗时、失败数必须来自当前任务，不得写死。

---

## 13. 真实性与工程纪律

禁止：

- 把表面体素预览称为完成的 Cut-cell 网格；
- 用渲染遮盖内部错误单元；
- 把相交单元直接删除后宣称贴合几何；
- 伪造网格质量、耗时、内存或并行加速比；
- 只测试日志中是否出现“success”；
- 用截图代替拓扑和体积验证；
- 遇到 NaN、负体积或非闭合单元时静默修正；
- 把未实现的导出格式显示成可用；
- 在仅测试球体后声称支持复杂航空发动机几何；
- 用 AI 输出代替确定性计算几何算法；
- 在算法不可用时优先扩展 UI。

所有自动修复都必须在报告中列出：

- 修改对象；
- 修改类型；
- 原始尺度；
- 使用容差；
- 修改后的影响。

---

## 14. 测试体系

### 14.1 单元测试

至少覆盖：

- Morton 编码与解码；
- 层级坐标转换；
- AABB 相交；
- 三角形与盒相交；
- 射线与三角形相交；
- 邻居查找；
- 八叉树细化与粗化；
- 2:1 平衡；
- 多边形裁剪；
- 多面体体积和质心；
- 面法向和有向面积；
- 边界标签传播；
- 序列化往返。

### 14.2 解析几何验证

必须建立可计算真值的测试：

- 与网格轴对齐的立方体；
- 倾斜平面切割单元；
- 球体；
- 圆柱；
- 两个不相交实体；
- 带内部空腔的实体；
- 窄通道；
- 薄壁；
- 几乎穿过顶点或边的切面。

验证指标包括：

- 总流体体积误差；
- 表面积误差；
- 质心误差；
- 分类错误数；
- 负体积数；
- 非闭合单元数；
- 共享面不匹配数；
- 网格加密时的误差收敛趋势。

### 14.3 回归测试

每个基准记录：

- 输入文件哈希；
- 配置文件；
- 三角形数量；
- 各层级叶单元数；
- Cut-cell 数；
- 最小体积分数；
- 总流体体积；
- 边界面积；
- 拓扑错误数；
- 输出网格哈希；
- 墙钟时间；
- 峰值 RSS；
- CPU 线程数。

性能数据允许随硬件变化，几何和拓扑结果不应无故漂移。

### 14.4 模糊与性质测试

对以下情况进行随机但可复现的测试：

- 三角形顺序打乱；
- 顶点编号重排；
- 几何平移、旋转和尺度变化；
- 切面接近单元顶点；
- 小幅坐标扰动；
- 局部几何增删；
- 不同线程数。

必须验证重排不会改变物理网格结果，线程数不会改变确定性哈希。

### 14.5 外部验证

至少采用两种独立方式：

- ParaView/VTK 可视化检查；
- CGNS 检查工具、OpenFOAM `checkMesh` 或目标求解器网格读取；
- 对简单案例进行有限体积守恒检查；
- 与至少一种成熟网格器在相同几何和近似尺寸下比较单元量、生成时间和几何误差。

只通过本项目自己的检查器不算充分验证。

---

## 15. 基准测试体系

### 15.1 基础几何组

- 单个立方体；
- 球体；
- 圆柱；
- NACA 机翼拉伸体；
- 多孔板；
- 弯管；
- 多实体装配；
- 带狭缝和薄壁的人工几何。

### 15.2 复杂公开几何组

应只使用许可证允许再分发或提供下载脚本的公开几何，例如公开航空器、机翼、叶片或通用复杂三角曲面。每个文件必须记录来源、许可证和预处理步骤。

### 15.3 工程脱敏几何组

如果可以获得燃气轮机项目的脱敏几何，应建立：

- 单叶片；
- 带冷却孔叶片；
- 简化燃烧室；
- 旋流器；
- 多孔气膜冷却结构；
- 局部几何修改前后成对数据。

不得把受保密约束的工程几何提交到公开仓库或外部服务。

### 15.4 性能报告格式

每次正式基准必须记录：

```text
版本/提交：
操作系统：
CPU：
内存：
线程数：
编译器与优化级别：
输入格式：
输入三角形数：
叶单元数：
Cut-cell 数：
峰值 RSS：
导入耗时：
BVH 构建耗时：
细化耗时：
平衡耗时：
分类耗时：
Cut-cell 重建耗时：
拓扑检查耗时：
导出耗时：
完整墙钟时间：
失败/警告数量：
输出哈希：
```

生成与导出必须分开计时，不得只报告最快的内部阶段。

---

## 16. 阶段计划与验收门槛

### 阶段 0：项目骨架与解析几何

目标：建立原生核心、CLI、测试和基准框架。

实现：

- C++20/CMake 工程；
- 基本向量、AABB 和三角形类型；
- 解析立方体和球体；
- 规则 Cartesian 网格；
- VTK 输出；
- 实际时间与内存报告。

通过条件：

- 全新环境可重复构建；
- 单元数量与解析期望一致；
- VTK 可被 ParaView 打开；
- 立方体和球体测试有数值真值；
- 不含 GUI 和求解器。

### 阶段 1：STL、BVH 与均匀分类

目标：让封闭 STL 真正影响体网格分类。

通过条件：

- 二进制和 ASCII STL 均可读取；
- 能检测非封闭表面；
- BVH 查询替代全三角遍历；
- 球体、圆柱的体积误差随网格加密下降；
- 内外分类冲突为零或被明确报告；
- 百万级背景单元有真实基准。

### 阶段 2：自适应线性八叉树

目标：实现紧凑、自适应、可扩展的主体数据结构。

通过条件：

- 表面和用户区域细化有效；
- 2:1 平衡通过自动检查；
- Morton 编码无冲突；
- 邻居关系正确；
- 相同输入输出哈希稳定；
- 一千万叶单元的内存和构建测试完成，但此时不宣称它们全部是求解器可用 Cut-cell。

### 阶段 3：求解器可用 Cut-cell

目标：生成闭合、正体积、带邻接的 Cut-cell 多面体。

通过条件：

- 解析平面切割结果与真值一致；
- 球体总体积和表面积呈收敛趋势；
- 负体积为零；
- 非闭合单元为零；
- 共享面不匹配为零；
- 边界面积与体积守恒检查通过；
- 输出至少被一个外部 CFD 网格检查器接受。

### 阶段 4：狭缝、薄壁与复杂结构

目标：处理航空航天几何中最常见的失败模式。

通过条件：

- 指定最小通道能够被保留；
- 未解析通道会明确报错；
- 多部件和内部空腔可分类；
- 小 Cut-cell 有完整统计；
- 复杂公开几何完成生成和外部检查；
- 所有容错处理均有日志。

### 阶段 5：增量式局部重构

目标：形成论文和产品的核心差异点。

通过条件：

- 局部几何变化只触发局部重构及必要平衡扩展；
- 未影响单元 ID 稳定；
- 提供旧网格到新网格映射；
- 增量结果与全量重构结果等价；
- 报告复用率、影响范围和加速比；
- 至少在孔径、孔位和局部轮廓变化三类案例上验证。

### 阶段 6：千万级完整网格与产品化

目标：在真实复杂几何上达到千万级求解器可用网格。

通过条件：

- 背景 Cartesian 单元数量达到一千万量级，并与实际求解器控制体数量分开报告；
- Cut-cell、拓扑、边界和质量检查完整；
- 峰值内存满足明确预算；
- 完整生成时间和导出时间已实测；
- 外部工具成功读取；
- OpenFOAM 默认质量检查输出 `Mesh OK.`，不能只把“格式可读/核心拓扑可读”当作求解器质量通过；
- 查看器仍可交互检查局部区域；
- 不存在通过关闭检查或丢弃问题单元换取“成功”的情况。

到达某一阶段后必须停止扩展，先提交完整验证记录，再进入下一阶段。

---

## 17. 增量重构的研究评价设计

每个局部几何修改案例同时执行：

1. 原始几何完整生成；
2. 修改后几何完整重新生成；
3. 从原网格执行增量更新；
4. 比较完整重构与增量重构结果。

评价指标：

- 完整生成时间；
- 增量更新时间；
- 加速比；
- 峰值内存；
- 被重新访问的叶单元比例；
- 被保留的叶单元比例；
- 保持原 ID 的单元比例；
- 新增、删除、细分、合并的单元数；
- 网格体积和边界面积差异；
- 拓扑差异；
- 2:1 平衡扩散范围；
- 旧解映射覆盖率。

研究结论必须来自多种修改尺度，而不能只选择一个特别有利的小修改。

---

## 18. 主要技术风险

### 风险 1：项目退化成体素查看器

对策：阶段 3 必须生成真实多面体、邻接、边界和标准 CFD 输出。

### 风险 2：脏 CAD 消耗全部开发时间

对策：第一阶段只接受封闭流形 STL；几何修复作为独立阶段，不与基础网格算法混在一起。

### 风险 3：高雷诺数近壁单元爆炸

对策：明确纯各向同性 Cartesian 的适用边界；后期研究混合棱柱层、壁面模型或各向异性细化。

### 风险 4：小 Cut-cell 导致求解器不可用

对策：第一阶段完整检测，后续增加合并、聚合或求解器稳定化接口。

### 风险 5：显式拓扑导致内存失控

对策：普通 Cartesian 区域使用隐式邻接，仅在 Cut-cell/过渡区域保存显式拓扑，采用 SoA 和流式输出。

### 风险 6：过早开发漂亮界面

对策：阶段 0–3 以 CLI、VTK 和外部检查器为主；只有核心网格通过后才开发桌面 UI。

### 风险 7：并行结果不确定

对策：固定 Morton 排序、稳定归并和确定性 ID；将不同线程数哈希一致性加入测试。

### 风险 8：第三方许可证限制产品化

对策：建立依赖登记表，在采用 p4est、Open CASCADE、CGNS、VTK 等组件前审查许可证和发布方式。

---

## 19. 新项目开发规则建议

新项目的 `AGENTS.md` 至少应包含以下规则：

1. 每次只实现一个阶段目标；
2. 修改前说明目标、文件和测量方式；
3. 优先验证几何和拓扑，不优先开发界面；
4. 每个可见网格必须来自当前算法输出；
5. 不得把体素分类冒充 Cut-cell；
6. 不得隐藏负体积、非闭合、非流形或分类冲突；
7. 性能必须记录墙钟时间、峰值 RSS、线程数和硬件；
8. 每个里程碑必须使用外部工具验证；
9. 千万级必须是完整生成、检查和可导出的网格；
10. 不自动开发 CFD 求解器；
11. 不自动上传工程几何或敏感数据；
12. 不使用 AI 结果替代确定性几何算法；
13. 任何“工业级”“科研级”“实时”声明都必须有对应证据；
14. 发现性能不足时先做剖析，不能凭感觉重写；
15. 发现几何错误时保留最小复现案例并加入回归测试。

---

## 20. 新项目第一轮工作建议

新项目建立后，不要立即编写 Cut-cell 或界面。第一轮只完成：

1. 创建 C++20/CMake 项目；
2. 建立测试、基准和格式化配置；
3. 定义坐标、AABB、Triangle、CellKey、MortonCode；
4. 实现规则三维 Cartesian 网格；
5. 生成解析立方体和球体测试；
6. 输出 VTK；
7. 用 ParaView 实际打开；
8. 记录单元数、体积、墙钟时间和峰值内存；
9. 建立第一份 `VERIFICATION.md`；
10. 完成后停止，审查基础数据结构是否适合千万级。

第一轮不加入：Open CASCADE、GUI、MPI、求解器、AI、云端或多种导出格式。

---

## 21. 新项目启动提示词

把本文件复制到新项目根目录并命名为 `PROJECT_BRIEF.md` 后，可以向开发代理发出以下指令：

> 请完整阅读 `PROJECT_BRIEF.md` 和 `AGENTS.md`。当前只执行阶段 0，不实现阶段 1 之后的功能。先检查环境和空项目状态，然后说明阶段目标、将创建的文件、正确性测试、性能测量和外部可视化验证方法。确认方案后实现 C++20/CMake 的最小网格核心、解析立方体/球体、规则三维 Cartesian 网格、VTK 输出、单元测试、基准和验证记录。不得加入 CFD 求解器、GUI、AI、云端功能或尚未通过验收的高级网格功能。编译通过不代表完成，必须实际生成网格并使用外部工具验证输出。

---

## 22. 参考项目与资料

以下资料用于理解工业路线和建立对照，不代表直接复制实现：

1. NASA Cart3D：<https://www.nas.nasa.gov/publications/software/docs/cart3d/pages/>
2. NASA Cart3D Mesh Generation：<https://www.nas.nasa.gov/publications/software/docs/cart3d/pages/meshGeneration.html>
3. NASA Cartesian Cut-cell Methods for High Reynolds Number Flows：<https://www.nas.nasa.gov/pubs/ams/2026/02-05-26.html>
4. OpenFOAM snappyHexMesh：<https://doc.openfoam.com/2606/tools/pre-processing/mesh/generation/snappyhexmesh/>
5. Ansys Fluent Mosaic Poly-Hexcore：<https://www.ansys.com/content/dam/resource-center/white-paper/ansys-fluent-mosaic-technology-wp.pdf>
6. CONVERGE Gas Turbines：<https://convergecfd.com/applications/gas-turbines>
7. FLOW-3D FAVOR：<https://www.flow3d.com/whats-new-in-flow-3d/>
8. p4est：<https://p4est.org/>
9. Open CASCADE：<https://dev.opencascade.org/doc/overview/html/index.html>
10. Intel Embree：<https://github.com/RenderKit/embree>
11. CGNS：<https://cgns.org/>

正式开发时，应进一步整理每个参考项目的算法论文、许可证、输入限制、输出格式、网格质量指标和公开基准，不应只参考产品宣传页面。

---

## 23. 最终愿景

项目的长期目标不是成为另一个功能庞杂的通用 CFD 平台，而是成为一个在以下方面有明确优势的网格核心：

- 对复杂航空航天三角曲面自动生成网格；
- 主体采用紧凑的自适应 Cartesian/八叉树结构；
- 边界采用可验证的 Cut-cell 几何和拓扑；
- 对冷却孔、狭缝和薄壁有明确的解析能力报告；
- 几何局部修改后能够增量更新并保留大部分旧网格；
- 可扩展到千万级单元；
- 能被真实 CFD 工具读取；
- 所有性能、质量和正确性结论均有可重复证据。

只要沿着“解析几何真值 → 封闭 STL → 自适应八叉树 → Cut-cell → 复杂结构 → 增量重构 → 千万级”的顺序推进，这个项目既可以形成有分量的研究课题，也有机会发展成真正有工程价值的软件。
