# 当前状态与接手重点

更新：2026-09-19。本文件是唯一当前状态入口。历史过程交给 Git；具体数字只适用于下列输入、参数、工具链和证据快照。

## 交付定位与唯一工作区

当前交付是**有明确支持范围的原生二维工程验证版**。近期目标的无量纲尺寸、万级至十万级真实网格、有限扰动、桌面导出和实际 CFD 验证已经形成可复核证据；工业级通用精度和鲁棒性仍需持续完善。

唯一工作区为本仓库根目录，当前开发分支 `codex/cfd-development`，前端只看 `desktop/src/`。用户确认的整理基线是桌面 0.2.0 / `9bddfbf`，旧目录整理提交为 `0ef933d`。三维仓库是独立项目，不混用其代码或验证。源码导航、构建命令和历史查找见 [DEVELOPMENT_CN.md](DEVELOPMENT_CN.md)。

用户已授权连续推进，无需逐项审批普通实现。Astra 负责设计、核心算法和小验证；已授权的 Luna 负责固定批量执行。主代理核对原始日志、实际网格和摘要；摘要自报 PASS 不能替代证据。不使用 usage reset，不放宽质量门。

## 阶段节点与维护路径

- 网格阶段标签 `mesher-v0.3.0` 固定指向 `664ac7c`：桌面0.3.0，加入自研 FVM/CFD 核心前的最后提交；标题为“网格生成器阶段完成”。它是工程验证版里程碑，不是通用工业资格认证。
- `codex/mesh-maintenance` 从 `664ac7c` 开始，承接网格生成、导入、质量及网格桌面功能的独立维护。
- 自研 CFD 从 `c933a55`（扩散基础）开始，`0350b3d` 形成0.4.0层流初版；`codex/cfd-development` 承接后续 CFD 工作，`main` 保存已验收的集成状态。
- 网格修复可先在维护分支验证，再合并到 CFD 分支及 main 并执行相关回归；标签不随分支移动。无需另复制一份仓库或混入三维目录；唯一工作区现已切至 CFD 开发分支；main 保留此前验收状态。

## 当前连续目标：可验证的二维工程 CFD 软件

用户要求扩大长期目标，已建立新的持续目标。0.4.0 稳态层流初版是已完成的基础，不代表本目标完成。开发在 `codex/cfd-development`，阶段验证后提交并推送；main 只集成已验收阶段。保留 `mesher-v0.3.0` 和网格维护分支。

| 顺序 | 要交付的能力 | 完成依据 |
| --- | --- | --- |
| 1：精度与性能基础 | 稳态 FVM 的共享面动量守恒、边界配置、有界高阶对流、可信壁面受力；固定稀疏结构、可复用工作区和适宜压力预条件 | 先记录现有程序的时间、内存、线性迭代；同输入/同容差比较优化前后；解析/制造解、至少三档细化和域敏感性，报告误差而非只报残差 |
| 2：非定常与输运 | 非定常不可压层流、被动标量、恒物性热输运；CFL、物理时间、重启 | 解析衰减/输运、非定常圆柱及网格/时间步细化；守恒、频率/受力、重启一致性；禁止用稳态迭代震荡冒充物理时间 |
| 3：工程湍流 | 固定并标注具体版本的 k-omega SST RANS，必要时 URANS；明确壁面处理与 y+ | 参考 NASA TMR 的模型定义和标准例；先平板，再有压力梯度/分离的二维算例；近壁、域和网格敏感性，不以一张云图验证湍流 |
| 4：可压缩单相流 | 理想气体质量/动量/总能量守恒、适合激波的有界通量、可压缩边界 | 激波管解析解、等熵涡及喷管/激波标准例；密度压力正性、守恒、波速/位置和细化；不能把不可压压力修正简单套过来 |
| 贯穿各阶段 | 桌面工况配置、取消/失败诊断、残差和物理量监测、场显示、重启与导出；三平台运行 | 每个物理功能完成可操作闭环；macOS/Linux/Windows 分别实测，源码可编译不代替实际运行 |

规模目标是 **1万、10万、50万最终单元的真实求解**，分别记录网格/工具哈希、硬件、耗时、峰值常驻内存、线性与非线性迭代、停止原因和正确性。百万级先做资源资格试验。完整收敛和固定步数吞吐分开记录；矩形规则网格与真实 Cut-cell 分开验证，不能按网格数量推断运行时间。基线出来后再设有依据的速度目标，不提前编造“几十秒算完”的保证。优先测量和改善稀疏存储、重复分配、压力预条件；并行化在串行结果可靠后验证。使用公开数值库时如实标注实现来源和许可，不将库功能说成原创算法。

工程能力按工况验收：离散方程收敛、质量/动量/能量守恒、目标物理量、网格/时间步/域敏感性、标准或实验对照分开判断。误差允许值须在对应算例运行前给出依据；没有全软件统一的残差“及格线”，不重建 Q1。实际 OpenFOAM 对照可用时执行；目前 Docker 被手动暂停，不能伪报运行。三维、多相、燃烧、LES/DNS 不属于本次目标，避免无限追加范围。

张量在二维应力和速度梯度中已经适用。已查殷雅俊《广义协变导数与平坦时空的协变形式不变性》，它提供广义协变微分/变分理论；是否对应用户所学课程仍待确认。当前未发现它能直接加速本稀疏求解器的实测依据。若提出具体算法，先以相同精度、网格和硬件对照评估再引入，不以理论名称替代性能证据。参考来源与路线入口见 DEVELOPMENT_CN。

已加入性能观测入口并完成下述基线；新增 `--profile` 只输出诊断，不修改离散格式、收敛条件或网格质量门。后续按资源预算逐档扩大。当前机器为8 GiB内存，启动本轮时可用磁盘约13 GiB；计算串行执行，先限制单例180秒、保留最终场与日志，未经基线评估不直接启动50万/百万格长算。

## 当前性能改进：固定稀疏结构与 IC(0)

流动系统已从每轮动态 map 改成固定、按列排序的 CSR 连接表，五份矩阵复用连接结构和数值数组，U/V/压力复用线性工作区。压力默认自然顺序 IC(0)–PCG，CLI 保留 `--pressure-preconditioner jacobi` 对照。未改变物理离散、网格门、3000次线性上限或真残差/外迭代停止条件；不做对角移位或隐藏回退，分解失败明确报错。独立矩阵回归检查已知解与真残差，不仅检查“返回成功”。

同一8 GiB Mac、系统 clang Release，通道每种方法三次取中位数；圆柱每种方法仅一次。原 map/Jacobi 来自紧邻的上一基线，CSR/Jacobi 与 CSR/IC(0) 使用本次同一二进制，计时有操作系统调度波动，不作通用速度承诺。

| 工况/单元数 | 原 map/Jacobi | CSR/Jacobi | CSR/IC(0) | 原时间/当前时间 |
| --- | ---: | ---: | ---: | ---: |
| 通道3,596 | 1.655 s | 1.201 s | 0.528 s | 3.13 |
| 通道15,372 | 6.250 s | 4.683 s | 1.687 s | 3.70 |
| 圆柱4,880 | 41.818 s | 31.591 s | 18.369 s | 2.28 |

压力累计迭代从15,270/15,246/324,230降到3,518/3,226/123,687；对应外迭代仍是44/21/336。通道与圆柱的最大场差约6.86e-12，最大面通量差约3.15e-14，原离散方程未替换。新核心的两档通道、两档方腔和 Cut-cell 圆柱共五例独立几何、通量、解析/参考值检查通过；圆柱阻力仍约2.28728，其既有约11.85%参考差并未因求解变快而消失。

新增两档**真实局部加密通道**，含粗细连接，保持4×1 m几何、峰值速度1 m/s、ν=.01 m²/s。各运行一次，并在最终原生网格上重新独立检查几何、质量守恒和解析速度/压力：

| 最终单元数 | SIMPLE次数 | 求解耗时 | 进程峰值 RSS | 速度相对 L2 | 压降梯度相对误差 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 112,368 | 18 | 40.02 s | 176.59 MiB | 4.477e-6 | 1.919e-4 |
| 500,472 | 15 | 164.24 s | 763.09 MiB | 9.563e-7 | 9.287e-5 |

两例都满足原停止条件。流量相对误差分别2.028e-6、4.921e-7；生成参数、网格/二进制/字段哈希及系统原始计时进入证据。五十万格上限设为300秒、进程组RSS 1.2 GiB，实际未触发，整进程耗时171.79秒。表中求解耗时不含读入和导出；峰值RSS是系统测量，不能和进程组抽样峰值混用。通道初值为解析抛物线，两档局部加密框宽不同；它们是规模试验，不据此声明严格网格无关性、任意十万格绕流同样快或湍流可用。百万格尚未试验。

系统 clang 完整构建、CTest **103/103**、前端 **106/106** 通过。新增线性代数测试实际编译运行；Luna执行固定验收，主代理复核原始日志。

[性能与独立验证证据](../artifacts/current/native-flow-sparse-performance.json) · [五十万格真实网格与流场](../artifacts/current/native-flow-500k.png)；大规模最终字段在 `outputs/native-flow/sparse-scale/`，原/新方法对照在 `outputs/native-flow/sparse-performance/`。只新增少量证据入库，大网格不进Git。此阶段尚未重新打包桌面0.4.0，旧安装包仍使用此前求解器；外部 checkMesh/OpenFOAM 本次未运行。下一步回到稳态物理基础：保守压力力、边界条件与高阶对流验证，同时保留这些固定性能对照。

## 工程扩展起点：性能基线

在 macOS 26.6.2、MacBookAir10,1、8 GiB、系统 clang Release 上串行测量。通道各重复三次，表中取求解秒数中位数及峰值 RSS 最大值；圆柱只有一次测量。读入与导出不计入“求解秒数”，进度输出计入；日志保留整个进程耗时及系统 RSS。不是跨机器速度保证。

| 最终网格/工况 | 单元数 | SIMPLE 次数 | 求解秒数 | 压力线性求解占比 | 峰值 RSS |
| --- | ---: | ---: | ---: | ---: | ---: |
| 通道 | 3,596 | 44 | 1.655 | 78.5% | 8.99 MiB |
| 通道细化 | 15,372 | 21 | 6.250 | 88.2% | 31.24 MiB |
| 圆柱 Re20 | 4,880 | 336 | 41.818 | 89.3% | 11.75 MiB |

通道以解析抛物线速度初始化，更细网格的迭代次数反而较少；不能将其时间外推到任意复杂流动。压力单次 PCG 最大迭代数从通道粗档406增至细档811，累计压力迭代为15,270/15,246，支持下一步优先研究压力预条件与稀疏运算。这是优化前基线；后续加速与十万/五十万格实测见上节。

三例独立读取几何和面通量、算例误差检查通过；通道速度相对 L2 为 1.65875e-4 → 3.30287e-5。3,596格通道和4,880格圆柱的原六份物理文件与0.4.0正式证据逐字节一致。profile 开关不改变物理解、迭代上限仍明确 `converged:false` 的回归已加入。Luna 完成代码统计口径复核，主代理核对实际结果；统计字段明确称“线性求解耗时”，不冒充包含组装的整阶段时间。

[性能及正确性证据](../artifacts/current/native-flow-performance.json)；完整数据与系统日志位于 `outputs/native-flow/performance-qualified/`。初次沙箱内 `/usr/bin/time -l` 因只读 sysctl 受限而无法测 RSS，日志保留在 `performance-baseline/`；表中使用获准读取系统计时后的完整测量。系统 clang 完整构建、CTest **102/102**、前端 **106/106** 通过。此次未改物理算法、前端或重新打包；外部 OpenFOAM 未运行。

## 原生二维稳态层流与桌面 0.4.0：本地交付

新增 `cartmesh2d_flow_cli`，直接读取最终多边形 `.solver.cm2d`，重查几何/拓扑与原 Solver 门。实现不可压、恒物性、稳态层流的单元中心 FVM、SIMPLE、Rhie–Chow、共享面对流/黏性通量及非正交修正。动量一阶迎风、Jacobi–BiCGStab；压力修正专用 Jacobi–PCG。停止条件与单位有明确公式，见 DEVELOPMENT_CN；不是新的模糊质量评级，也不是所有 CFD 软件的统一残差门。

| 真实最终网格 | 单元数 | SIMPLE 轮数 | 独立数值对照 |
| --- | ---: | ---: | --- |
| 矩形通道粗/细 | 780 / 3,596 | 54 / 44 | 速度相对 L2 7.727e-4 → 1.659e-4；流量相对误差 7.599e-4 → 1.539e-4 |
| Re100 顶盖方腔粗/细 | 196 / 900 | 111 / 403 | Ghia 中心线 RMSE 0.02773 → 0.01230（以顶盖速度归一化，含实际壁值的插值）；最大差 0.05741 → 0.02740 |
| Re20 圆柱 Cut-cell | 4,880 | 336 | 逐格连续性 1.65053e-14，全局相对失衡 9.65630e-15；有限场、质量守恒和预设工况检查通过 |

圆柱仍是32段折线，直径2 m、速度1 m/s、ν=0.1 m²/s，域为42×42 m，四向距物面约10D，上下为滑移。Fx/(ρ·depth)=2.28728 m³/s²，Fy 接近0；换算 Cd=2.28728，相对 Dennis–Chang 1970 的开域 Re20 参考2.045高约11.85%。这个比较仅作背景诊断，几何、有限域和离散均不完全相同；未证明网格/域无关或阻力工程精度。近场旧域出现出口回流后被明确拒绝，未通过改检查掩盖。

独立 Python 验证器重算真实 polygon 面积/质心/边关联，逐格累积 owner/neighbour 反号通量，核对 JSON 与 CSV 残差、解析速度/压降和参考中心线。五例使用最终二进制通过，网格/字段/二进制有 SHA256。脚本的算例误差阈值是本轮回归范围，不是通用 CFD 验收标准；完整数值而非单个 PASS 保留在证据中。

细通道曾在首轮压力线性求解达到3000次上限；使用能保留共轭方向的压力 PCG 后，在原阈值、原3000步上限内解决。真实3596格失败输入已进入 CLI 回归。另明确拒绝不连通流体、数值溢出、错误工况/未分类边界；封闭腔固定 cell0 压力，开放工况以右出口面压力0为基准；局部及全局质量守恒同时参与停止条件。

桌面在当前网格上设置外流/矩形通道/顶盖腔、ν、速度与上限，可取消真实子进程，切换速度/运动学压力色图并导出六份流动文件。部分复制失败会清理不完整顶层结果，失败/取消保留诊断；上限退出2明确显示未收敛。实际打包 App 生成4,880格圆柱、336轮收敛、显示和导出通过；ZIP 中最终网格及六份流动结果与独立验证逐字节一致，重算最大逐格积分失衡5.93969e-15。测试还覆盖未收敛显示、回流失败和真实进程取消。

系统 clang Release 完整构建、**CTest 102/102、前端106/106**；macOS arm64 打包及实际运行通过。新求解功能尚未在 Windows/Linux 实际运行，旧三平台网格生成证据不替代本轮求解器验收。本轮再次确认 Docker 手动暂停，**未运行外部 checkMesh/OpenFOAM 求解对照**。

[数值与桌面证据](../artifacts/current/native-flow.json) · [真实桌面](../artifacts/current/native-flow-desktop.png) · [场图](../artifacts/current/native-flow-fields.png) · [参考对照](../artifacts/current/native-flow-benchmarks.png) · [残差历史](../artifacts/current/native-flow-residuals.png)。完整数据在 `outputs/native-flow/formal-final/`，真实 App/ZIP 在 `outputs/native-flow/desktop-final/`；旧失败在 `coarse-probe/` 和 `formal-v1/`，没有覆盖为成功。安装包 `desktop/dist/CartMesh2D-0.4.0-arm64-mac.zip`，根目录启动脚本指向本次 App。

## 自研 FVM 扩散/泊松基础：已验证

新增独立 `build/cartmesh2d_fv_cli` 和 `cartmesh2d_fv` 库，直接读最终 `.solver.cm2d`，使用真实二维面积、共享边法向、加权最小二乘梯度、非正交扩散修正和 Jacobi-PCG。每条内部边通量两侧反号装配；重查拓扑和原 Solver 门，不依赖 AUDIT 自报。支持正的常数扩散系数、常量源及全部 Dirichlet 边界；尚不是 Navier–Stokes 流动求解器，尚未进入桌面包。

- 固定输入 `examples/acceptance/circle.xy`、同一域 `[-1.93,-1.97,2.07,2.03]`、固定物理加密带 .375 m。实际壁面格宽 .125/.0625/.03125，最终 **480 / 1592 / 5860** 个单元，包含真实切割和粗细连接。没有通过改变几何或计算域凑误差下降。
- 常量、线性、正弦制造解和内壁1/外域0扩散场，共 **12 例**完成。独立 Python 从 CM2D/CSV 重算面积、质心、关联、通量、RHS 及完整修正方程残差，核对原生结果，并绑定二进制/网格/字段哈希。
- 正弦制造解面积加权 L2 为 **0.0229412 → 0.00648431 → 0.00164611**；固定序列观测阶 **1.8229 / 1.9779**，不声明任意多边形普遍二阶。所有例满足预设完整方程 `||r||₂ <= 1e-12 + 1e-10 ||baseRHS||₂`；细档正弦实测 4.89010e-9，停止界限4.89765e-9。没有使用线性迭代的小残差代替非正交修正后的结果。
- 三档整体最坏逐格积分失衡 **7.7836e-9**、全局积分失衡绝对值 **2.4156e-8**。验证脚本早期额外尝试的绝对 `1e-10` 守恒门失败，记录保留在证据 diagnostics；它与原方程停止条件的量纲/尺度不同，现作为诊断报告，不把修改临时门槛说成原门已通过。生成器 Solver 门和数值停止条件未放宽。
- 独立审查找出的极大输入溢出假收敛已修复：`1e154` 边界值现在明确报数值范围错误并退出1；坏缓存索引、错误边关联/面积、非有限输入、未收敛上限都有最小回归。上限退出2并保留 `converged:false`，不包装成功。
- 系统 clang Release 构建无新增告警；完整原生 **101/101**、前端 **97/97**。真实场和误差图已人工复核。当前 Docker Desktop 被手动暂停，因此**本轮未运行外部 checkMesh/OpenFOAM 对照**；独立读取与解析解不能冒充它。新增求解工具只在当前 macOS 实测，未声称已有 Windows/Linux 求解器运行验收。

小型证据 [native-fv-foundation.json](../artifacts/current/native-fv-foundation.json)；[误差收敛图](../artifacts/current/native-fv-sine-convergence.png)、[三档制造解场](../artifacts/current/native-fv-sine-fields.png)、[三档实际扩散场](../artifacts/current/native-fv-diffusion-fields.png)。完整网格、12 例 CSV/VTK/日志在 `outputs/native-fv/verification-run/`；构建/测试日志在 `outputs/native-fv/`。复现命令、算法口径与当前边界条件限制见 DEVELOPMENT_CN 的自研求解器入口。

## 现有高密翼型：Re=1e6 实际 OpenFOAM 求解

按用户选择，直接复制既有 NACA2412 hybrid **140,305 格**（生成基线 `bf11105`）运行 OpenFOAM **2606 / simpleFoam / kOmegaSST**。源与计算副本的 points、faces、owner、neighbour、boundary 五文件 SHA-256 在求解前后完全一致；没有重新生成网格，也不将它当成当前核心新网格的验收。

- 恒定入口 **15 m/s**、参考弦长 **1 m**、nu=**1.5e-5 m²/s**、参考 Re=**1,000,000**、攻角 **0°**；湍强 1%、入口湍粘比初估 10。稳态、全湍流 SST 配 Spalding/omega 壁函数。前 200 步 upwind，之后仅 U 切换 linearUpwind，k/omega 保持 upwind。最初湍粘比偏高的试跑已停止并隔离，未混入正式结果。
- 标准 checkMesh **Mesh OK**，最大非正交 **69.8112°**、最大 skewness **3.8218**。扩展 `-allGeometry -allTopology` **仍失败 1 项，2720 个 concave cells**，另有 4 个短边关联点提示；未运行原厂质量配置组合，不声称扩展质量通过。
- 正常运行到 **3500 次 SIMPLE 迭代**，没有发散或负 k/omega/nut；全部最终单元字段有限。首个初始残差 Ux=**1.84e-7**、Uy=**1.03e-7**、p=**2.22e-6**、k=**6.74e-8**、omega=**1.66e-8**。压力未达到原 `1e-6` 停止条件，日志没有 `SIMPLE solution converged`；明确记录为**到达迭代上限，未通过严格收敛条件**，不使用后续压力修正残差代替。
- 最终最大速度 **19.1303 m/s**；进出口相对流量不平衡 **4.23e-12**，出口 **0/256** 面回流。Cd=**0.0137050**、Cl=**0.202453**；最后 35 个输出样本（3160–3500 步）的范围/均值分别约 **0.00203% / 0.02471%**，只是稳定性测量，不替代残差和精度判定。CmPitch 的正向以实际输出头 `pitchAxis=(0,0,-1)` 为准。
- 最终重新计算壁面 yPlus：min **3.6023**、max **24.5516**、算术平均 **14.9366**，按真实壁面积独立加权平均 **14.9586**。上下游边界仅距翼型约 **0.5c**，上下是 slip；这个受限域不能冒充自由远场。四层及当前近壁尺度没有证明 SST 壁面解析精度，未做域无关性、网格无关性、转捩或实验对照。
- 速度/压力图直接读最终场；流线只作局部流体内插值并遮挡实体。Cp 明确使用**出口表压零点**，不当作标准自由流 Cp。壁面 Cp 仍有可见尖峰，原输入为 80 点折线；本次没有确认尖峰来源，保留曲线供后续定位几何拐角、壁面网格和离散格式影响。

复现工具为 `tools/verification/run_airfoil_rans.py` 和 `tools/visualization/render_airfoil_rans.py`，具体命令见 DEVELOPMENT_CN 的 CFD 验证节。正式算例、原始日志、最终场与 ParaView 入口 `case/airfoil.foam` 位于忽略提交的 `outputs/airfoil-rans-Re1e6-final/`。四张 PNG 和含原始终末日志、配置、哈希的证据位于 [airfoil-rans-Re1e6.json](../artifacts/current/airfoil-rans-Re1e6.json)。两名 Sol 子代理分别复核工况/口径和实现后处理，主代理核对原始日志与最终图片。实际求解四段 Docker 包装耗时合计约 **2157 s**，不是通用性能基准。本轮只新增试算/后处理工具与证据，脚本语法及实际运行验证通过，未改 C++/前端、未重复宣称其完整测试已重跑。

## 跨平台桌面构建

macOS / Linux / Windows 已分别完成本机构建、打包与实际 App 验收。最终源码 `132adad` 的 [desktop-platforms 运行 34689931686](https://github.com/wuhahaTWT/cartesian-mesh-generator-2d/actions/runs/34689931686) 三个平台全部通过；运行包在对应系统完成测试和实际导入/生成/导出、独立读回后才上传。

- 用流式 ZIP 库替代 macOS 专用 ditto，保留 UTF-8 文件名和空目录；原生工具、11 个样例与平台 manifest 随包分发，打包时读取 PE/ELF/Mach-O 头并检查系统/架构，拒绝错误二进制混装。
- Windows 使用系统标准窗口控件、静态 CRT、UTF-8 源码与进程路径 manifest。精确方向谓词在 MSVC 上使用标准 32 位 limb + 64 位乘积；支持 128 位整数的编译器保持 64 位 limb 路径。二者保持相同的整数容量、精确符号与容差，新增消去/溢出/极端指数回归。
- 三个平台分别通过前端 **97/97**、原生 **99/99**，实际运行打包 App，在含中文与空格的路径下完成 PNG/JPG/hybrid 三例，ZIP 独立读回均通过。macOS 的 PNG/JPG 为 5,128 格，Windows/Linux 为 5,130 格；混合圆均为 5,348 格。本机还单独验证了标准 32 位 limb 路径的 99 项测试；不宣称不同编译器生成逐字节一致。
- 修复了 Windows 超时验证对 POSIX 信号的依赖；App 和导出 PNG 随包加载获 OFL 授权的中文字体，解决无中文系统字体的 Linux 方框字。最终 App 验收检查字体实际加载，并人工复核 Windows/Linux 界面和导出图片。
- 本轮未运行外部 OpenFOAM checkMesh 或 CFD；独立 OpenFOAM 文件读回不代替目标求解器原生检查。Windows CI 实际系统是 Server 2022，Windows 10/11 用户桌面与更多 Linux 发行版仍需实际使用反馈。

平台要求和下载/构建方法见 DESKTOP_APP_CN 与 DEVELOPMENT_CN。小型证据与截图在 `artifacts/current/desktop-platforms*`，完整日志位于忽略的 `outputs/platform-validation/`，Windows 运行包已下载到 `desktop/dist/platforms/windows-x64/`；Linux 包由上述工作流的 `CartMesh2D-linux-x64` 附件下载，本地慢速副本下载未完成。仅 Windows x64、Linux Ubuntu 22.04 x64、macOS arm64 进入当前自动验收矩阵；未宣称所有发行版或 CPU 架构已验证。

## PNG / JPG 图片输入

桌面“打开文件”现接受 PNG、JPG、JPEG。图片在本地解码，Worker 自动选择阈值并提取二维闭合轮廓；支持明暗、透明度、背景色模式、点选主体/保留多个区域和显式线稿填孔。默认保留孔洞；小噪点移除、未选主体与简化回退会显示提示。红线叠加预览后必须输入轮廓整体的实际宽度，换算为米再接入原生 XY 生成链；不会猜测照片尺度或恢复三维形状。

- 输入上限 20 MB / 4000 万像素 / 单边 20000；识别最长边 1024 像素，默认约 1 处理像素简化。保留环方向、相交和面积检查；64 环、4096 输出点上限，复杂输入明确拒绝。增加网格数量不能恢复图片损失的几何细节。
- 主进程保留所读原图快照与 SHA-256，确认后再次校验轮廓并标定；源单位锁定，避免重复换算。ZIP 增加原图、`image-outline.png`、`image-import.json`，沿用真实网格 PNG 和中文说明。
- 实际打包 App：透明 PNG L 形、JPEG L 形均得到 **5,128** 个最终单元；闭合矩形线稿显式填孔后 **5,016** 个。均按轮廓宽 200 mm 标定；主代理从最终 ZIP 独立读取 XY 和 OpenFOAM，核对宽 .2 m、源图哈希、最终单元数及导出图片。三例真实 OpenFOAM 2606 **标准 checkMesh 均 Mesh OK**，最大非正交分别 33.2919°、33.2919°、18.4349°；未运行扩展检查或 CFD 求解。这些是合成格式验收图，不是任意实拍照片成功率评估。
- JPEG 椭圆成功识别，但默认小规模生成仍被现有 Solver 质量门拒绝（7 项，含非正交 84.5815° > 70°、插值权重和体积比）。未放宽门槛、未导出为成功结果；保留输入和失败诊断。图片导入能力不代表通用核心鲁棒性已经修复。
- 空白图明确拒绝，取消保留旧几何；未标定时禁用确认，参数变化当场使旧轮廓失效；最终包实测 800×560 窗口确认按钮可达。JPEG 生成后返回开始仍能导出含图片记录的上一份结果。

本轮前端 **95/95**、原生 CTest **99/99**；原生算法和阈值未改。证据 `artifacts/current/raster-import.json`，复现图片与识别界面在同目录；完整日志/ZIP/网格位于忽略的 `outputs/raster-import/`。使用步骤见 DESKTOP_APP_CN 的“用照片或图片生成网格”。

## 陌生几何与导入范围抽查（图片功能加入前）

本次使用 `0ba7461` 打包 App 的同一组后端二进制，固定 Lref=2 m、wall/Lref=.02、background/Lref=.08、band=3、padding/Lref=.5；hybrid 三层、首层/Lref=.005、growth=1.2。没有针对单个形状反复调参。只验证小规模生成与文件读回，没有运行这些新例的真实 OpenFOAM checkMesh 或 CFD 求解，也不是完整 GUI 自定义文件导入的逐例验收。

- L 形、五角星凹多边形、普通 U 形凹槽、带凹口尖角、两个分离实体：两种模式均成功；最终导出 pure 2,056–2,440 格，hybrid 2,778–4,561 格。U 形槽宽 .9 m、普通双物体间距 .4 m，不将它们称为极端窄缝。
- 带孔内流：pure 最终2,004格成功，hybrid 因嵌套轮廓明确拒绝。自交蝴蝶结和重复顶点形成的零长边在 pure 中正确拒绝，不计为有效几何生成失败。
- .01 m 间隙双矩形（请求壁面 h=.04 m）：pure 2,112、hybrid 3,352 格成功。y=.12345 m 截面缝宽被完整保留，但只横跨两个单元；hybrid 保留468个层单元/请求756，首层壁长覆盖71.44%、完整三层覆盖60.46%。生成成功不等于足够的缝内分辨率或完整层覆盖。
- 共13份成功结果实际导出 OpenFOAM，独立 `check_openfoam2d.py` 读回全部通过，主代理逐份核对最终CM2D与reader的cell_count、valid和issues。pure 首轮摘要的 stabilized_cells 与最终 Solver 分割后数量不同，证据分别记录。这不是目标求解器的原生 checkMesh；这些案例没有 CFD 精度结论。
- 当时 App 不接受 PNG/JPG 位图轮廓，现已按上节加入。SVG 的 transform 会警告但未执行变换，圆角 rect 可能被跳过后继续导入其他元素，未闭合 polyline 会警告后自动闭合；width/height/viewBox 的物理尺寸语义没有完整应用。用户预览确认有帮助，但应优先补齐保形导入与明确拒绝机制。
- 旧 `hidden-spline-bulge.dxf` 漏检在当前打包导入器本次未复现：请求弦高 .001 m，曲线采样62段；独立10,001点密采样到导入折线的最大距离约 .00076946 m。这只是固定反例的有限采样复核，不代表通用严格误差上界已证明。

输入坐标、结果及主代理复核证据：`artifacts/current/shape-import-probe.json`；完整命令、原始日志与 case 位于忽略的 `outputs/shape-probe/`。下一步优先建立用户真实轮廓的导入保形、默认参数生成、边界条件与目标求解器验证闭环；图片识别及强制宽度标定现已接入，复杂背景与透视仍需人工处理。

## 桌面补充：回到开始、预览与导出图片

- 标题栏新增“回到开始”：清空几何/网格画布、结果面板及绘图缓存，释放主进程持有的预览网格；上一份磁盘结果继续可导出，开始下一次生成后替换。保留主题、显示模式与参数。生成和导出期间按钮禁用，生成可先取消。
- 大网格按 64 个单元分块缓存世界坐标 Path2D；缩放/平移只提交与视口相交的块（保留线宽余量），边界单独缓存，鼠标与窗口变化合帧，画布仅在像素尺寸变化时重分配。完整网格不抽样、不删单元，返回开始释放全部块缓存。
- ZIP 新增 `mesh-preview.png`（2000×1080 白底全景＋壁面局部）和 `README_CN.md`（简短中文文件说明）。图片独立读取当前最终网格，与界面主题/相机无关；边界层颜色使用最终网格 lineage，不套固定示意图，不声称外部 CFD 检查已运行。新增文件采用 ASCII 名称，避免跨系统解压文件名乱码。
- 固定喷管 148,287 单元在打包 App 中生成、返回开始前后分别导出，两份 ZIP 的最终 CM2D 均与上一核心交付 SHA-256 完全一致，PNG 字节也一致。返回后网格、路径缓存和主进程预览引用均释放；800×560 布局与保留导出入口实测通过。本次没有更改原生网格算法、外部阈值或重跑 CFD。


本次验证：前端 **78/78**、现有原生 CTest **99/99**，macOS 打包及真实高密 App 导出通过。148,287 单元、900×700 CSS 像素画布、连续 6 次缩放并用 `getImageData` 强制同步回读：修改前稳定约 **17.94 s**、单个大路径缓存约 **0.677 s**、最终分块版约 **0.190 s**。这是指定本机与强制同步绘图条件的对比，不是日常交互 FPS 或任意规模的流畅保证。证据见 `artifacts/current/start-export-evidence.json`，PNG 与开始页截图位于同目录。

## 本次交付：取消 Q1、修复目标求解器质量、侧栏伸缩

用户明确取消未经审核的 Q1：当前源码、CLI、桌面和导出不再执行或输出该合同。几何拓扑、内部 Solver、方向连通以及实际外部检查继续独立报告。历史 Q1 失败记录保留，不能解释为本次网格仍在进行 Q1 评级。

固定喷管保持原几何、Lref=6、wall/Lref=.00125、background/Lref=.0125、band=40、四层及首层/Lref=.0003125、growth=1.2、挤出厚度 .02。对连接区做精确并合/重划分，148,375 → **148,287 个最终单元**；保留 **8,768 个壁面层单元**，lineage 失配 0。新 mesh 不通过更换参数或修改外部阈值获得结果。

- 非正交：原 OpenFOAM `>65°` 的 448 面 → **0**；最大 69.9349° → **64.9644°**。
- 官方质量配置的 all-face determinant：4 个角部单元 / 20 个问题面 → **0**；独立复算最小值由 .00091742 提升到 **.00512556**（阈值 .001）。该指标包含前后 empty 面，取决于挤出厚度；与旧 internal-face 方向指标是不同公式。
- 完整几何检查仍报告 **5,677 个共面拆分单元**，不写总 PASS。独立 face-plane 复算 positive-side=0，全部 near-coplanar，集合与 OpenFOAM 一致；另保留 16 个短边关联点提示。这是保留共形粗细连接产生的共面侧面，未删除单元或屏蔽日志。
- 质量改进存在取舍：最小 face weight 为 .051547（限 .05）、volume ratio 为 .010371（限 .01），仍过原阈值但低于修复前。没有声称所有指标同时改善。
- 新增两个真实最小回归：喷管 face69310 的 66.794° 连接面、cell33552 的低 determinant 三角形；验证目标修复、面积、锁定层不改动和 determinant 缩放。固定案例通过不代表任意几何、任意 CFD 工况均合格。
- 界面仅显示“现代简约 / 科研终端”，保留背景插画，网格画布不透明。标题栏分栏按钮及 Cmd/Ctrl+B 切换设置侧栏，状态保存在本机；窗口最小 800×560，窄窗工具栏换行、结果区纵向排列和滚动。修复了反复收展侧栏导致预览持续缩小的问题，缩放和视图中心可恢复。

本次最终验证：系统 clang Release 构建和 macOS 打包成功；CTest **99/99**、前端 **74/74**。打包 App 实测侧栏、快捷键、800×560 布局及高密生成/导出；CLI、App 与 ZIP 内 CM2D SHA-256 均为 `1f6d8db4…4d24468a`。实际 OpenFOAM 标准检查 PASS；完整检查及完整+原厂配置组合仍因上述共面项 FAIL，原厂质量配置各项超限为 0。未新增 CFD 流场求解。

以下规模、CFD、旧桌面性能矩阵为 **修复前基线证据**，只对应各自记录中的二进制和网格；不能直接当成新核心全量重跑。最新构建、桌面与外部检查证据见 `artifacts/current/target-quality-and-sidebar.json`。

## 密度、单位与最终测量

- 两条 CLI 与桌面使用共同尺寸场：显式参考长度 `Lref`、壁面 `h/Lref`、背景 `h/Lref`、留白/Lref；贴体首层和挤出厚度也可按 Lref 设置。树深度是派生诊断。自动参考取包围盒最长边，随旋转可能变化；需要旋转不变参考时显式设置 Lref。
- XY/CSV/TXT/SVG 按源单位换算为米，不隐式归一到 1 m。非 DXF 的“自动”按米读取并提示。SVG CSS/viewBox/transform 的全部物理语义尚未覆盖。
- `.resolution.json` 测量**最终求解拓扑**：实际单元数、面积等效尺寸分布、壁面边长、owner 切向/法向跨度，以及切向跨度超标的壁长占比和边 ID。请求尺寸不等于最终已实现尺寸，面积等效尺寸不能替代边界层方向尺寸。
- 层保留检查逐个核对源层 polygon、最终几何、唯一来源、wall owner 和连续 owner/neighbour。首层覆盖、完整请求层数覆盖、首层高度和超标比例分别报告。纯路径为 `not_requested`；fallback 为 `no_layers_retained`；缺失/改形/断连为 `incomplete`。
- 按壁面目标细分输入直线段，精确保留输入角点；细分不恢复原始折线没有表达的曲率。圆基准实际是固定 32 段多边形，不能当成精确圆的几何误差验收。法向尺寸不是经认证的 y+。
- 桌面实际能力表中，纯 Cut-cell 默认安全深度为 11、hybrid 为 8；此前笼统写为 8 不准确。高于对应默认深度仍需显式允许，质量阈值不变。本轮增加按数量的自动配置，范围和局限见下节。

## 桌面 0.3：双主题、数量配置与图片托管

- 新版提供“现代简约”与“科研终端”两套外观，独立于网格显示模式，localStorage 记住主题。背景同人插画由内置 imagegen 生成，作为淡化的全界面背景持续显示，侧栏保留角色近景；首页为大幅背景，实际几何/网格画布保持独立不透明底色。原生 details 展开诊断，显示内部拓扑、Solver 与方向连通结果；外部检查另外运行。
- 自动数量档位为约 5 千、1.5 万、5 万、15 万、30 万，以及 1 千至 50 万自定义。目标范围为 ±30%，不是精度等级或任意几何成功承诺；30 万仍标为规模试验。旧常规/更密预设保留兼容入口。
- 初值按流体面积与数量预算选择较粗的可实现背景层级，为加密带预留约一半预算；多环使用明确标记的包围盒面积初估。原生求解仍保持真实 domain-solid 语义。最多 3 次按实测数量调整带宽或尺寸；混合层回退时可在同一总次数限制下尝试首层 0.65/1.5 倍。域留白、内外流、所选方法和质量门不被数量搜索改变。
- 圆柱、翼型、喷管可一键载入既有高密案例参数，明确替换 Lref、域留白与高深度选项，转入手动检查；该入口与保持计算域的数量搜索分开。
- 界面显示初值、实际数量、是否达到范围；保留最接近目标的成功结果，后续失败不覆盖它。可把建议或实际参数转入手动，提供整体粗/细按钮。首层几何初值为壁面目标的 1/4，仍无 y+ 或流场误差驱动。
- GitHub 图床直接使用现有公开仓库的 `展示素材/` 六张 PNG。Markdown 使用编码后的 raw.githubusercontent.com 链接，六图已匿名 HTTP 200 读取且与本地 SHA-256 一致。用户额外创建的本地 `展示素材.zip` 未纳入源码。

本轮数量初值矩阵（Lref 为圆 2 m、喷管 6 m，留白 0.5，alpha=0.1，显式允许高深度）：

| 几何/方法 | 目标 1.5 万 | 目标 15 万 |
|---|---:|---:|
| 圆/纯 Cut-cell | 14,764 | 142,444 |
| 圆/hybrid | 11,211 | 114,936 |
| 喷管/纯 Cut-cell | 16,632 | 114,352 |
| 喷管/hybrid | 回退被拒绝 | 180 秒超时 |

六个成功结果均进入 ±30% 范围；喷管混合路径仍存在连接区 Solver 质量问题，不能把其纯回退输出当成混合成功。首层重试与小扰动诊断仍回退或超时，单独保留在 `outputs/desktop-budget-validation-r2/`，不抹掉初始失败。原先手动验证的 148,375 格喷管仍见下节；数量初值还不能替代每个几何的工程配置。

实际打包 App 的混合圆“约 15 万”得到 114,936 格，预览、CLI 与 ZIP 中 CM2D 的 SHA-256 完全相同。该网格与 142,444 格纯圆都通过独立读取器（含 native 方向报告核对）和真实 OpenFOAM 2606 标准 checkMesh；扩展检查均 FAIL，分别标记 7,700 / 1,360 个 concave 单元，Q1 FAIL 保留。现代主题默认约 5 千档得到 5,204 格，并实测主题持久化、切换不改变网格以及实际参数转手动。

新打包 App 使用“载入已验证高密样例参数”生成喷管 hybrid **148,375** 格，预览与导出数量相同，App/ZIP 的 CM2D 与此前精确参数 CLI 逐字节一致（SHA-256 `463850cc…4d8791`）。该按钮可复现固定成功案例，不能据此声称通用喷管自动数量配置已修复。

背景后续调整已在打包 App 中以 5,204 格小例验证：选模型后背景仍在，网格画布底色不透明，主题切换不改变网格；前端 74/74。该历史版仍带 Q1；当前已按用户要求删除。证据与截图在 `artifacts/current/desktop-wallpaper.json/png`（截图后缀为 `-welcome` / `-mesh`）。

上述桌面基线轮没有修改 C++ 网格算法和质量门；本次目标质量修复见文首。实际数量控制、打包 App、独立读取与外部检查的最终证据汇总在 `artifacts/current/desktop-ui-refresh.json`；源码导航和复现参数在 DEVELOPMENT_CN.md。原生完整 CTest 101/101、前端 74/74 通过。完整 CFD 流场未重算，本轮外部验证为网格检查。

## 修复前核心的规模证据

下列高档和中档均以当前纯 CLI `052a1d51…cfb60bd6`、hybrid CLI `0467983d…a889058` 复现。最终数量是测量值，不是强制凑数目标。

| 几何 | 纯 Cut-cell 中档 | 纯 Cut-cell 高档 | hybrid 中档 | hybrid 高档 |
|---|---:|---:|---:|---:|
| 固定圆多边形 | 55,904 | 153,208 | 59,476 | 164,970 |
| NACA2412 | 66,207 | 133,810 | 68,806 | 140,305 |
| 喷管内流 | 52,824 | 141,488 | 56,348 | 148,375 |

十二例均真实导出：纯路径三类独立读取器通过，hybrid 四类通过；方向读取器明确使用 `--report` 核对 native 测量。十二例内部 Solver 和实际 OpenFOAM 2606 **标准 checkMesh PASS**；**Q1 与扩展 checkMesh 均 FAIL**。具体凹/近共面标记、实际参数和日志分别见：

- `artifacts/current/pure-high-regression.json`
- `artifacts/current/hybrid-circle-164k.json/png`
- `artifacts/current/hybrid-high-regression.json`
- `artifacts/current/engineering-middle-validation.json`

万格纯喷管额外固定为 **11,472** 格：wall/Lref=.0025、background/Lref=.0125、band=3、Lref=6，不增加远场。三类读取器、Solver、标准 checkMesh PASS；Q1/扩展 FAIL（968 个 concave-or-planar 标记）。证据 `artifacts/current/pure-nozzle-11k.json`。当前制造解细档另验证了 14,800 格纯圆，见 CFD 部分。此前同轮纯翼型低档 17,112 的历史记录保留于 `outputs/engineering-scale/`，不冒充本次最终核心的低档重跑。

尺度阶梯的 wall/background/band 参数并非全部只有一个变量变化，尤其 band=3→40 改变近壁加密范围；这些是规模/能力验证，不能直接当成严格网格收敛序列。真实 CFD 序列另列。

## 修复前有限缩放、旋转与边界层

当前核心对三种固定几何，各测缩放 0.001、1000 与旋转 17°，**9/9 hybrid 成功**，无纯网格回退/超时。四类读取器均通过并核对 native 方向报告。

| 几何 | 缩小 0.001 | 放大 1000 | 旋转 17° | 最终层单元 |
|---|---:|---:|---:|---:|
| 圆 | 16,672 | 16,674 | 16,680 | 1,280 |
| 喷管 | 8,428 | 8,424 | 8,172 | 2,400 |
| NACA2412 | 18,575 | 18,570 | 18,558 | 932 |

均 Solver PASS、Q1 FAIL。**没有运行该九例矩阵的外部 checkMesh**，不能借用原尺度外部结果。最终数量少量变化，不能宣称缩放后逐个单元拓扑一致；更不是任意几何成功率。此前纯路径的有限缩放/平移/旋转记录仍保留，历史失败也未删除。
证据：`artifacts/current/hybrid-finite-robustness.json`；前一核心的 9 例原始记录在 `outputs/engineering-hybrid-endpoint-matrix/`。

高档实际层保留：

| 几何 | 保留/请求层单元 | 首层壁长覆盖 | 完整四层壁长覆盖 | 首层高度超标 |
|---|---:|---:|---:|---:|
| 圆 | 5,120/5,120 | 100% | 100% | 0 |
| NACA2412 | 3,380/3,400 | 99.8467% | 99.2178% | 0 |
| 喷管 | 8,768/8,816 | 100% | 98.9033% | 0 |

三个高档层来源/连续性失配为 0。圆请求 wall/Lref=.0025，最终 owner 切向跨度 P95=.002453506365、最大 .002511985635，**切向超标仍占壁长 5%**。层全部保留不代表所有方向尺寸达标。

## 修复前性能、真实 App 与导出

当前高档资源复测使用系统 `/usr/bin/time -l`，主代理从原始日志读取 RSS，并将六份新 CM2D 与已检查产物逐字节核对。以下是诊断运行；存在其他验证工作，不能当成隔离性能基准或跨硬件承诺。

| 模式/几何 | native 秒 | maximum RSS bytes |
|---|---:|---:|
| hybrid 圆 164,970 | 23.8122 | 1,128,448,000 |
| hybrid NACA 140,305 | 33.2959 | 1,077,526,528 |
| hybrid 喷管 148,375 | 60.6987 | 1,142,833,152 |
| pure 圆 153,208 | 30.0022 | 694,026,240 |
| pure NACA 133,810 | 44.3776 | 972,439,552 |
| pure 喷管 141,488 | 49.2977 | 1,011,154,944 |

macOS `peak memory footprint` 与 maximum RSS 是不同口径；该批前者最大约 1.83 GB，不可混用。完整参数、wrapper 时间和两种内存指标在 `artifacts/current/engineering-resource-measurement.json`。资源包装器的三次 pure 执行误复用了原 OpenFOAM 输出目录；原 CM2D 未覆盖，六份新旧 CM2D 哈希均相同，实际命令和此问题已保留，不虚构独立 pure 资源案例目录。其他旧测量的 time 权限失败/RSS 缺失仍保留，不能把 null 当零。

此前同参数 hybrid 圆在 300 s 超时；此次第一份合格生成 native 21.8387 s。超时不是旧版完成耗时，不能据此计算正式加速比。该次 solver 候选全域重建仅 3 次，源修复 .798747 s、最终重划分 1.555667 s。

**此前实际打包 App 验证** 已完成同一高档圆：预览、OpenFOAM、ZIP 均 **164,970** 格，CLI/App/ZIP 的最终 CM2D 逐字节一致，侧栏底部可达。native **24.2598 s**；整个 App 启动、生成、渲染、ZIP、截图和清理 **111.680827 s**，桌面端到端耗时仍有优化空间。

默认深度 8 首次正确拒绝高档；随后使用已有显式高深度选项成功，默认上限和质量门未变。此结果纠正了此前“锁屏必然不能运行 App”的推断。截图已检查；Q1 FAIL 从真实 renderer DOM 读取，该行不在本张截图可见范围，结果区可滚动。
证据：`artifacts/current/desktop-hybrid-circle-164k.json/png`。此前纯圆 153,208 和贴体喷管 148,394 的实际 App/ZIP 证据属于各自旧核心快照，不冒充本次最新运行。

安装产物：`desktop/dist/mac-arm64/CartMesh2D.app`。使用系统 clang++，未签名打包；没有声称完成第三方签名、公证或所有机器的安装验收。

## 已有 CFD 与精度边界

已使用本地 Docker 的 `opencfd/openfoam-run:2606` 实际执行，独立读取、内部 Solver、Q1、标准/扩展 checkMesh、迭代收敛和空间误差分别报告。

### 制造解

固定多边形外域，解析调和场 `T=x²-y²+.15x-.05y`。实际生成、writeCellCentres/writeCellVolumes、laplacianFoam 和误差评估均已执行。

| wall h/Lref | 最终单元 | L2 误差 | Linf 误差 |
|---|---:|---:|---:|
| .04 | 1,292 | 3.179e-3 | 1.853e-2 |
| .02 | 4,144 | 1.037e-3 | 1.328e-2 |
| .01 | 14,800 | 3.200e-4 | 5.722e-3 |

三档末次线性残差 <1e-12；标准 checkMesh/Solver PASS，Q1/扩展 FAIL。最新核心已按原精确参数重新生成三档，最终 CM2D 均与实际求解输入逐字节一致，因此复用对应已有场和误差证据。它只验证指定标量方程与多边形域，不代表湍流、传热或任意几何精度。
原始结果：`outputs/engineering-cfd/mms/`；当前拓扑关联：`artifacts/current/cfd-topology-compatibility.json`。

### 固定喷管层流

原始 `nozzle_profile.xy`、Lref=6 m、挤出 .02 m、入口 U=(.1,0,0) m/s、nu=.01 m²/s、出口运动学 p=0、无滑移壁面、前后 empty。入口/出口明确绑定源段 81/40；这是验证工况配置，**产品分段 patch UI 尚未完成**。

| 最终单元 | 收敛迭代 | 运动学压降 m²/s² | 相对流量不平衡 |
|---:|---:|---:|---:|
| 2,152 | 279 | .08624959289 | 1.09e-12 |
| 6,200 | 574 | .08760781335 | 3.58e-12 |
| 52,824 | 3,952 | .08874178 | 1.14e-12 |

最新粗/细档 CM2D 与已有 CFD 输入逐字节一致。中档从旧 6,204 变为 **6,200**，已在全新目录重新运行真实 simpleFoam，未复用旧中档场。三档末次初始残差均 ≤1e-8；最新中档 Ux=1.12e-9、Uy=9.78e-9、p=9.40e-9/2.54e-10。标准 checkMesh/Solver PASS，Q1/扩展 FAIL；扩展标记分别 148/320/496。

更新后相邻压降差 **1.5503% → 1.2778%**，统一出口区间上的归一化速度积分 L2 差 **.00227855 → .00149993**。差异在减小，**尚未建立网格无关性或通用误差保证**。此前 6,204 中档的独立内侧截面后处理未显示单调收缩；该额外后处理未对新 6,200 中档重测，不套用其旧数值。

当前合并评估：`outputs/engineering-cfd-final-r02/current-three-grid-comparison.json`；新中档场/完整命令/原始日志：`outputs/engineering-cfd-final-r02/`；粗细场：`outputs/engineering-cfd/nozzle-flow/r01`、`r03`。汇总及更新图：`artifacts/current/nozzle-flow-validation.json/png`。可复现驱动：`python3 tools/verification/run_nozzle_flow.py --output-root outputs/nozzle-flow-new`，要求新目录；子集用 `--cases r02`。

## 先前核心改动与验证

- 输入角点的算术格点归属统一登记，完整格与 Cut-cell 使用共同输入端点；格点几何预算和舍入预算分开。
- 几何长度/面积容差恢复量纲一致；拓扑面积用局部原点计算。共享输入端点直接返回原点，避免相邻层边近共线重构出末位偏差造成误停层。hair-ray 端点判定按物理长度比较。保留三尺度真实失败回归。
- 不可变轮廓诊断缓存、共享边点索引与 R1 顶点索引减少重复搜索；不改变有效性门。
- 源质量候选带真实邻格，并比较相对原补丁的改善；最终批量支持 2→2 切分和 2→1 精确合并。原备用路径允许的凹合并仍受原 concavity/angle/aspect 限制；每次提交仍经过完整拓扑和 Solver 分数门，不触碰不可变层单元。
- 保留 18 源多边形和 21 最终单元的真实局部案例。后者候选全域重建 71→15，前后都保留面积、输入身份并通过 Solver。CM2D 抽取的单元身份只是诊断跟踪，不声称恢复全部 native provenance。
- 方向连通修复与独立读取适用于未耦合、平面、均匀挤出、前后 empty 的范围；不能冒充通用三维 determinant 验证。
- 面平面独立诊断保持原 dot > -1e-6；旧 140,306 格 NACA 的 2,724 标记与原 OpenFOAM 集合完全一致，全部近共面、无 dot > +1e-6。仅解释该固定网格，原扩展 FAIL 保留；当前其他凹面提示不可据此抹掉。`--expected-set` 不匹配现返回失败。

上一轮核心交付完整 **CTest 101/101（26.63 s）**、前端 **54/54**、macOS 打包通过。该轮新增桌面功能未改变原生算法；本次修复范围见文首。日志：`outputs/concave-union-final-{ctest,npm,package}.log`。

## 修复前质量审核依据复核

用户指出 Q1 未经本人审核后，重新核对源码、成熟工具资料和真实外部结果。本次是审核依据复核，未改变 Q1 / Solver 阈值、评级或网格算法。

- Q1 是项目内部诊断；未找到完整的逐项阈值来源及工况误差校准记录，不能把既有测试通过当成标准已经成立。原始面积比硬阈值 0.05 与允许的分级裁切配置并不总兼容：细格保留面积分数 0.1、邻格宽度为其 2 倍时，面积比可为 0.025。它仍可能提示数值风险，但不能把此项失败自动解释成拓扑非法或 CFD 必然失败。边界层仍只 OBSERVED。
- 推荐以目标求解器的独立工具为主：OpenFOAM 2606 `checkMesh` 标准检查、完整拓扑/几何检查、显式 `meshQualityDict` 分开记录；保留版本、原始日志、参数和问题位置。不将 Fluent 的 skewness 数值直接搬到 OpenFOAM；公式和适用单元类型不同。
- 固定 148,375 格喷管，在输入网格未变的副本上新跑：标准 PASS；完整检查 FAIL（face-plane 检查标记 5,641 个 concave 单元，并报告 16 个短边关联点）；再启用原厂配置后 FAIL，质量配置额外标记 448 个非正交角 >65° 的面、20 个 determinant 条件面，共 468 个质量问题面。各条目的数量与定义保持分开，不能写成 468 个坏单元。
- 原厂配置用于本次有出处的对照，不等于普适工业标准。例如 `minVol=1e-13` 是有量纲、依赖尺度的条件；几何合法性、数值风险和工况适用性仍需区分。当前失败没有通过修改阈值消除。
- 后续验收应分别回答：几何拓扑是否正确；目标求解器检查发现哪些风险；指定工况是否收敛、守恒且物理量合理；在一致的加密序列中关键结果是否稳定、误差是否满足任务要求。GCI 只在条件适合时使用，不能将不一致的加密带宽序列直接包装成正式 GCI。

证据：`artifacts/current/quality-review.json`；原始日志、原厂配置、质量场和可视化问题集合在 `outputs/quality-review/`。本次未新增 CFD 求解，不借用其他案例的求解结果证明这个高密喷管满足任意工况。

资料：[OpenFOAM 自定义质量检查](https://openfoam.org/release/2-2-0/meshing-tools/)、[Fluent 的质量定义](https://ansyshelp.ansys.com/public/Views/Secured/corp/v242/en/flu_ug/tgd_user_report.html)、[Ansys 流体网格建议](https://ansyshelp.ansys.com/public/Views/Secured/corp/v252/en/discovery/UDA/user_manual/meshing/topics/r_meshquality_bestpractices.html)、[Gmsh 质量测量 API](https://gmsh.info/doc/texinfo/gmsh.html#index-gmsh_002fmodel_002fmesh_002fgetElementQualities)、[NASA 网格收敛方法](https://www.grc.nasa.gov/www/wind/valid/tutorial/spatconv.html)。

## 保留边界与下一步

1. **质量优先于继续堆数量**：当前已取消 Q1；固定喷管的官方配置超限已修复，扩展 checkMesh 的共面拆分诊断仍保留。边界层是否适合工况仍需实际流动验证。不能把标准 Mesh OK、Solver PASS 或残差收敛当成工业质量全部合格。
2. **精度**：目标尺寸与最终尺寸有偏差；折线输入本身的几何误差、边界层方向尺度、流场误差和成本需共同控制。尚无自动 y+ 或误差驱动自适应闭环。
3. **鲁棒性范围**：九例有限扰动不代表任意几何。自交/零面积/非流形/分类冲突必须显式失败；旧重叠和伪造全零 AUDIT 反例仍在 `tests/repro/core-probe.cpp`，属于待修缺陷，不能据此放宽规则。
4. **输入与边界**：旧 `tests/repro/hidden-spline-bulge.dxf` 反例本次未复现，有限复核见文首；SVG transform、物理尺寸及部分元素处理仍不全。pure 可处理部分多环，局部孔洞可能显式拒绝；hybrid 已在本次分离双实体外流小例成功，嵌套孔洞仍拒绝，不能把单个微缝成功推广为通用窄缝支持。
5. **桌面产品完整性**：保持最终求解网格预览和 ZIP；高档完整流程约 112 s，仍需定位预览/导出开销。数量自动配置已接入；分段边界条件 UI、物理精度预设、大 CAD 输入和跨机器安装还未全面验收。仅支持所列 OpenFOAM 路径，未验收所有 CFD 软件。
6. **工程纪律**：构建只用 `build/`，实验在忽略的 `outputs/`，不新增长期隐藏工作区/日期计划/代码副本。Q3/Q4 开关保留默认关闭及累积关系；不因局部失败无限追加阶段。确定性同工具链与跨编译器逐字节一致分别验收。

下一步以目标求解器的真实检查和指定 CFD 工况验证继续拓展质量修复范围，建立误差验收并完善自动选参。新增问题归入这些既有目标，不能靠换参数、删坏格或隐藏告警“通过”。
