# 当前状态与接手重点

更新：2026-09-20。本文件是唯一当前状态入口。历史过程交给 Git；具体数字只适用于下列输入、参数、工具链和证据快照。

## 交付定位与唯一工作区

当前交付是**有明确支持范围的原生二维工程验证版**。近期目标的无量纲尺寸、万级至十万级真实网格、有限扰动、桌面导出和实际 CFD 验证已经形成可复核证据；工业级通用精度和鲁棒性仍需持续完善。

唯一工作区为本仓库根目录，当前开发分支 `codex/cfd-development`，前端只看 `desktop/src/`。用户确认的整理基线是桌面 0.2.0 / `9bddfbf`，旧目录整理提交为 `0ef933d`。三维仓库是独立项目，不混用其代码或验证。源码导航、构建命令和历史查找见 [DEVELOPMENT_CN.md](DEVELOPMENT_CN.md)。

用户已授权连续推进，无需逐项审批普通实现。Astra 负责设计、核心算法和小验证；已授权的 Luna 负责固定批量执行。主代理核对原始日志、实际网格和摘要；摘要自报 PASS 不能替代证据。不使用 usage reset，不放宽质量门。

## 阶段节点与维护路径

- 网格阶段标签 `mesher-v0.3.0` 固定指向 `664ac7c`：桌面0.3.0，加入自研 FVM/CFD 核心前的最后提交；标题为“网格生成器阶段完成”。它是工程验证版里程碑，不是通用工业资格认证。
- `codex/mesh-maintenance` 从 `664ac7c` 开始，承接网格生成、导入、质量及网格桌面功能的独立维护。
- 自研 CFD 从 `c933a55`（扩散基础）开始，`0350b3d` 形成0.4.0层流初版；`codex/cfd-development` 承接后续 CFD 工作，`main` 保存已验收的集成状态。
- 网格修复可先在维护分支验证，再合并到 CFD 分支及 main 并执行相关回归；标签不随分支移动。无需另复制一份仓库或混入三维目录；唯一工作区现已切至 CFD 开发分支；main 保留此前验收状态。

## 当前连续目标：可验证的二维工程 CFD 软件

用户要求扩大长期目标，已建立新的持续目标。0.4.0 稳态层流初版是已完成的基础，不代表本目标完成。开发在 `codex/cfd-development`，阶段验证后提交并推送；main 只集成已验收阶段。保留 `mesher-v0.3.0` 和网格维护分支。

| 顺序 | 要交付的能力 | 完成依据 |
| --- | --- | --- |
| 1：精度与性能基础 | 稳态 FVM 的共享面动量守恒、边界配置、有界高阶对流、可信壁面受力；固定稀疏结构、可复用工作区和适宜压力预条件 | 先记录现有程序的时间、内存、线性迭代；同输入/同容差比较优化前后；解析/制造解、至少三档细化和域敏感性，报告误差而非只报残差 |
| 2：非定常与输运 | 非定常不可压层流、被动标量、恒物性热输运；CFL、物理时间、重启 | 解析衰减/输运、非定常圆柱及网格/时间步细化；守恒、频率/受力、重启一致性；禁止用稳态迭代震荡冒充物理时间 |
| 3：工程湍流 | 固定并标注具体版本的 k-omega SST RANS，必要时 URANS；明确壁面处理与 y+ | 参考 NASA TMR 的模型定义和标准例；先平板，再有压力梯度/分离的二维算例；近壁、域和网格敏感性，不以一张云图验证湍流 |
| 4：可压缩单相流 | 理想气体质量/动量/总能量守恒、适合激波的有界通量、可压缩边界 | 激波管解析解、等熵涡及喷管/激波标准例；密度压力正性、守恒、波速/位置和细化；不能把不可压压力修正简单套过来 |
| 贯穿各阶段 | 桌面工况配置、取消/失败诊断、残差和物理量监测、场显示、重启与导出；三平台运行 | 每个物理功能完成可操作闭环；macOS/Linux/Windows 分别实测，源码可编译不代替实际运行 |

规模目标是 **1万、10万、50万最终单元的真实求解**，分别记录网格/工具哈希、硬件、耗时、峰值常驻内存、线性与非线性迭代、停止原因和正确性。百万级先做资源资格试验。完整收敛和固定步数吞吐分开记录；矩形规则网格与真实 Cut-cell 分开验证，不能按网格数量推断运行时间。基线出来后再设有依据的速度目标，不提前编造“几十秒算完”的保证。优先测量和改善稀疏存储、重复分配、压力预条件；并行化在串行结果可靠后验证。使用公开数值库时如实标注实现来源和许可，不将库功能说成原创算法。

工程能力按工况验收：离散方程收敛、质量/动量/能量守恒、目标物理量、网格/时间步/域敏感性、标准或实验对照分开判断。误差允许值须在对应算例运行前给出依据；没有全软件统一的残差“及格线”，不重建 Q1。实际 OpenFOAM 对照可用时执行；目前 Docker 被手动暂停，不能伪报运行。三维、多相、燃烧、LES/DNS 不属于本次目标，避免无限追加范围。

张量在二维应力和速度梯度中已经适用。已查殷雅俊《广义协变导数与平坦时空的协变形式不变性》，它提供广义协变微分/变分理论；是否对应用户所学课程仍待确认。当前未发现它能直接加速本稀疏求解器的实测依据。若提出具体算法，先以相同精度、网格和硬件对照评估再引入，不以理论名称替代性能证据。参考来源与路线入口见 DEVELOPMENT_CN。

已加入性能观测入口并完成下述基线；新增 `--profile` 只输出诊断，不修改离散格式、收敛条件或网格质量门。后续按资源预算逐档扩大。当前机器为8 GiB内存，启动本轮时可用磁盘约13 GiB；计算串行执行，先限制单例180秒、保留最终场与日志，未经基线评估不直接启动50万/百万格长算。

## 当前改进：同步热输运规模对照与数值控制

本轮增加CLI瞬态速度松弛选项与独立规模验证工具，**默认仍为.6，桌面仍为0.4.10，未新增桌面控件或重新打包**。流动用 `--velocity-relaxation`，同步热输运用 `--flow-velocity-relaxation`；只用于非定常内迭代，明确拒绝稳态/冻结载流误用。没有修改方程、线性/非线性停止门或时间步。

实际单位方域Taylor–Green相切涡，nu=.1、D=.02、Uref=1、dt=.005、2步，流动和标量都使用限制线性重构、aggregation压力预条件；流动停止1e-8，标量沿用原停止门。系统clang、M1/8 GiB上串行运行，时间含读取/求解/导出。只完成t=.01的短时间诊断，不能代替长时间/工程热流验证。

| 最终单元数 | 默认.6同步耗时 | 显式.8同步耗时 | 两步流动外迭代（.6→.8） |
| --- | ---: | ---: | ---: |
| 10,000 | 8.17 s | 6.07 s | 378→200 |
| 40,000 | 107.36 s | 57.29 s | 1,166→542 |
| 102,400 | 180 s预算内未完成首步 | 300 s预算内仅完成首步 | 未完成，不能比较完整两步 |

四万格.8同步求解峰值RSS约99 MiB，默认约102 MiB；独立单流场求解57.05 s，压力线性59.47→29.83 s。标量每步仍为10轮，因此主要瓶颈在流动—压力内迭代。参数.9及1.0的四万格试验均触发线性求解上限，不能把“越大越快”做成自动默认。四万格不同松弛结果的最大速度分量差5.57e-7、运动学压力差8.76e-7；均满足原停止门，但不宣称有限迭代结果逐位相同。上述时间是固定案例单组对照，非通用速度承诺。

一万/四万格两种设置均重新独立审核实际CM2D、最终逐面本构通量、质量/动量/标量与时间项；同步载流与同系数单独流动的checkpoint逐字节相同，逐步接受历史和迭代数相符。没有独立存档全部中间流场，审核范围以最终场及原始历史为准。

.8设置下，标量相对**空间连续的后向欧拉参考解**的RMS误差从1.4501e-7降至3.8099e-8；速度向量从2.0923e-6降至2.0236e-7。相对连续时间解析解的误差却分别约2e-6及6.7e-5，时间离散已占主导。两档之间的误差比只是诊断，不是完整三档收敛阶认证；十万格未完成，所以**本次规模研究总体仍未通过**。参考解不改写物理误差，空间/载流/迭代误差也没有完全分离。

旧计时封装的超时只杀死 `/usr/bin/time`，十万格.8试验在315秒时还观察到孤立求解器；随后准备终止时进程已消失，最终场未导出，仅保留t=.005接受状态，实际终止原因未确认。因此300秒只是计时封装预算，不冒充严格的进程树实际耗时。本轮已修复**新规模工具**的进程组超时清理、日志重复及清理等待上限，并用会启动子进程的故障测试验证；没有追溯修改原失败日志，其他旧验证脚本的超时实现未统一修改。macOS受限环境中计时器还曾报权限错误；.8试探的数值结果可独立读取，但该次计时器退出1及RSS缺失保留，不冒充完整资源测量。

系统clang完整构建、CTest118/118、前端140/140通过；增强超时测试后另跑本工具12项测试通过，最终驱动36/100/196格端到端验证通过。未运行新版CLI的GUI打包、Linux/Windows或外部OpenFOAM。新工具有Windows进程清理分支，但本次只实测macOS，不能据此声称跨平台验收。

[实际四万格场和不完整细化图](../artifacts/current/native-flow-thermal-scale.png) · [参数、哈希、原始命令与失败证据](../artifacts/current/native-flow-thermal-scale.json)。输出在 `outputs/native-flow/thermal-scale/`，运行方式见DEVELOPMENT。下一步优先改善细网格非定常的流动—压力迭代效率，再补齐原三档验证；不是继续放大松弛、时间预算或放宽残差门。长期工程CFD目标保持进行中。

## 当前改进：多重网格装配存储 0.4.10

粗层Galerkin装配改为按行分桶、行内稳定排序和直接对称CSR写入，减少两次全局tuple排序及双向tuple临时列表；首个前向平滑只读取已覆盖的下三角，省去清零和读取上三角零值。聚合选择、细面累加次序、long double求和、矩阵系数、实际迭代和停止门不变。新增独立稠密PᵀAP逐层核查，覆盖置换编号、重复聚合连接、组内连接、精确对称及工作区复用后的零右端。

同一系统clang工具链、同一网格和物性，以f73b535保存的CLI与当前CLI串行比较，均使用aggregation。圆柱/十万格ABBA各两组，五十万格一组；单次180秒上限。计时包含读网格、求解和导出。启动磁盘约4.7 GiB可用，复用已有网格，本轮数值文件逻辑总量约1.4 GiB；对哈希相同的61份生成结果做硬链接去重，保留全部路径和字节，实际目录约459 MiB。未清理用户文件。

| 输入 | 优化前总耗时 | 优化后总耗时 | 峰值RSS（前→后） |
| --- | ---: | ---: | ---: |
| 4,880格Cut-cell圆柱，Re20，dt=.1、5步、限制重构、1e-9 | 8.724 / 8.724 s | 8.429 / 8.419 s | 约14.6–14.7 →14.5–14.6 MiB |
| 112,368格通道，nu=.01、迎风、1e-6 | 10.309 / 10.137 s | 10.559 / 10.102 s | 219–240 →216–229 MiB |
| 500,472格通道，同上 | 37.474 s | 37.031 s | 934→804 MiB |

五十万格求解本体26.421→25.819秒，压力线性19.080→18.482秒；SIMPLE13轮、压力累计965次不变。**主要可见收益是本次五十万格RSS减少约130 MiB（14%）；整体加速有限，十万格总时间有波动，不能宣称通用显著提速。** 单次内存测量也不代表所有几何固定节省14%。微型矩阵单测中50万未知量层次构建约.144→.070秒，仅为局部计时，不能外推整个求解。

三个实际CFD输入的最终速度、压力、逐面通量、VTK及残差均逐字节相同，圆柱另含checkpoint/时间历史；独立几何、质量、共享面动量及圆柱时间项审核均通过。十万/五十万通道解析速度L2误差4.2851e-6 /9.2522e-7，压力7.2732e-5 /4.3449e-5；五十万全域相对质量不平衡5.45e-13。独立Python审核五十万格约43.83秒、进程峰值1.77 GiB，与求解器分开报告。规则通道的这些证据不认证五十万格复杂绕流或湍流。

系统clang完整构建、CTest117/117及前端140/140通过。macOS arm64 0.4.10打包完成，实际App生成5,204格圆柱并以限制重构/聚合预条件/法向回流完成498次迭代、显示速度场及ZIP导出。GUI沿用1e-6停止，不与命令行性能案例的容差混为同次运行。实际GUI的几何/守恒/动量独立读回通过，ZIP32项与原文件逐字节相同；App内flow/transport与本轮build的SHA一致。直接从终端启动曾退出134且无输出，原因未确定；确认进程结束后通过macOS open正常启动完成验证，失败记录保留。本轮未改界面，未重测热输运GUI、外部文件选择或取消续算操作。Linux/Windows/OpenFOAM未运行。

[真实网格与测量](../artifacts/current/aggregation-assembly.png) · [实际桌面](../artifacts/current/desktop-aggregation-assembly.png) · [命令、哈希与独立证据](../artifacts/current/aggregation-assembly.json)。本轮没有改变物理适用范围；SST、可压缩、更大规模热输运及跨平台CFD资格继续待完成。输出在 `outputs/native-flow/aggregation-assembly/`，长期目标保持进行中。

## 当前改进：可选压力出口回流 0.4.9

新增 `OutletBackflow2D::NormalInlet`，API/流动CLI/同步温度CLI/桌面都可显式选择；默认仍是检测到回流时停止。右侧压力出口p=0，流出沿用现有条件；q<0时法向速度零法向梯度、切向速度0，实际负面通量进入质量/动量/温度方程。没有裁剪通量、抹掉负速度、添加外流体积源或放宽收敛门。此模型对斜出口、任意patch、湍流和强非定常开放边界的适用性尚未验证。

有源平行双向流解析验证：单位方形、nu=.1、U=1、限制线性、聚合压力预条件、原方程停止1e-8。同一右侧压力边界同时流入和流出。独立读取器从真实CM2D重算几何、体积源、边界分量、面压力/对流/黏性通量及原动量方程；篡改源、流量、切向流入和模式标记都会失败。

| 单元数 | 速度向量L2/U | 流入出口面数 |
| --- | ---: | ---: |
| 196 | .0145074458 | 4 |
| 900 | .0032111995 | 10 |
| 3,844 | .0007539898 | 20 |

速度观测阶1.9787/1.9961，压力1.9860/1.9991。该特定光滑有源算例接近二阶，不代表任意网格或流动均二阶；它与下面无源真实圆柱分开。

5,204格默认紧凑圆柱，直径2、U=1、nu=.05（Re40）、留白/Lref=.5：reject模式约4秒因出口回流失败；normal-inlet模式约20秒收敛（当前机器、输出也计时）。最终16个出口面流入，流入量.1282986345 m²/s，独立原动量残差6.76338e-9<1e-8；实际面本构重算差<9e-16。对称单元配对速度差<8e-14。这是短计算域上的守恒/稳定性验证，不是无限远场圆柱阻力认证；Re20参考阻力不用于Re40误差比较。

原生64格非定常热输运另从有符号平行初流启动：首步仍有4个出口流入面、流入量约.306901；出口温度7、其余入口5，计算温度保持[5,7]，缺少出口流入温度明确拒绝。全域残差-1.36693e-8，是逐格残差之和；按sqrt(N)||r||₂核对，逐格归一残差3.6628e-10<1e-9，未改变标量停止条件。完整2步与1步+联合保存+续1步逐位相同。

桌面0.4.9已显示新模式、真实回流面数和流入量；同一Re40圆柱真实App生成/求解/ZIP导出通过。桌面沿用其默认1e-6停止，独立CLI用1e-8，结果不可混为同次运行。同步温度桌面另用nu=.1、dt=.05测试失败/取消保护和联合续算至t=.3；该GUI用例不冒充实际流入测试，真实瞬态回流由上述原生测试覆盖。系统文件选择对话框未自动操作。

流动checkpoint升级v2保存模式，旧v1按reject读取；联合热状态仍外层v1、内嵌FLOW v2。原生和桌面拒绝模式不匹配/未知模式/不支持的版本，防止续算时悄悄换边界。完整CTest117/117、前端140/140及独立读取器42/42通过；macOS arm64打包通过，Linux/Windows/OpenFOAM本轮未运行。SST、可压缩、复杂边界和工程资格仍未完成。

[真实回流及加密误差](../artifacts/current/outlet-backflow.png) · [实际桌面](../artifacts/current/desktop-outlet-backflow.png) · [原始命令、哈希与审核](../artifacts/current/outlet-backflow.json)。输出位于 `outputs/native-flow/outlet-backflow/`。长期目标保持进行中。

## 当前改进：壁面压力重构与默认圆柱 0.4.8

修复0.4.7默认自动5千格/留白.5圆柱在第一步出现的数值失稳。保持原5,204格CM2D、流动物性、边界、停止容差及4轮压力非正交修正，不删除单元。原因是个别壁面切割单元只有两个近乎共线的直接压力采样：形式上满秩，最小二乘条件数却约564.65，首次压力修正把局部速度放大至123.48；这不是把真实出口回流判据关掉就能修好的问题。该网格最大非正交角约42.31°，角度本身未揭示取样病态。

现在对省略未知压力边界的单元加入真实第二圈邻居，固定问题单元由2个样本增至5个，条件数8.20。保留仿射一致性；内部满秩单元和速度重构不变。实际6单元补丁回归在旧算法失败、新算法通过。独立读取器另修复全局坐标面积/质心抵消，80位Decimal复核；这解决最小时间步的误报，不放宽1e-9标量逐格停止条件。新流场摘要明确标记 `one-sided-linear-2ring`，读取旧结果仍使用旧算法。

同一原失败圆柱，nu=.1、U=1、D=.1、初温/入口300K、物面350K，流动限制线性、压力聚合预条件：dt=.05两步、.01一步、.005一步均完成，并由独立工具回读标量面本构和每格/全域守恒。dt=.05两步约10.6秒；单独流动t=.1最大速度2.29166，46个出口面均为正向通量，独立原动量方程审核通过。原二进制在三组时间步均于t=0失败，日志和哈希保留。大域4,880格旧/新各一步也通过；场值有所改变，不宣称旧/新逐位相等。

macOS arm64打包App使用默认自动5千格/留白.5，真实生成5,204格，执行2步、续2步、故意失败、接受一步后取消、再续1步至t=.3；温度显示、监测、取消保护、续算、缩放布局及ZIP导出通过。此时约300–349.67745K。未自动操作系统文件选择对话框。App位于 `desktop/dist/mac-arm64/CartMesh2D.app`，未签名；Windows/Linux与本轮OpenFOAM未运行。

完整CTest117/117、前端136/136通过。同步热输运原15次验证重新执行通过：空间误差仍下降但混有时间误差、观测阶3.02/0.91不稳定；时间阶约.938/.924；连续和拆分续算的联合状态与字段逐位一致。没有以此认证通用二阶或换热精度。仍不支持物理出口回流，近远场敏感性、复杂几何、更大规模热输运、SST/可压缩及工业资格仍待推进。

[实际流场与压力取样](../artifacts/current/pressure-stencil.png) · [默认圆柱桌面温度场](../artifacts/current/desktop-pressure-stencil.png) · [命令、哈希与独立证据](../artifacts/current/pressure-stencil.json)。原始输出在忽略提交的 `outputs/native-flow/backflow/`。总目标保持进行中。

## 当前改进：桌面同步温度工作流 0.4.7

已接入真实 `cartmesh2d_transport_cli`，每步流动和温度均收敛才接受时间。桌面复用流动工况/黏度/速度/时间设置，新增热扩散率、初温、温度源、限制线性或迎风，以及物面/矩形四边的定值或向外扩散通量。温度用K、D用m²/s、源为K/s、边界通量为K·m/s；温度积分为K·m²，不能直接标作热量。内流按四边设置；外流物面单独设置。流动仍不支持出口回流，指定温度流入值不会改变这一限制。

结果采用独立运行目录，验证摘要/实际cells CSV/联合状态/时间历史后才绑定温度图；取消和失败不覆盖上次完整显示，最新接受状态单独标时。可在会话内续算，或载入导出包中的 `.thermal.checkpoint` 与同目录 `desktop-state.json` 恢复配置；启动时原生接口仍严格核对完整网格和物性。外部文件选择按钮已实现，但本轮没有自动驱动系统选择对话框；会话内保存、取消和恢复则进行了真实原生及打包GUI验证。温度与独立速度/压力显示互不冒充；更换几何清除旧绑定。

macOS arm64真实0.4.7应用，圆柱4,880格、Lref=2、留白/Lref=10、壁面相对尺寸.0625、背景.5、过渡3格、smallAlpha=.1；nu=.1、U=1、D=.1、初温/入口300K、物面350K、dt=.05。实际执行2步、续算2步、故意仅1内迭代失败、活进程接受一步后取消，再续1步到t=.3。显示场、最新续算时间、温度色图/监测与导出均验证。独立Python读回最终场：约300–349.78837K，全域储热/边界/源平衡-1.91e-8，最大面本构通量差3.73e-11。另原生桌面运行管理测试连续4步与2+2续算的联合状态逐字节相同。当前硬件/二进制范围，不是换热精度或跨平台资格。

首个默认自动5千格/留白.5的GUI试算触发 `Flow outlet backflow unsupported`，失败日志与参数保留在证据；随后使用上述已验证大计算域检验界面。这没有修复出口回流，也不是证明扩大计算域总能解决问题；未改变回流判据、质量门或收敛门。该默认算例已在0.4.8定位为壁面压力取样数值失稳并修复，见上节；物理出口回流处理仍待实现。

完整CTest **117/117**、前端 **136/136**、runtime核对及打包通过。ZIP含真实全域+物体周围温度PNG、VTK、逐格逐面CSV、历史、联合续算与简短说明。原生工具打包清单已加入transport，Windows/Linux仍未实际运行本轮功能；OpenFOAM未运行。App位于 `desktop/dist/mac-arm64/CartMesh2D.app`，未签名；大型二进制不进入Git。

[实际桌面](../artifacts/current/desktop-thermal.png) · [实际ZIP温度图](../artifacts/current/desktop-thermal-temperature.png) · [哈希与检查证据](../artifacts/current/desktop-thermal.json)。SST、可压缩、大规模热输运、外部对照和工业资格仍未完成，总目标保持进行中。

## 当前改进：同步流动与温度推进、联合续算

`b3a0120`阶段新增 `advanceThermalFlow2D`：每一步先收敛t+dt的流动，再用该时刻的共享面通量求解后向欧拉标量。只有两者均收敛才返回accepted状态。前态保持不可变，流动失败不会启动标量；标量失败不会接受已算出的半步流场。它是**单向、恒物性的同步输运**，温度不反馈密度/黏度/浮力，不是双向热流耦合。

`cartmesh2d_transport_cli --evolve-flow`支持现有channel/cavity/external工况；默认冻结载流入口仍保留。新增`.thermal.checkpoint`单文件包含温度、速度、压力、面通量、共同物理时间、完整网格，以及逐格源项/逐面热边界/扩散系数与对流格式。先写临时文件，再原子替换接受态；诊断用`.carrier.checkpoint`不是续算依据。重启逐项核对实际配置，拒绝改物性、边界、源项、空间格式或网格；允许改变时间步和迭代控制。API支持固定空间分布源和逐面BC，CLI目前仍为均匀源与固定BC表，未实现时间变化边界。

实际单元/面数据证明：196格解析涡连续4步，与2步+保存+续算2步的joint checkpoint、carrier checkpoint、cells及faces CSV逐字节相同；同条件独立运行原flow CLI，载流checkpoint也逐字节相同，并通过原独立时间项、质量和动量方程审核。这证明当前软件/工具链的相同输入一致性，不代表跨平台逐位相同。测试还真实执行了两类失败、原地续算文件保护、活进程正时间取消及恢复；不以进程退出码替代场和时钟核查。

解析耦合验证采用单位方形Taylor–Green流，theta=sin(pi x)sin(pi y) exp(-2 D pi²t)。速度沿theta等值线，解析U·grad(theta)=0，壁面theta=0、源为0；速度与标量分别按nu和D衰减，仍实际执行两套离散方程，没有直接写入解析解。nu=.1、流动与标量均限制线性，聚合压力预条件。

| 实际单元数 | D=.02、dt=.0025、t=.05的theta L2误差 |
| --- | ---: |
| 196 | .000125095 |
| 900 | .0000125432 |
| 3,844 | .00000645015 |

空间细化的误差下降，但相邻观测阶3.02/0.92不稳定；固定dt的结果混有时间误差，**没有因此认证同步求解的通用二阶空间精度**。900格D=.2、共同t=.1，dt=.02/.01/.005，L2=.00513680/.00268112/.00141297，观测时间阶.938/.924。独立工具重算当前几何、面本构通量、前态时间项、时钟一致性与joint状态，不接受旧时间的载流。

实际重新生成4,880格Cut-cell圆柱，从流体静止、theta=0开始，速度1、nu=.1（Re20）、D=.1、物面theta=1、外域零扩散通量/流入theta=0，dt=.05推进4步至t=.2。全单元范围约[5.09e-103,.994624]；最后一步独立全域热量平衡-1.51e-8（包括储热项），边界绝对通量3.29，最大本构通量重算差9.35e-13。约7.13秒包含4步流动、温度、每步联合保存及导出；未测RSS，不与上轮冻结流场耗时混比。它是启动加热演示，尚无圆柱换热系数或时间/空间无关性认证。

系统clang构建、完整CTest **117/117**、前端 **124/124** 通过。桌面仍0.4.6，未打包/接入温度UI；OpenFOAM及Linux/Windows未运行。下一步仍需桌面配置、监测、取消/续算、结果显示导出闭环，以及更大规模和外部热输运验证；SST/可压缩与工业资格仍未完成。

[真实同步温度/流场及误差图](../artifacts/current/native-thermal-flow.png) · [命令、哈希与独立证据](../artifacts/current/native-thermal-flow.json)。输出位于忽略提交的`outputs/native-flow/thermal/validation/`，可复现驱动见DEVELOPMENT。

## 当前改进：守恒标量与恒物性热输运核心

`f749aac`阶段新增 `ScalarTransport2D` 和 `cartmesh2d_transport_cli`，原生求解 `d(theta)/dt + div(U theta - D grad(theta)) = source`。直接使用流场共享面体积通量，检查每格载流连续性；支持稳态、后向欧拉单步、定值、指定向外扩散通量（零为绝热），回流必须显式提供流入标量值。每条内部面共用一份对流/扩散通量，分别导出时间项、源项、逐格及全域平衡，不裁剪温度或增加人工守恒源项。

常密度/常比热温度可取 `D=k/(rho cp)`、体积热源 `Q/(rho cp)`、边界向外热通量 `q/(rho cp)`。**这是该阶段的单向恒物性输运基础**：当时CLI从严格匹配几何的flow checkpoint读取并冻结载流；即使标量推进物理时间，流场也不随之推进。没有浮力、变物性、黏性发热、共轭传热、辐射；尚未接入标量桌面控制和标量重启。App仍0.4.6，未重新打包；后续需接入同步流动/标量时间推进及完整桌面闭环，不能把这次演示说成已完成热流耦合软件。

稀疏结构和BiCGStab工作区复用现有原生实现，对流可用迎风或限制线性重构，扩散包含真实非正交修正。初版对矩阵对角线松弛，导致3,844格线性方程在1,000轮内未收敛；失败保存在 `outputs/native-flow/scalar/refinement-final/`。改为求解完整稀疏主部后对场松弛，保持同一方程、1e-9相对/单元尺度停止和1e-12绝对停止，没有加大上限或放松误差门。

独立Python读取实际CM2D多边形和导出场，重算梯度、对流/扩散本构通量、每格载流与标量守恒、时间项、源项；篡改面通量、前一步状态或源项会失败。系统clang构建、完整CTest **115/115**、前端 **124/124** 通过。没有运行本轮OpenFOAM/Windows/Linux/桌面GUI验收，也未验证标量十万/五十万规模。

| 实际单元数 | 光滑制造解迎风L2误差 | 限制线性L2误差 | 限制线性相邻观测阶 |
| --- | ---: | ---: | ---: |
| 196 | .0278087 | .00139401 | — |
| 900 | .0146485 | .000347414 | 1.823 |
| 3,844 | .00750751 | .0000863315 | 1.918 |

单位方形，解析标量 `sin(pi x)sin(pi y)`、载流(.35,0)、D=.08及对应解析源。时间验证为900格纯扩散D=.2、相同终止t=.5：dt=.1/.05/.025，解析衰减L2误差 .0253855/.0131977/.00677543，观测阶.944/.962；有限空间误差仍包含其中，不能当纯时间误差。额外C++测试包括斜网格混合定值/通量仿射解、绝热源项升温、常量保持、迎风有界性及显式失败路径。限制重构不保证任意非正交网格的离散最大值原理。

真实4,880格Cut-cell圆柱：读取此前Re20、t=.5的已验收载流checkpoint，固定该时刻通量后求稳态标量，D=.1、物面theta=1、外域扩散通量0、外域流入theta=0。这是冻结流场上的热输运演示，**不是瞬态温度/流场同步演化或换热系数基准**。全单元theta范围约[8.06e-40,.998649]，独立最大面通量重算差3.89e-13，全域平衡-2.61e-8，除以边界绝对通量约1.06e-8。单次CLI总耗时约.25秒（含输入和导出，未测峰值RSS），不外推至大网格。圆柱网格与载流来自现有验证产物，空间细化的三档方形网格本轮实际重新生成。

[实际温度场与细化图](../artifacts/current/native-scalar-transport.png) · [命令、哈希、失败与验证证据](../artifacts/current/native-scalar-transport.json)。复现方式见DEVELOPMENT；大场与日志保留在忽略提交的 `outputs/native-flow/scalar/`。工程目标继续保持未完成。

## 当前改进：方腔公开参考与四档空间细化

本轮没有修改求解器、App或旧Ghia阈值；重新核对参考来源并实际复算当前0.4.6核心。Ghia 1982原文表I/II的Re100坐标与数值均与仓库相符，没有抄录错误。另引入[Marchi、Suero与Araki 2009](https://doi.org/10.1590/S1678-58782009000300004)表6/7：从1024×1024研究进行Richardson外推的中心线速度及作者估计离散误差；这不是解析真值或严格置信区间。30个速度和30个误差估计经PDF页面与独立文本提取核对，数值、坐标、出处页码和PDF哈希进入版本化数据文件。

四档均为实际二维单位方腔Re100，nu=.01、顶盖速度1、限制线性对流、对称黏性应力、聚合压力预条件、停止容差1e-8。使用同一二进制与仿射取样器；较早历史报告的1e-6结果不混入这条序列。前三档单例180秒预算，新增15,876格为360秒；全部实际收敛并通过独立几何、质量及逐面动量读回。

| 最终单元数 | 对Marchi表的速度RMSE/U_lid | 相邻观测阶 | 对旧Ghia表的RMSE/U_lid |
| --- | ---: | ---: | ---: |
| 196 | .0189640 | — | .0135745 |
| 900 | .00409782 | 2.010 | .00232233 |
| 3,844 | .000902071 | 2.085 | .00348274 |
| 15,876 | .000216579 | 2.012 | .00389432 |

两份表的取样坐标集合不同，不能将两列RMSE直接当作同点误差比较；每一列内部始终使用相同30个参考点。最细档对Marchi的RMSE为顶盖速度的0.0217%，最大点值差0.0414%；仍显著大于参考表的估计误差。原生中心速度u=-.208796729、v=.057503925，Marchi给出-.2091491418/.057536559，Ghia给出-.20581/.05454。原来独立FD33/65/129保存场也在Marchi采样点重新比较，文件哈希与原记录匹配；这是旧场新审核，未声称重新运行FD。

因此，目前证据支持此Re100方腔的中心线速度接近二阶趋向独立公开高精度结果；**旧Ghia单调误差门仍然失败**，它不足以单独证明求解器精度退化。没有为贴合旧表修改离散方程，也没有将旧失败改成PASS。新的对照工具并排保留两组检查，`allChecksPassed:false`并返回1；这不是新产品质量等级或Q1。角点奇性、壁面载荷、其他Re和任意几何不能由中心线结果认证。

最细档需要8,590次SIMPLE外迭代、324.73秒总时间，其中压力线性212.24秒，动量线性23.15秒；本轮未单独测峰值RSS。该方腔比之前五十万格已发展通道慢，说明数量不能单独预测成本，后续仍需改善外迭代。没有用放宽停止条件或初填参考解减少迭代。

新增可复用生成/复审入口及9项来源、几何、混合配置和失败保留测试。实际入口另外重新生成196/900/3,844格并求解，数值文件与直接命令逐字节相同；程序退出1准确保留Ghia失败。系统clang构建、完整CTest **113/113**、前端 **124/124** 通过。本轮未改生产核心，App仍0.4.6、未重新打包或执行新GUI/OpenFOAM/Windows/Linux检查。持续目标未完成。

[真实方腔网格与参考对照](../artifacts/current/native-flow-cavity-reference.png) · [命令、哈希、全部检查与资源证据](../artifacts/current/native-flow-cavity-reference.json)。完整输出在 `outputs/native-flow/cavity-reference/`；复现命令见DEVELOPMENT。

## 当前改进：试验性压力多重网格（0.4.6）

新增可选的聚合多重网格压力预条件，CLI 为 `--pressure-preconditioner aggregation`，桌面为“压力求解 → 多重网格（试验）”。默认仍是 IC0。相同矩阵的非正交修正复用分解/层次，系数改变则重建；真实残差、原3000步线性上限、非线性停止及独立守恒门不变。标准多重网格原理由本仓库独立实现，没有复制或链接 hypre，不宣称具有成熟 AMG 库的通用鲁棒性。

同一8 GiB Apple Silicon机器串行比较 `f606e6a` 的 IC0 与本版聚合方法。圆柱和十万格通道各两组ABBA，五十万格一组；总时间包含读网格、求解及导出。

| 实际输入 | IC0 总耗时 | 聚合方法总耗时 | 压力线性迭代总数 |
| --- | ---: | ---: | ---: |
| 4,880格 Cut-cell 圆柱，Re20，dt=.1，5步，限制重构、容差1e-9 | 11.25 / 11.32 s | 9.20 / 9.15 s | 118,346 → 33,428 |
| 112,368格通道，nu=.01，迎风、容差1e-6 | 22.74 / 22.68 s | 10.56 / 10.64 s | 10,783 → 1,400 |
| 500,472格通道，相同物性与格式 | 105.02 s | 38.01 s | 10,930 → 964 |

五十万格求解本体94.22→27.12秒，压力线性部分85.95→19.65秒；SIMPLE仍为13轮。峰值RSS约723→876 MiB，十万格约184–187→227–229 MiB，圆柱约16→17–19 MiB。缓存单独试验几乎没有总时间收益，主要收益来自多层预条件；不能把迭代数下降等同于同比耗时下降，也不能将本通道的2.76倍总加速外推至湍流或任意Cut-cell。启动本次测量时磁盘约7.7 GiB可用，计算串行；大场文件留在忽略提交的 outputs。

更换迭代路径后数值文件不再逐位相同。三组对照最大绝对速度分量差小于3.6e-12，压力差最大1.30e-9；分别通过独立实际多边形几何、逐面动量、单元/全域质量读回，圆柱另复算时间项。十万/五十万格解析速度相对L2误差4.29e-6 / 9.25e-7、压力误差7.27e-5 / 4.34e-5，五十万格全域相对质量不平衡5.43e-13。五十万格Python独立审核46.37秒、RSS约1.58 GiB，审核成本与求解器分开记录。

初版聚合在真实稳态圆柱中触发“粗化不足”的内存保护，失败日志保留。修复为把有连接的未配对节点并入相邻配对组，保留所有行和Galerkin贡献，没有放宽保护或回退掩盖失败；40节点星形最小回归覆盖此问题。每层至少约20%缩减、最多32层，过弱粗化仍显式拒绝。修复后通道、方腔、光滑制造解各两档及稳态圆柱共7例独立审核通过；这组两档检查不替代原三档 Ghia 回归，**旧方腔细化失败仍未解决**。

系统clang完整构建及CTest **112/112**、前端 **124/124** 通过。专项检查固定V-cycle的线性/对称/正二次型、独立已知解和真实残差、系数突变重建、失败恢复与内存保护。macOS 0.4.6实际App完成2步IC0→切换聚合续算2步→故意内迭代失败→已接受一步后取消→再续算2步至t=.07，保留先前完整结果与最后接受的checkpoint；侧栏、主题、800×560布局及ZIP导出通过。导出包内实际网格、流场与时间项独立读回通过；对照同输入连续7步聚合计算，最大绝对速度差1.79e-14、压力差1.14e-13，不宣称不同方法逐位相同。App内CLI的SHA与正式计时一致。外部checkpoint文件对话框本轮未重测。

本地应用为 `desktop/dist/mac-arm64/CartMesh2D.app`，安装包为 `desktop/dist/CartMesh2D-0.4.6-arm64-mac.zip`。当前仍是有限工况的不可压层流；未运行外部OpenFOAM或Windows/Linux新版本。湍流、热输运、可压缩流及工程级总体目标未完成，继续按上方路线推进。

[真实网格及性能图](../artifacts/current/native-flow-aggregation.png) · [原始计时、哈希、独立审核与桌面证据](../artifacts/current/native-flow-aggregation.json)。完整输出保留于 `outputs/native-flow/pressure-reuse/`。

## 当前改进：残差范数提速与新核心规模复核（0.4.5）

将线性求解器逐分量调用`hypot`的L2范数改为带缩放的平方和，只在末尾开方。仍计算同一欧氏范数，保留长精度累加、有限值检查、真实残差回代及原停止条件；没有删单元、改变方程、放宽容差或缓存旧残差。采用公开的标准数值思路，自行实现，没有复制LAPACK代码或引入库依赖；数学来源见DEVELOPMENT。

以`0022ae7`保存的旧CLI与当前CLI在同一8 GiB Apple Silicon机器串行对照，总时间包含读网格、求解和导出。圆柱按ABBA顺序各两次，通道各一次；不是多机统计性能保证。

| 同一输入工况 | 优化前总耗时 | 优化后总耗时 | 迭代与数值输出 |
| --- | ---: | ---: | --- |
| 4,880格Cut-cell圆柱，Re20，dt=.1，5步，限制重构、容差1e-9 | 17.99 / 18.28 s | 11.11 / 11.10 s | SIMPLE合计390，压力线性118,346次；七份数值文件逐字节相同 |
| 112,368格通道，nu=.01，迎风、容差1e-6 | 36.49 s | 22.28 s | SIMPLE16，压力线性10,783次；五份数值文件逐字节相同 |
| 500,472格通道，相同物性与格式 | 164.46 s | 100.41 s | SIMPLE13，压力线性10,930次；五份数值文件逐字节相同 |

上述实测总耗时减少约39%，原始每次计时、命令、哈希、峰值RSS和输出清单保留。500,472格优化后求解本体89.72秒，其中压力线性82.17秒，压力求解仍是瓶颈。进程RSS在十万格约199→183 MiB，五十万格约692→801 MiB；这是单次观测，不能宣称内存优化或所有工况都同样加速。

独立读取器改为逐行分词，避免一次保留完整网格文本和所有token字符串，原几何、拓扑与物理检查不变。新增BOM/空白、截断、尾随数据、编号和环长度拒绝回归。三组新输出分别通过独立实际多边形几何、逐面动量及单元/全域质量审核，圆柱另复算时间项；通道还对照解析解。十万/五十万格的速度相对L2误差分别4.29e-6 / 9.25e-7，压力相对L2误差7.27e-5 / 4.34e-5；全域相对质量不平衡1.50e-12 / 2.19e-12。五十万格Python审核45.19秒、峰值RSS约1.58 GiB；未测旧读取器同条件峰值差，不能据此量化节省比例。

系统clang完整构建、CTest **111/111**、前端 **123/123** 通过。范数专项覆盖极大/极小数、次正规数、接近上限、NaN/Inf及数学溢出的拒绝。当前二进制与正式计时的候选SHA一致；跨编译器逐位一致性尚未验证。

macOS桌面0.4.5已重新打包，App所带CFD二进制SHA与正式计时候选一致。实际App生成4,880格圆柱并完成2步→续算2步→故意限制内迭代失败→已接受一步后取消→续算2步至t=.07，检验之前完整结果与最后接受checkpoint的保护、侧栏、主题及800×560窗口。ZIP含真实网格PNG，最终场通过独立时间项、几何、逐面动量与质量读回；六份最终数值文件与相同输入连续7步CLI逐字节相同。未重新实测原生文件对话框导入外部checkpoint。本地应用`desktop/dist/mac-arm64/CartMesh2D.app`，安装包`desktop/dist/CartMesh2D-0.4.5-arm64-mac.zip`。详细GUI动作和哈希包含在本节证据JSON；大文件不提交源码。

这些通道使用已发展入口层流，并非复杂绕流、湍流或任意边界配置的50万格资格。原Ghia限制重构细化未通过、圆柱瞬时启动载荷未认证等问题保留。本轮未运行外部OpenFOAM或Windows/Linux，不宣称工程级目标已完成。继续按既定路线完善压力求解效率、非定常精度与输运能力。

[真实网格和计时图](../artifacts/current/native-flow-pressure-performance.png) · [输入、资源和独立审核证据](../artifacts/current/native-flow-pressure-performance.json)。完整文件在`outputs/native-flow/pressure-performance/`，性能对照命令见DEVELOPMENT。

## 当前改进：较长物理时间与步长敏感性

对现有非定常核心做独立长时间验证，生产算法、默认格式、停止容差和App均未修改。新增可复用时间步比较器，重新审核网格/二进制来源、命令与字段、工况、时间步、从零开始的完整历史以及一致的终止时间。相邻场按真实面积和cell id比较。观测阶是诊断，不能将比较器的valid字段理解为工业质量等级。

900格Taylor–Green自由滑移解析衰减从此前t=.2延长到t=2，nu=.1、速度1、限制重构、每步容差1e-9，四档dt=.04/.02/.01/.005，分别实际推进50/100/200/400步。独立最终时间项、面动量和质量复算通过。最终速度相对解析L2误差为 **15.75%→7.68%→3.72%→1.75%**，能量相对误差为 **33.98%→15.96%→7.57%→3.54%**；速度相邻差给出时间阶1.025/1.013。当前一阶Euler的较大时间步可以充分迭代收敛，却仍有明显时间误差。

新增能量衰减诊断：单位方域连续解析能量 `E=Uref²/4 exp(-4νπ²t)`。本例解析衰减率3.94784，实测从3.80156逐档接近3.93046；中间步是CSV监测，只有最终能量独立从场复算，不能声称全部中间场均经独立审核。相对误差很重要，因为衰减后的绝对数值本身已经很小。

真实4,880格Re20圆柱另比较dt=.1/.05/.025、共同终止t=2；这是一个直径对流时间的启动过程，不是充分发展稳态或涡脱落认证。最细档初次触及180秒资源预算，在t=1.9处保留最后接受checkpoint，未算通过；根据实测单独以270秒预算从静止重跑，原容差1e-9、网格与格式保持不变。原超时记录保留，完成序列另有来源映射，不覆盖失败记录。重跑在187.39秒完成，三档均通过独立最终离散审核。末时刻速度相邻L2差4.7463e-4→2.5046e-4，对应观测时间阶0.922；末时刻水平力/密度/深度为2.82099→2.81089→2.80593，后两档差约0.18%。这只支持时间步趋近，不证明外域、空间分辨率或真实阻力准确。

圆柱边界瞬时从静止切到速度1，第一步载荷尖峰随时间步明显变化；图中保留全部尖峰并用对数轴展示后段。第一步峰值尚不能作为可信瞬态载荷，需平滑启动与时空细化进一步核查，不能仅凭末段接近就忽略它。

系统clang完整构建、CTest **111/111** 和前端 **123/123** 通过。新增比较工具的6项测试覆盖字段映射、网格/二进制哈希、步长及命令错配、重启拒绝；初版测试替身未写审核文件导致3项失败，补齐与真实验证器一致的文件输出后重跑通过。本轮只改验证工具及文档，桌面仍为0.4.4，未重新打包。

本轮没有新空间/域无关性、湍流或高雷诺外部CFD对照，也未认证新核心大网格吞吐量。原生压力线性求解在这组圆柱中占求解时间约86%，给后续性能工作提供实际依据；较高阶时间积分也值得以同精度成本对照，但本轮没有实现或宣称其收益。原Ghia细化未通过项保留，长期目标继续。

[真实圆柱流场、衰减误差与时间步比较](../artifacts/current/native-flow-transient-long.png) · [原始命令、资源停止与独立数值证据](../artifacts/current/native-flow-transient-long.json)。复现入口见DEVELOPMENT的“固定网格的时间步比较”；完整字段在 `outputs/native-flow/transient-long/`。

## 当前改进：桌面非定常监测与续算（0.4.4）

桌面已接入固定步长非定常层流：物理时间与内迭代分别显示，监测动能、受力和最大CFL，可从静止开始、从已保存状态继续，或打开外部checkpoint。选中的外部文件只先读配置，完整网格/状态匹配仍由原生核心严格核对。续算锁定物性、工况与对流格式，允许改变时间步和本次步数。

每次先在独立目录求解并验证摘要、场、时间历史和checkpoint，再替换完整结果。失败候选场只留诊断，不覆盖上次完整流场；取消后保留最后接受的状态。因此当前显示场时间可能早于可续算时间，界面分别标明。ZIP包含PNG网格图、完整求解文件、续算状态、时间历史及未完成诊断，说明文件同步更新。曲线按需抽样显示，CSV保留本次全部步记录；不承诺跨多次调用自动拼接历史。

macOS 0.4.4 打包App实际完成4,880格圆柱：2步→续算2步→故意限制为1次内迭代使候选步失败→再次续算、观察已接受步骤后取消→从取消checkpoint继续2步，最终 t=.07 s。dt=.01、nu=.1、速度1、限制重构、默认每步容差1e-6。失败时完整结果仍为t=.04；取消后完整结果保持t=.04、续算状态推进到t=.05。导出场经独立几何/时间项/逐面动量/质量读回通过，六份最终数值文件与相同输入连续7步CLI逐字节相同。此短启动只验证操作闭环与离散一致性，不验证脱涡频率或长期阻力精度。

原生CTest110/110、前端123/123通过；桌面实际验证主题切换、侧栏和800×560窗口。验收中发现并修复窄窗口曲线与摘要同占网格行的问题，新增真实布局不重叠断言；初次受限环境App启动返回-6，改在正常macOS会话启动后通过。独立读回第一次错误地假定ZIP没有顶层目录，因找不到网格失败；按实际目录重新提取后通过，原日志保留。

该阶段交付App为 `desktop/dist/mac-arm64/CartMesh2D.app`，当时安装包为 `desktop/dist/CartMesh2D-0.4.4-arm64-mac.zip`；当前版本已更新到0.4.5，见上方性能阶段。本轮未运行外部OpenFOAM、未实测Windows/Linux新功能、未重跑新核心大规模性能，亦未实测原生文件选择对话框导入外部checkpoint的完整GUI路径（元数据读取和核心严格续算另有测试）。固定Euler、无自动CFL、无湍流/热输运的限制保留。下一步是更长非定常标准流动与时间/网格敏感性，再推进标量/热输运；长期目标仍在进行。

[实际桌面截图](../artifacts/current/desktop-transient.png) · [命令、哈希、异常保护与独立读回](../artifacts/current/desktop-transient.json)。完整文件保留在 `outputs/native-flow/transient/`，不提交大网格和安装包。

## 当前改进：非定常层流核心与重启

已完成 **CLI/核心的一阶后向欧拉非定常不可压层流**，物理时间与SIMPLE内迭代分开。支持现有矩形通道、顶盖腔、固定固体外流从静止启动，以及单位方域自由滑移Taylor–Green无源衰减验证工况；默认物性与网格固定。每步均达到原动量、速度/压力变化和质量停止条件才接受。新输出包含逐步物理监测、实际CFL、时间积分项、最后接受状态的checkpoint；连续5步与2+3步的最终场、面通量及checkpoint逐字节相同。

修复了两个实际问题：初版缺少内松弛面通量修正，900格较大dt出现发散；加上旧时刻和内迭代的面插值缺陷修正后完成。4,880格圆柱启动在第三步曾停于动量残差5.22e-9，原因是全局线性停止条件掩盖小单元残差；现额外按每行对角尺度约束动量线性残差，原全局线性条件和非线性1e-9要求都保留。尝试过统一收紧全域绝对残差，但达到舍入停滞，已弃用，未作为成功方案。失败命令、日志与checkpoint仍保留在 `outputs/native-flow/transient/`。

最终同一二进制实际完成 **13组**：900格四档时间步、规则/扭曲网格各196/900/3,844格三档、780格通道/196格腔/4,880格圆柱各5步启动。独立读取器从真实多边形复算时间项、面压力/对流/完整应力、每格与全域动量/质量、CFL和最终动能；13组均通过，最大面重建差4.03e-12、动量归一化残差差3.81e-15、全域时间项加边界动量与单元残差和的差1.65e-14。CSV重建一致性容差不是物理精度认证。

900格、nu=.1、Uref=1、固定终止t=.2、限制线性重构、每步容差1e-9：

| 物理时间步 | u分量解析L2误差 | 压力解析L2误差 |
| --- | ---: | ---: |
| .04 | .00494839 | .00755833 |
| .02 | .00248673 | .00384641 |
| .01 | .00121569 | .00197162 |
| .005 | .000570181 | .00107636 |

同网格相邻时间步最终速度场的差给出观测时间阶 **.953 / .976**，支持当前一阶时间格式。另固定dt=.001、t=.05的三档空间验证，速度向量L2：规则 .00144494→.000102859→.0000548263；扭曲 .00177334→.000229763→.0000716232。两者均下降，但细档已混入有限时间步误差；规则网格动能误差从中档到细档还出现回升，不包装成纯空间二阶或全指标单调改善。

系统clang完整构建，CTest **110/110**、前端 **109/109** 通过；独立非定常验证器另覆盖篡改时间项、面动量、状态、步长、CFL、力、负时间、缺列、NaN及过期通过报告的拒绝。首轮完整测试中一个异常路径测试失败，是测试步长过小在参数解析阶段就被拒绝，尚未启动新输出；改用实际启动后的失败路径并重跑全套通过，未改宽生产验收门。

同一最终二进制还重跑196格稳态方腔和4,880格稳态圆柱，各自六份数值输出与原0.4.3逐字节相同，确认这两例稳态行为未改变。

该核心阶段当时未更新桌面，后续0.4.4接入见上一节；未运行外部OpenFOAM、未实测Linux/Windows、未做新核心大规模资格。原Ghia限制重构细化问题继续保留。当前没有二阶时间、自适应CFL、非定常圆柱脱涡频率/阻力精度、热输运或湍流模型；启动五步只是功能与守恒验证。后续优先接入桌面的物理时间、监测与重启，再完成更长非定常标准流动和时间/网格敏感性，继而按长期目标推进输运/湍流和规模性能。

证据：[真实网格与误差图](../artifacts/current/native-flow-transient.png) · [命令、哈希、结果及限制](../artifacts/current/native-flow-transient.json)。完整13组网格来源、场、日志和独立读回在 `outputs/native-flow/transient/final/`；复现命令见DEVELOPMENT_CN的“非定常层流与断点续算”。长期目标仍在进行。

## 当前改进：方腔取样误差分离

修复验证器的中心线取样，不改变0.4.3求解器、参考表、网格或已计算的场。旧IDW8把附近单元和两端壁值加权平均，不能重现线性场；在已知仿射场的三档规则布局上可产生约 .0102 的取样误差。现在从单元与真实壁面样本中共同选最近8点，使用距离加权的局部仿射拟合；坐标/权重归一化，秩不足或矛盾的重合样本明确失败。它用于点值诊断，不是守恒映射，也不保证任意数据无过冲。

在缩放1e-6/1/1e6的解析校准中，新取样器最大仿射误差4.44e-16；二次场的取样误差观测阶1.93/2.01。另有偏斜点、真实壁值、点重合、输入顺序、秩不足和非有限值测试。旧取样结果保留在 `benchmark.legacyIdw`；不同取样方法混在一条细化序列时明确拒绝，避免拿新旧报告直接比较。

对同一批哈希匹配的六份已保存方腔场重新读回，几何、连续性、逐面动量及单例阈值均通过，未重跑或改写流场。限制重构与Ghia的中心线RMSE：

| 最终单元数 | 原IDW8取样 | 新仿射取样 |
| --- | ---: | ---: |
| 196 | .0168986 | .0135804 |
| 900 | .00604057 | .00231881 |
| 3,844 | .00675440 | .00339637 |

**两种取样下，限制重构仍未满足原来“细档误差不超过粗档1.10倍”的趋势门。** 校正取样并未消除全部差异，不能把新误差较低当成求解器精度改善。迎风三档新RMSE .0266341→.0122882→.00560610，趋势通过。可复现审核器在上述混合两种格式的报告上有意返回1并保存 `valid:false`，因为仍有真实未通过项。

独立张量积取样在101个共同位置比较原生限制重构与FD129，RMS .0171774→.00333480→.000493750；FD自身33→65→129的相邻差也下降。这里只能推断两套离散方法在加密后更接近；FD129是有限网格数值解，不能据此断言Ghia错误或认证工程精度。原论文及其表格出处仍保留。下一步按持续目标推进非定常时间项、真实物理时间与时间步细化，并保留此稳态限制；不为追逐一组参考值调整生产算法或放宽验收门。

系统clang构建、完整CTest106/106、前端109/109通过；包含5项新增取样/混合报告检查的独立验证器25项通过。最终复现入口按预期保留未通过结果，相关CLI/验证器测试再次通过，篡改源网格哈希被拒绝。本轮仅修复验证工具，App仍为0.4.3；未新打包、重跑GUI、大规模求解或外部OpenFOAM。小型证据见 [取样对照图](../artifacts/current/native-flow-cavity-sampling.png) 和 [来源、误差与未通过项](../artifacts/current/native-flow-cavity-sampling.json)；完整日志在 `outputs/native-flow/cavity-sampling/`。

## 当前改进：守恒对称应力与壁面受力

0.4.3 默认黏性通量为 `-nu*(gradU+gradU^T)·S`。转置梯度真实进入每轮动量方程、原方程残差及导出，内部面两侧共用唯一通量并反号。壁面常量速度的切向导数为零，法向导数来自真实边界值和几何法向距离；当前轴对齐滑移边界保持零剪切。力的符号为流体对边界；不把单独的后处理应力修正冒充求解器修复。

默认 `forceX/Y` 与 `discreteForceX/Y` 使用相同的完整应力，原单元梯度牵引另存 `reconstructedForceX/Y`。新增所有无滑移壁面/顶盖的总力及黏性分量，便于封闭域和通道核查。CLI `--viscous-stress laplacian` 保留旧算子对照，桌面显式请求symmetric并核对元数据、力的有限性及一致性。旧结果不冒充新应力格式。刚体旋转零应力、仿射剪切/拉伸、斜面紧致通量、壁面切向导数和滑移剪切均有独立算子检查。

两种对流格式的通道/方腔/圆柱共14次，加上规则/扭曲三档制造解6次，**20个单例均实际收敛并通过独立几何、连续性和逐面动量审核**。最大面重建偏差1.18e-11，动量残差重建差5.18e-14。制造解 ν=.1、Uref=1、压力斜率1、停止容差1e-8；单例预算180秒，最细扭曲例实际100.49秒。没有把这些小例运行时间外推成50万格性能。

新增独立边积分审核：逐条真实壁边用16点Gauss积分解析压力/完整黏性应力，比较每单位壁长平均牵引，避免仅检查整体抵消。规则/扭曲制造解三档总牵引RMS（m²/s²）：

| 最终单元数 | 规则网格 | 扭曲网格 |
| --- | ---: | ---: |
| 196 | .0205429 | .0255088 |
| 900 | .00460712 | .00583658 |
| 3,844 | .00108972 | .00138418 |

末段观测阶1.99/1.98。速度L2为规则 .00761041→.00152613→.000351221、扭曲 .00826761→.00167570→.000386227。压力误差仍随加密下降，但同档比0.4.2略大（最细 .000557133/.000753404），不能宣称所有误差同时改善。

物理通道 ν=.01、Umax=1、宽4高1，两壁流体作用的解析黏性总力为0.32（除以密度与深度）。限制重构的780/3,596/15,372格结果 .3190651→.3197649→.3199035，相对误差 **0.292%→0.0735%→0.0301%**；迎风结果相近。制造解和通道解析积分均有整体自验，不为结果新设通过阈值。脚本初稿曾把梯度与应力混用、漏除面长，主审发现后修正，旧诊断输出不作为证据。

**原Ghia方腔细化检查仍未通过**：限制重构RMSE .0168986→.00604057→.00675440，仍超过原10%退化门。圆柱Re20的共享面受力Fx=2.148862，旧单元梯度诊断Fx=2.118658；前者现在与动量通量一致，但尚无圆柱域/网格无关性，不能作为通用阻力精度认证。下一步推进非定常所需的时间项、物理时间、重启与时步验证，并继续追踪方腔参考差异；高雷诺湍流、可压缩流和新核心的大规模性能资格未完成。

系统clang完整构建、CTest **106/106**、前端 **109/109** 通过；独立验证器20项正负向测试通过，增强壁面导数算子测试单独重跑通过。实际 macOS 0.4.3 App 生成4,880格圆柱、353轮收敛并显示/导出；ZIP原生场经独立动量读回，6份求解文件与同输入CLI逐字节一致。OpenFOAM文件结构独立读取通过，但本轮未运行原生checkMesh或外部CFD；Windows/Linux未实测0.4.3。首次GUI包装命令因系统没有timeout命令而未启动，改用有180秒闹钟的直接App执行后退出0；另有细档输入路径误写，修正为既有真实网格路径后成功，原失败日志保留。

证据：[真实网格、壁面牵引和误差图](../artifacts/current/native-flow-symmetric-stress.png) · [独立检查及桌面交付记录](../artifacts/current/native-flow-symmetric-stress.json)。完整网格/场保存在 `outputs/native-flow/symmetric-stress/`。长期工程CFD目标仍进行中。

## 当前改进：压力边界外推与独立方腔对照

修复了未知壁面压力的梯度重构：旧实现把它当成零法向梯度约束，线性压力 `p=2x−3y` 在壁面附近也无法精确恢复。现在用真实内部邻居外推压力；单层邻接不够时，确定性地加入第二圈真实单元，仍无法确定二维梯度则明确失败。速度的滑移/出口处理不变；压力修正的边界面流量仍保持原约束，未向不透流壁面添加虚构源。共享面压力、Rhie–Chow 抵消及单元压力修正使用一致算子，紧致法向压力差保留。原生算子与独立读取器均验证线性压力、斜网格、尖端第二圈及真正秩不足的拒绝路径。

制造解增加线性压力项 `Uref²*slope*(x+y)`，用 slope=1 显式覆盖非零壁面压力法向梯度，体积源同步由解析方程推导。限制重构、ν=.1、Uref=1、容差1e-8，规则/扭曲网格各三档共6次实际收敛并通过独立几何、连续性、源项与逐面动量复算。压力面积加权 L2/Uref²：

| 最终单元数 | 修复前 / 规则 | 修复后 / 规则 | 修复后 / 扭曲 |
| --- | ---: | ---: | ---: |
| 196 | .0131848 | .00763390 | .00954094 |
| 900 | .00604936 | .00190439 | .00233145 |
| 3,844 | 未运行 | .000477949 | .000583233 |

修复后末段压力观测阶约1.90/1.91；速度 L2 分别 .00828159→.00203308→.000504950 和 .00865317→.00213003→.000529178，末段约1.92。修复前只跑了两档，不虚构第三档对照。最终二进制另重跑两种格式的通道、方腔和圆柱14次，所有单例收敛及独立守恒审核通过。生产执行记录与 `--reuse` 独立读回摘要分别保存，后者不覆盖真实返回码。

**原 Ghia 方腔细化门依然未通过。** 限制重构中心线 IDW8 RMSE 为 .0166505→.00611448→.00682924，最细档退化仍超过既有10%限制。没有改门、换正式采样器或删结果。为定位差异，另写独立流函数/涡量有限差分诊断：33/65/129节点均以接受的新状态残差≤1e-8收敛，DST-I离散正弦测试误差5.55e-16。共同中心线的相邻网格差 .00673822→.00165059；本FVM限制重构与FD129在101个相同位置的RMS差 .0164238→.00276539→.000359813。该对照支持两套方法随加密趋近，但FD129不是解析真值，也不是外部软件认证。独立FD对旧Ghia表同样出现非单调误差，参考数据、插值和角点处理仍需分开调查，不能据此断言Ghia数据有误。

诊断期间发现并纠正过Poisson测试归一化/散度轴、RK2阶段残差误用及浮点坐标建网格问题；旧无效摘要保留在本地并明确排除。当前FD停止检查使用最终接受状态，中心线比较严格检查行列完整、有限值、文件哈希及Re100适用条件。生产求解器没有调用这套FD代码。

系统clang Release构建、完整CTest **106/106**、前端 **108/108** 通过；独立验证器 **17/17**，最终压力算子/制造解/读回三项再次通过。此轮未重跑10万/50万性能，不将旧时间当作新边界处理的规模认证；未运行OpenFOAM/checkMesh或Windows/Linux新求解功能。壁面完整应力与离散黏性通量的一致性、非定常和湍流尚待完成，工程CFD持续目标保持进行中。

桌面已重新打包为本地 macOS **0.4.2**，实际 App 生成4,880格圆柱并以限制重构完成353轮求解，显示速度场、导出ZIP及截图；打包原生二进制与上述CLI哈希一致。独立几何/连续性/动量读回通过，ZIP中32个文件与导出源逐字节一致，6份求解文件与同输入CLI逐字节一致；App自然退出码0。一次非提权启动返回134、无可用诊断；另一次命令误选153,208格高密预设并被中止，二者均保留，不能归因为锁屏或声称该高密绕流已完成。当前运行包为 `desktop/dist/mac-arm64/CartMesh2D.app`，没有将旧三平台下载包标成0.4.2。

证据：[真实压力场与细化图](../artifacts/current/native-flow-pressure-boundary.png) · [实际0.4.2桌面流场](../artifacts/current/native-flow-pressure-desktop.png) · [参数、哈希、独立对照与未通过项](../artifacts/current/native-flow-pressure-boundary.json)。完整场在 `outputs/native-flow/pressure-boundary/`，复现入口见 DEVELOPMENT 的制造解和独立方腔诊断。

## 前一验证快照：完整流动方程制造解

新增验证专用 `--case manufactured`：单位方形四壁静止，以解析无散速度和压力推导体积源，真实求解同一套稳态 Navier–Stokes / SIMPLE。没有将解析速度直接填入结果；默认物理工况不加源、不改变文件格式。源项另经多速度/黏度的有限差分导数复核，步长减半后误差下降。独立 Python 从 CM2D 重算几何、解析真值、体积源及逐面动量，防止只用导出 exact 字段相互证明。

规则与平滑扭曲方腔各三档196/900/3,844格，两种对流格式，共12次全部实际收敛，逐例几何、连续性、源项与动量审核通过。ν=.1、Uref=1、停止容差1e-8，单例180秒、7,000轮上限；最终均在上限内完成。以下为面积加权速度矢量 L2/Uref，**不是残差**：

| 最终单元数 | 规则 / 迎风 | 规则 / 限制重构 | 扭曲 / 迎风 | 扭曲 / 限制重构 |
| --- | ---: | ---: | ---: | ---: |
| 196 | .0503357 | .00819342 | .0504535 | .00855797 |
| 900 | .0278878 | .00203118 | .0282799 | .00212800 |
| 3,844 | .0146605 | .000504918 | .0148904 | .000529129 |

限制重构速度观测阶约1.83→1.92，最细压力 L2/Uref² 为 .000600409 / .000786400，末段压力阶约1.89；迎风末段速度阶约 .89 / .88。该光滑案例提供完整方程接近二阶的证据，但**原 Re100 顶盖腔细化未通过仍未修复**。制造解采用静止光滑边界及零法向压力梯度，不能据此排除顶盖角点、一般压力边界或 Cut-cell 问题，更不能代替湍流验证。

独立面项最大重建偏差3.42e-13，原生/独立动量残差差2.19e-14，全局动量抵消差1.19e-15；最大原生连续性2.62e-11。扭曲网格由相同连接关系的真实节点连续变形后重新计算面积，经过原生和独立几何检查，不以文件内 AUDIT 标记代替重算。完整场和命令保存在 `outputs/native-flow/manufactured/`；[实际网格、流场与误差图](../artifacts/current/native-flow-manufactured.png) · [参数、哈希与审核证据](../artifacts/current/native-flow-manufactured.json)。复现命令见 DEVELOPMENT。

系统clang Release构建、CTest **106/106**、前端 **108/108** 通过，独立验证器含12项正负向测试。与已打包0.4.1对照，同一196格方腔、两种格式的六份物理输出均逐字节一致。本次增加CLI验证设施，未重新打包桌面或宣称新增用户物理工况；现有本地App仍为0.4.1。Docker Desktop实际返回“manually paused”，本轮未执行OpenFOAM/checkMesh，Windows/Linux未运行。长期目标继续，下一步仍解决方腔边界精度与壁面应力一致性，再扩展非定常。

## 前一数值快照：共享面压力、试验性限制重构与桌面0.4.1

动量压力源改为唯一共享面压力的 Gauss 积分，内部两侧反号；Rhie–Chow 取消单元压力响应的项和单元速度修正同步使用该算子，面法向紧致压力差保留以控制棋盘模态。新增 `--convection upwind|limited-linear`，默认仍是迎风；限制重构用真实面中心和局部邻居界限，显式修正、原方程残差及导出采用同一面值。公开算法由本仓库实现，不是新提出的通用高阶算法。

独立 Python 审核现在从最终 CM2D 的真实几何及 cell u/v/p 重建压力、对流、黏性面动量通量，再检查局部动量残差、壁面压力/离散力和全局抵消。两种格式 × 三档通道、三档 Re100 方腔、一档 Re20 Cut-cell 圆柱，共14次均收敛且单例几何、质量和动量审核通过。最大逐面重建偏差8.49e-12，归一化动量残差与原生报告最大差3.86e-14，单元残差总和与外边界通量差1.21e-14。对比容差是浮点导出/重建一致性要求，不是新增质量等级或物理精度门。故意修改压力、对流/黏性面项、单元压力或删除新列均能被检测；旧版完整缺字段只报告动量审核不可用。

**这一阶段的精度资格仍有明确未通过项。** 使用原有 Ghia 中心线采样和未修改的细化趋势门：

| 方腔最终单元数 | 默认迎风中心线 RMSE | 限制重构中心线 RMSE |
| --- | ---: | ---: |
| 196 | 0.0280110 | 0.0163158 |
| 900 | 0.0123292 | 0.00612693 |
| 3,844 | 0.00691507 | 0.00683355 |

误差以顶盖速度归一化。限制重构的最细档比中档误差略大，超过既有最多10%退化的回归门；该系列整体 `valid:false` 保留在证据中，没有放宽门或删掉最细档。最细档另以1e-8停止容差求解2476轮，RMSE仍为0.00687357，最大u变化仅2.02e-4，不能归因于原迭代没有收敛。临时张量积线性中心线采样也未消除非单调趋势；正式报告仍保留原采样，不选择对结果有利的采样器。后续优先调查边界/压力—速度离散及限制重构，尚不能认证完整流动方程的二阶收敛。

独立算子检查中，解析光滑指数场、斜网格8/16/32上的受限面值重构观测阶约1.96/1.98，迎风约0.96/0.98；它使用解析梯度，只证明该重构算子，不替代上述流动误差证据。三档通道解析速度相对L2随细化下降。圆柱的重构应力Cd由迎风2.28950变为限制重构2.14468，对开域参考2.045的差分别11.96%/4.87%；同一32段几何、有限滑移域尚未做网格/域无关性，不以这一个改善授予阻力精度资格。

受力明确分开：`pressureForce` 复用动量面的压力；`discreteForce` 加上原 Laplacian 黏性动量通量；原 `forceX/Y` 仍是完整 Newtonian 应力的梯度重构诊断。限制重构圆柱Fx分别为2.15451（离散通量）和2.14468（重构应力），差别未隐藏。完整应力一致性、强偏斜网格上的 Rhie–Chow skew 响应还需要专项验证。

桌面0.4.1加入格式选择、请求/结果一致性和格式改变后的旧结果失效；旧摘要缺压力离散字段标为“旧结果未记录”，不能冒充共享面守恒。macOS打包 App 实际生成4,880格圆柱、限制重构353轮收敛，显示流场并导出ZIP；独立读回几何及动量通过，六份物理文件与同输入CLI逐字节一致，打包原生二进制哈希一致。侧栏/主题/窗口交互检查通过。初次自动审批超时未执行，随后一份 smoke 命令误用 `control=relative` 而失败，修正为实际支持的 `manual` 后通过；失败日志保留，不是修改求解器以掩盖失败。

系统clang Release完整构建、CTest **105/105**、前端 **108/108** 通过；新增独立验证器的6项正负向检查另行通过。图像直接读取本次真实CM2D/CSV，并在图上标记细化未通过。[数值/桌面证据](../artifacts/current/native-flow-convection.json) · [实际流场与三档比较](../artifacts/current/native-flow-convection.png)。完整字段及失败系列位于 `outputs/native-flow/face-conservation/`，不提交大网格。Windows/Linux新求解功能、外部checkMesh/OpenFOAM对照本轮均未运行。长期目标继续进行，当前优先补齐上述精度问题及应力一致性，然后扩展非定常；没有将本次提交当作工程CFD目标完成。

## 当前性能改进：固定稀疏结构与 IC(0)

流动系统已从每轮动态 map 改成固定、按列排序的 CSR 连接表，五份矩阵复用连接结构和数值数组，U/V/压力复用线性工作区。压力默认自然顺序 IC(0)–PCG，CLI 保留 `--pressure-preconditioner jacobi` 对照。未改变物理离散、网格门、3000次线性上限或真残差/外迭代停止条件；不做对角移位或隐藏回退，分解失败明确报错。独立矩阵回归检查已知解与真残差，不仅检查“返回成功”。

同一8 GiB Mac、系统 clang Release，通道每种方法三次取中位数；圆柱每种方法仅一次。原 map/Jacobi 来自紧邻的上一基线，CSR/Jacobi 与 CSR/IC(0) 使用本次同一二进制，计时有操作系统调度波动，不作通用速度承诺。

| 工况/单元数 | 原 map/Jacobi | CSR/Jacobi | CSR/IC(0) | 原时间/当前时间 |
| --- | ---: | ---: | ---: | ---: |
| 通道3,596 | 1.655 s | 1.201 s | 0.528 s | 3.13 |
| 通道15,372 | 6.250 s | 4.683 s | 1.687 s | 3.70 |
| 圆柱4,880 | 41.818 s | 31.591 s | 18.369 s | 2.28 |

压力累计迭代从15,270/15,246/324,230降到3,518/3,226/123,687；对应外迭代仍是44/21/336。通道与圆柱的最大场差约6.86e-12，最大面通量差约3.15e-14，原离散方程未替换。新核心的两档通道、两档方腔和 Cut-cell 圆柱共五例独立几何、通量、解析/参考值检查通过；圆柱阻力仍约2.28728，其既有约11.85%参考差并未因求解变快而消失。

新增两档**真实局部加密通道**，含粗细连接，保持4×1 m几何、峰值速度1 m/s、ν=.01 m²/s。各运行一次，并在最终原生网格上重新独立检查几何、质量守恒和解析速度/压力：

| 最终单元数 | SIMPLE次数 | 求解耗时 | 进程峰值 RSS | 速度相对 L2 | 压降梯度相对误差 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 112,368 | 18 | 40.02 s | 176.59 MiB | 4.477e-6 | 1.919e-4 |
| 500,472 | 15 | 164.24 s | 763.09 MiB | 9.563e-7 | 9.287e-5 |

两例都满足原停止条件。流量相对误差分别2.028e-6、4.921e-7；生成参数、网格/二进制/字段哈希及系统原始计时进入证据。五十万格上限设为300秒、进程组RSS 1.2 GiB，实际未触发，整进程耗时171.79秒。表中求解耗时不含读入和导出；峰值RSS是系统测量，不能和进程组抽样峰值混用。通道初值为解析抛物线，两档局部加密框宽不同；它们是规模试验，不据此声明严格网格无关性、任意十万格绕流同样快或湍流可用。百万格尚未试验。

系统 clang 完整构建、CTest **103/103**、前端 **106/106** 通过。新增线性代数测试实际编译运行；Luna执行固定验收，主代理复核原始日志。

[性能与独立验证证据](../artifacts/current/native-flow-sparse-performance.json) · [五十万格真实网格与流场](../artifacts/current/native-flow-500k.png)；大规模最终字段在 `outputs/native-flow/sparse-scale/`，原/新方法对照在 `outputs/native-flow/sparse-performance/`。只新增少量证据入库，大网格不进Git。此阶段尚未重新打包桌面0.4.0，旧安装包仍使用此前求解器；外部 checkMesh/OpenFOAM 本次未运行。下一步回到稳态物理基础：保守压力力、边界条件与高阶对流验证，同时保留这些固定性能对照。

## 工程扩展起点：性能基线

在 macOS 26.6.2、MacBookAir10,1、8 GiB、系统 clang Release 上串行测量。通道各重复三次，表中取求解秒数中位数及峰值 RSS 最大值；圆柱只有一次测量。读入与导出不计入“求解秒数”，进度输出计入；日志保留整个进程耗时及系统 RSS。不是跨机器速度保证。

| 最终网格/工况 | 单元数 | SIMPLE 次数 | 求解秒数 | 压力线性求解占比 | 峰值 RSS |
| --- | ---: | ---: | ---: | ---: | ---: |
| 通道 | 3,596 | 44 | 1.655 | 78.5% | 8.99 MiB |
| 通道细化 | 15,372 | 21 | 6.250 | 88.2% | 31.24 MiB |
| 圆柱 Re20 | 4,880 | 336 | 41.818 | 89.3% | 11.75 MiB |

通道以解析抛物线速度初始化，更细网格的迭代次数反而较少；不能将其时间外推到任意复杂流动。压力单次 PCG 最大迭代数从通道粗档406增至细档811，累计压力迭代为15,270/15,246，支持下一步优先研究压力预条件与稀疏运算。这是优化前基线；后续加速与十万/五十万格实测见上节。

三例独立读取几何和面通量、算例误差检查通过；通道速度相对 L2 为 1.65875e-4 → 3.30287e-5。3,596格通道和4,880格圆柱的原六份物理文件与0.4.0正式证据逐字节一致。profile 开关不改变物理解、迭代上限仍明确 `converged:false` 的回归已加入。Luna 完成代码统计口径复核，主代理核对实际结果；统计字段明确称“线性求解耗时”，不冒充包含组装的整阶段时间。

[性能及正确性证据](../artifacts/current/native-flow-performance.json)；完整数据与系统日志位于 `outputs/native-flow/performance-qualified/`。初次沙箱内 `/usr/bin/time -l` 因只读 sysctl 受限而无法测 RSS，日志保留在 `performance-baseline/`；表中使用获准读取系统计时后的完整测量。系统 clang 完整构建、CTest **102/102**、前端 **106/106** 通过。此次未改物理算法、前端或重新打包；外部 OpenFOAM 未运行。

## 原生二维稳态层流与桌面 0.4.0：本地交付

新增 `cartmesh2d_flow_cli`，直接读取最终多边形 `.solver.cm2d`，重查几何/拓扑与原 Solver 门。实现不可压、恒物性、稳态层流的单元中心 FVM、SIMPLE、Rhie–Chow、共享面对流/黏性通量及非正交修正。动量一阶迎风、Jacobi–BiCGStab；压力修正专用 Jacobi–PCG。停止条件与单位有明确公式，见 DEVELOPMENT_CN；不是新的模糊质量评级，也不是所有 CFD 软件的统一残差门。

| 真实最终网格 | 单元数 | SIMPLE 轮数 | 独立数值对照 |
| --- | ---: | ---: | --- |
| 矩形通道粗/细 | 780 / 3,596 | 54 / 44 | 速度相对 L2 7.727e-4 → 1.659e-4；流量相对误差 7.599e-4 → 1.539e-4 |
| Re100 顶盖方腔粗/细 | 196 / 900 | 111 / 403 | Ghia 中心线 RMSE 0.02773 → 0.01230（以顶盖速度归一化，含实际壁值的插值）；最大差 0.05741 → 0.02740 |
| Re20 圆柱 Cut-cell | 4,880 | 336 | 逐格连续性 1.65053e-14，全局相对失衡 9.65630e-15；有限场、质量守恒和预设工况检查通过 |

圆柱仍是32段折线，直径2 m、速度1 m/s、ν=0.1 m²/s，域为42×42 m，四向距物面约10D，上下为滑移。Fx/(ρ·depth)=2.28728 m³/s²，Fy 接近0；换算 Cd=2.28728，相对 Dennis–Chang 1970 的开域 Re20 参考2.045高约11.85%。这个比较仅作背景诊断，几何、有限域和离散均不完全相同；未证明网格/域无关或阻力工程精度。近场旧域出现出口回流后被明确拒绝，未通过改检查掩盖。

独立 Python 验证器重算真实 polygon 面积/质心/边关联，逐格累积 owner/neighbour 反号通量，核对 JSON 与 CSV 残差、解析速度/压降和参考中心线。五例使用最终二进制通过，网格/字段/二进制有 SHA256。脚本的算例误差阈值是本轮回归范围，不是通用 CFD 验收标准；完整数值而非单个 PASS 保留在证据中。

细通道曾在首轮压力线性求解达到3000次上限；使用能保留共轭方向的压力 PCG 后，在原阈值、原3000步上限内解决。真实3596格失败输入已进入 CLI 回归。另明确拒绝不连通流体、数值溢出、错误工况/未分类边界；封闭腔固定 cell0 压力，开放工况以右出口面压力0为基准；局部及全局质量守恒同时参与停止条件。

桌面在当前网格上设置外流/矩形通道/顶盖腔、ν、速度与上限，可取消真实子进程，切换速度/运动学压力色图并导出六份流动文件。部分复制失败会清理不完整顶层结果，失败/取消保留诊断；上限退出2明确显示未收敛。实际打包 App 生成4,880格圆柱、336轮收敛、显示和导出通过；ZIP 中最终网格及六份流动结果与独立验证逐字节一致，重算最大逐格积分失衡5.93969e-15。测试还覆盖未收敛显示、回流失败和真实进程取消。

系统 clang Release 完整构建、**CTest 102/102、前端106/106**；macOS arm64 打包及实际运行通过。新求解功能尚未在 Windows/Linux 实际运行，旧三平台网格生成证据不替代本轮求解器验收。本轮再次确认 Docker 手动暂停，**未运行外部 checkMesh/OpenFOAM 求解对照**。

[数值与桌面证据](../artifacts/current/native-flow.json) · [真实桌面](../artifacts/current/native-flow-desktop.png) · [场图](../artifacts/current/native-flow-fields.png) · [参考对照](../artifacts/current/native-flow-benchmarks.png) · [残差历史](../artifacts/current/native-flow-residuals.png)。完整数据在 `outputs/native-flow/formal-final/`，真实 App/ZIP 在 `outputs/native-flow/desktop-final/`；旧失败在 `coarse-probe/` 和 `formal-v1/`，没有覆盖为成功。安装包 `desktop/dist/CartMesh2D-0.4.0-arm64-mac.zip`，根目录启动脚本指向本次 App。

## 自研 FVM 扩散/泊松基础：已验证

新增独立 `build/cartmesh2d_fv_cli` 和 `cartmesh2d_fv` 库，直接读最终 `.solver.cm2d`，使用真实二维面积、共享边法向、加权最小二乘梯度、非正交扩散修正和 Jacobi-PCG。每条内部边通量两侧反号装配；重查拓扑和原 Solver 门，不依赖 AUDIT 自报。支持正的常数扩散系数、常量源及全部 Dirichlet 边界；尚不是 Navier–Stokes 流动求解器，尚未进入桌面包。

- 固定输入 `examples/acceptance/circle.xy`、同一域 `[-1.93,-1.97,2.07,2.03]`、固定物理加密带 .375 m。实际壁面格宽 .125/.0625/.03125，最终 **480 / 1592 / 5860** 个单元，包含真实切割和粗细连接。没有通过改变几何或计算域凑误差下降。
- 常量、线性、正弦制造解和内壁1/外域0扩散场，共 **12 例**完成。独立 Python 从 CM2D/CSV 重算面积、质心、关联、通量、RHS 及完整修正方程残差，核对原生结果，并绑定二进制/网格/字段哈希。
- 正弦制造解面积加权 L2 为 **0.0229412 → 0.00648431 → 0.00164611**；固定序列观测阶 **1.8229 / 1.9779**，不声明任意多边形普遍二阶。所有例满足预设完整方程 `||r||₂ <= 1e-12 + 1e-10 ||baseRHS||₂`；细档正弦实测 4.89010e-9，停止界限4.89765e-9。没有使用线性迭代的小残差代替非正交修正后的结果。
- 三档整体最坏逐格积分失衡 **7.7836e-9**、全局积分失衡绝对值 **2.4156e-8**。验证脚本早期额外尝试的绝对 `1e-10` 守恒门失败，记录保留在证据 diagnostics；它与原方程停止条件的量纲/尺度不同，现作为诊断报告，不把修改临时门槛说成原门已通过。生成器 Solver 门和数值停止条件未放宽。
- 独立审查找出的极大输入溢出假收敛已修复：`1e154` 边界值现在明确报数值范围错误并退出1；坏缓存索引、错误边关联/面积、非有限输入、未收敛上限都有最小回归。上限退出2并保留 `converged:false`，不包装成功。
- 系统 clang Release 构建无新增告警；完整原生 **101/101**、前端 **97/97**。真实场和误差图已人工复核。当前 Docker Desktop 被手动暂停，因此**本轮未运行外部 checkMesh/OpenFOAM 对照**；独立读取与解析解不能冒充它。新增求解工具只在当前 macOS 实测，未声称已有 Windows/Linux 求解器运行验收。

小型证据 [native-fv-foundation.json](../artifacts/current/native-fv-foundation.json)；[误差收敛图](../artifacts/current/native-fv-sine-convergence.png)、[三档制造解场](../artifacts/current/native-fv-sine-fields.png)、[三档实际扩散场](../artifacts/current/native-fv-diffusion-fields.png)。完整网格、12 例 CSV/VTK/日志在 `outputs/native-fv/verification-run/`；构建/测试日志在 `outputs/native-fv/`。复现命令、算法口径与当前边界条件限制见 DEVELOPMENT_CN 的自研求解器入口。

## 现有高密翼型：Re=1e6 实际 OpenFOAM 求解

按用户选择，直接复制既有 NACA2412 hybrid **140,305 格**（生成基线 `bf11105`）运行 OpenFOAM **2606 / simpleFoam / kOmegaSST**。源与计算副本的 points、faces、owner、neighbour、boundary 五文件 SHA-256 在求解前后完全一致；没有重新生成网格，也不将它当成当前核心新网格的验收。

- 恒定入口 **15 m/s**、参考弦长 **1 m**、nu=**1.5e-5 m²/s**、参考 Re=**1,000,000**、攻角 **0°**；湍强 1%、入口湍粘比初估 10。稳态、全湍流 SST 配 Spalding/omega 壁函数。前 200 步 upwind，之后仅 U 切换 linearUpwind，k/omega 保持 upwind。最初湍粘比偏高的试跑已停止并隔离，未混入正式结果。
- 标准 checkMesh **Mesh OK**，最大非正交 **69.8112°**、最大 skewness **3.8218**。扩展 `-allGeometry -allTopology` **仍失败 1 项，2720 个 concave cells**，另有 4 个短边关联点提示；未运行原厂质量配置组合，不声称扩展质量通过。
- 正常运行到 **3500 次 SIMPLE 迭代**，没有发散或负 k/omega/nut；全部最终单元字段有限。首个初始残差 Ux=**1.84e-7**、Uy=**1.03e-7**、p=**2.22e-6**、k=**6.74e-8**、omega=**1.66e-8**。压力未达到原 `1e-6` 停止条件，日志没有 `SIMPLE solution converged`；明确记录为**到达迭代上限，未通过严格收敛条件**，不使用后续压力修正残差代替。
- 最终最大速度 **19.1303 m/s**；进出口相对流量不平衡 **4.23e-12**，出口 **0/256** 面回流。Cd=**0.0137050**、Cl=**0.202453**；最后 35 个输出样本（3160–3500 步）的范围/均值分别约 **0.00203% / 0.02471%**，只是稳定性测量，不替代残差和精度判定。CmPitch 的正向以实际输出头 `pitchAxis=(0,0,-1)` 为准。
- 最终重新计算壁面 yPlus：min **3.6023**、max **24.5516**、算术平均 **14.9366**，按真实壁面积独立加权平均 **14.9586**。上下游边界仅距翼型约 **0.5c**，上下是 slip；这个受限域不能冒充自由远场。四层及当前近壁尺度没有证明 SST 壁面解析精度，未做域无关性、网格无关性、转捩或实验对照。
- 速度/压力图直接读最终场；流线只作局部流体内插值并遮挡实体。Cp 明确使用**出口表压零点**，不当作标准自由流 Cp。壁面 Cp 仍有可见尖峰，原输入为 80 点折线；本次没有确认尖峰来源，保留曲线供后续定位几何拐角、壁面网格和离散格式影响。

复现工具为 `tools/verification/run_airfoil_rans.py` 和 `tools/visualization/render_airfoil_rans.py`，具体命令见 DEVELOPMENT_CN 的 CFD 验证节。正式算例、原始日志、最终场与 ParaView 入口 `case/airfoil.foam` 位于忽略提交的 `outputs/airfoil-rans-Re1e6-final/`。四张 PNG 和含原始终末日志、配置、哈希的证据位于 [airfoil-rans-Re1e6.json](../artifacts/current/airfoil-rans-Re1e6.json)。两名 Sol 子代理分别复核工况/口径和实现后处理，主代理核对原始日志与最终图片。实际求解四段 Docker 包装耗时合计约 **2157 s**，不是通用性能基准。本轮只新增试算/后处理工具与证据，脚本语法及实际运行验证通过，未改 C++/前端、未重复宣称其完整测试已重跑。

## 跨平台桌面构建

macOS / Linux / Windows 已分别完成本机构建、打包与实际 App 验收。最终源码 `132adad` 的 [desktop-platforms 运行 34689931686](https://github.com/wuhahaTWT/cartesian-mesh-generator-2d/actions/runs/34689931686) 三个平台全部通过；运行包在对应系统完成测试和实际导入/生成/导出、独立读回后才上传。

- 用流式 ZIP 库替代 macOS 专用 ditto，保留 UTF-8 文件名和空目录；原生工具、11 个样例与平台 manifest 随包分发，打包时读取 PE/ELF/Mach-O 头并检查系统/架构，拒绝错误二进制混装。
- Windows 使用系统标准窗口控件、静态 CRT、UTF-8 源码与进程路径 manifest。精确方向谓词在 MSVC 上使用标准 32 位 limb + 64 位乘积；支持 128 位整数的编译器保持 64 位 limb 路径。二者保持相同的整数容量、精确符号与容差，新增消去/溢出/极端指数回归。
- 三个平台分别通过前端 **97/97**、原生 **99/99**，实际运行打包 App，在含中文与空格的路径下完成 PNG/JPG/hybrid 三例，ZIP 独立读回均通过。macOS 的 PNG/JPG 为 5,128 格，Windows/Linux 为 5,130 格；混合圆均为 5,348 格。本机还单独验证了标准 32 位 limb 路径的 99 项测试；不宣称不同编译器生成逐字节一致。
- 修复了 Windows 超时验证对 POSIX 信号的依赖；App 和导出 PNG 随包加载获 OFL 授权的中文字体，解决无中文系统字体的 Linux 方框字。最终 App 验收检查字体实际加载，并人工复核 Windows/Linux 界面和导出图片。
- 本轮未运行外部 OpenFOAM checkMesh 或 CFD；独立 OpenFOAM 文件读回不代替目标求解器原生检查。Windows CI 实际系统是 Server 2022，Windows 10/11 用户桌面与更多 Linux 发行版仍需实际使用反馈。

平台要求和下载/构建方法见 DESKTOP_APP_CN 与 DEVELOPMENT_CN。小型证据与截图在 `artifacts/current/desktop-platforms*`，完整日志位于忽略的 `outputs/platform-validation/`，Windows 运行包已下载到 `desktop/dist/platforms/windows-x64/`；Linux 包由上述工作流的 `CartMesh2D-linux-x64` 附件下载，本地慢速副本下载未完成。仅 Windows x64、Linux Ubuntu 22.04 x64、macOS arm64 进入当前自动验收矩阵；未宣称所有发行版或 CPU 架构已验证。

## PNG / JPG 图片输入

桌面“打开文件”现接受 PNG、JPG、JPEG。图片在本地解码，Worker 自动选择阈值并提取二维闭合轮廓；支持明暗、透明度、背景色模式、点选主体/保留多个区域和显式线稿填孔。默认保留孔洞；小噪点移除、未选主体与简化回退会显示提示。红线叠加预览后必须输入轮廓整体的实际宽度，换算为米再接入原生 XY 生成链；不会猜测照片尺度或恢复三维形状。

- 输入上限 20 MB / 4000 万像素 / 单边 20000；识别最长边 1024 像素，默认约 1 处理像素简化。保留环方向、相交和面积检查；64 环、4096 输出点上限，复杂输入明确拒绝。增加网格数量不能恢复图片损失的几何细节。
- 主进程保留所读原图快照与 SHA-256，确认后再次校验轮廓并标定；源单位锁定，避免重复换算。ZIP 增加原图、`image-outline.png`、`image-import.json`，沿用真实网格 PNG 和中文说明。
- 实际打包 App：透明 PNG L 形、JPEG L 形均得到 **5,128** 个最终单元；闭合矩形线稿显式填孔后 **5,016** 个。均按轮廓宽 200 mm 标定；主代理从最终 ZIP 独立读取 XY 和 OpenFOAM，核对宽 .2 m、源图哈希、最终单元数及导出图片。三例真实 OpenFOAM 2606 **标准 checkMesh 均 Mesh OK**，最大非正交分别 33.2919°、33.2919°、18.4349°；未运行扩展检查或 CFD 求解。这些是合成格式验收图，不是任意实拍照片成功率评估。
- JPEG 椭圆成功识别，但默认小规模生成仍被现有 Solver 质量门拒绝（7 项，含非正交 84.5815° > 70°、插值权重和体积比）。未放宽门槛、未导出为成功结果；保留输入和失败诊断。图片导入能力不代表通用核心鲁棒性已经修复。
- 空白图明确拒绝，取消保留旧几何；未标定时禁用确认，参数变化当场使旧轮廓失效；最终包实测 800×560 窗口确认按钮可达。JPEG 生成后返回开始仍能导出含图片记录的上一份结果。

本轮前端 **95/95**、原生 CTest **99/99**；原生算法和阈值未改。证据 `artifacts/current/raster-import.json`，复现图片与识别界面在同目录；完整日志/ZIP/网格位于忽略的 `outputs/raster-import/`。使用步骤见 DESKTOP_APP_CN 的“用照片或图片生成网格”。

## 陌生几何与导入范围抽查（图片功能加入前）

本次使用 `0ba7461` 打包 App 的同一组后端二进制，固定 Lref=2 m、wall/Lref=.02、background/Lref=.08、band=3、padding/Lref=.5；hybrid 三层、首层/Lref=.005、growth=1.2。没有针对单个形状反复调参。只验证小规模生成与文件读回，没有运行这些新例的真实 OpenFOAM checkMesh 或 CFD 求解，也不是完整 GUI 自定义文件导入的逐例验收。

- L 形、五角星凹多边形、普通 U 形凹槽、带凹口尖角、两个分离实体：两种模式均成功；最终导出 pure 2,056–2,440 格，hybrid 2,778–4,561 格。U 形槽宽 .9 m、普通双物体间距 .4 m，不将它们称为极端窄缝。
- 带孔内流：pure 最终2,004格成功，hybrid 因嵌套轮廓明确拒绝。自交蝴蝶结和重复顶点形成的零长边在 pure 中正确拒绝，不计为有效几何生成失败。
- .01 m 间隙双矩形（请求壁面 h=.04 m）：pure 2,112、hybrid 3,352 格成功。y=.12345 m 截面缝宽被完整保留，但只横跨两个单元；hybrid 保留468个层单元/请求756，首层壁长覆盖71.44%、完整三层覆盖60.46%。生成成功不等于足够的缝内分辨率或完整层覆盖。
- 共13份成功结果实际导出 OpenFOAM，独立 `check_openfoam2d.py` 读回全部通过，主代理逐份核对最终CM2D与reader的cell_count、valid和issues。pure 首轮摘要的 stabilized_cells 与最终 Solver 分割后数量不同，证据分别记录。这不是目标求解器的原生 checkMesh；这些案例没有 CFD 精度结论。
- 当时 App 不接受 PNG/JPG 位图轮廓，现已按上节加入。SVG 的 transform 会警告但未执行变换，圆角 rect 可能被跳过后继续导入其他元素，未闭合 polyline 会警告后自动闭合；width/height/viewBox 的物理尺寸语义没有完整应用。用户预览确认有帮助，但应优先补齐保形导入与明确拒绝机制。
- 旧 `hidden-spline-bulge.dxf` 漏检在当前打包导入器本次未复现：请求弦高 .001 m，曲线采样62段；独立10,001点密采样到导入折线的最大距离约 .00076946 m。这只是固定反例的有限采样复核，不代表通用严格误差上界已证明。

输入坐标、结果及主代理复核证据：`artifacts/current/shape-import-probe.json`；完整命令、原始日志与 case 位于忽略的 `outputs/shape-probe/`。下一步优先建立用户真实轮廓的导入保形、默认参数生成、边界条件与目标求解器验证闭环；图片识别及强制宽度标定现已接入，复杂背景与透视仍需人工处理。

## 桌面补充：回到开始、预览与导出图片

- 标题栏新增“回到开始”：清空几何/网格画布、结果面板及绘图缓存，释放主进程持有的预览网格；上一份磁盘结果继续可导出，开始下一次生成后替换。保留主题、显示模式与参数。生成和导出期间按钮禁用，生成可先取消。
- 大网格按 64 个单元分块缓存世界坐标 Path2D；缩放/平移只提交与视口相交的块（保留线宽余量），边界单独缓存，鼠标与窗口变化合帧，画布仅在像素尺寸变化时重分配。完整网格不抽样、不删单元，返回开始释放全部块缓存。
- ZIP 新增 `mesh-preview.png`（2000×1080 白底全景＋壁面局部）和 `README_CN.md`（简短中文文件说明）。图片独立读取当前最终网格，与界面主题/相机无关；边界层颜色使用最终网格 lineage，不套固定示意图，不声称外部 CFD 检查已运行。新增文件采用 ASCII 名称，避免跨系统解压文件名乱码。
- 固定喷管 148,287 单元在打包 App 中生成、返回开始前后分别导出，两份 ZIP 的最终 CM2D 均与上一核心交付 SHA-256 完全一致，PNG 字节也一致。返回后网格、路径缓存和主进程预览引用均释放；800×560 布局与保留导出入口实测通过。本次没有更改原生网格算法、外部阈值或重跑 CFD。


本次验证：前端 **78/78**、现有原生 CTest **99/99**，macOS 打包及真实高密 App 导出通过。148,287 单元、900×700 CSS 像素画布、连续 6 次缩放并用 `getImageData` 强制同步回读：修改前稳定约 **17.94 s**、单个大路径缓存约 **0.677 s**、最终分块版约 **0.190 s**。这是指定本机与强制同步绘图条件的对比，不是日常交互 FPS 或任意规模的流畅保证。证据见 `artifacts/current/start-export-evidence.json`，PNG 与开始页截图位于同目录。

## 本次交付：取消 Q1、修复目标求解器质量、侧栏伸缩

用户明确取消未经审核的 Q1：当前源码、CLI、桌面和导出不再执行或输出该合同。几何拓扑、内部 Solver、方向连通以及实际外部检查继续独立报告。历史 Q1 失败记录保留，不能解释为本次网格仍在进行 Q1 评级。

固定喷管保持原几何、Lref=6、wall/Lref=.00125、background/Lref=.0125、band=40、四层及首层/Lref=.0003125、growth=1.2、挤出厚度 .02。对连接区做精确并合/重划分，148,375 → **148,287 个最终单元**；保留 **8,768 个壁面层单元**，lineage 失配 0。新 mesh 不通过更换参数或修改外部阈值获得结果。

- 非正交：原 OpenFOAM `>65°` 的 448 面 → **0**；最大 69.9349° → **64.9644°**。
- 官方质量配置的 all-face determinant：4 个角部单元 / 20 个问题面 → **0**；独立复算最小值由 .00091742 提升到 **.00512556**（阈值 .001）。该指标包含前后 empty 面，取决于挤出厚度；与旧 internal-face 方向指标是不同公式。
- 完整几何检查仍报告 **5,677 个共面拆分单元**，不写总 PASS。独立 face-plane 复算 positive-side=0，全部 near-coplanar，集合与 OpenFOAM 一致；另保留 16 个短边关联点提示。这是保留共形粗细连接产生的共面侧面，未删除单元或屏蔽日志。
- 质量改进存在取舍：最小 face weight 为 .051547（限 .05）、volume ratio 为 .010371（限 .01），仍过原阈值但低于修复前。没有声称所有指标同时改善。
- 新增两个真实最小回归：喷管 face69310 的 66.794° 连接面、cell33552 的低 determinant 三角形；验证目标修复、面积、锁定层不改动和 determinant 缩放。固定案例通过不代表任意几何、任意 CFD 工况均合格。
- 界面仅显示“现代简约 / 科研终端”，保留背景插画，网格画布不透明。标题栏分栏按钮及 Cmd/Ctrl+B 切换设置侧栏，状态保存在本机；窗口最小 800×560，窄窗工具栏换行、结果区纵向排列和滚动。修复了反复收展侧栏导致预览持续缩小的问题，缩放和视图中心可恢复。

本次最终验证：系统 clang Release 构建和 macOS 打包成功；CTest **99/99**、前端 **74/74**。打包 App 实测侧栏、快捷键、800×560 布局及高密生成/导出；CLI、App 与 ZIP 内 CM2D SHA-256 均为 `1f6d8db4…4d24468a`。实际 OpenFOAM 标准检查 PASS；完整检查及完整+原厂配置组合仍因上述共面项 FAIL，原厂质量配置各项超限为 0。未新增 CFD 流场求解。

以下规模、CFD、旧桌面性能矩阵为 **修复前基线证据**，只对应各自记录中的二进制和网格；不能直接当成新核心全量重跑。最新构建、桌面与外部检查证据见 `artifacts/current/target-quality-and-sidebar.json`。

## 密度、单位与最终测量

- 两条 CLI 与桌面使用共同尺寸场：显式参考长度 `Lref`、壁面 `h/Lref`、背景 `h/Lref`、留白/Lref；贴体首层和挤出厚度也可按 Lref 设置。树深度是派生诊断。自动参考取包围盒最长边，随旋转可能变化；需要旋转不变参考时显式设置 Lref。
- XY/CSV/TXT/SVG 按源单位换算为米，不隐式归一到 1 m。非 DXF 的“自动”按米读取并提示。SVG CSS/viewBox/transform 的全部物理语义尚未覆盖。
- `.resolution.json` 测量**最终求解拓扑**：实际单元数、面积等效尺寸分布、壁面边长、owner 切向/法向跨度，以及切向跨度超标的壁长占比和边 ID。请求尺寸不等于最终已实现尺寸，面积等效尺寸不能替代边界层方向尺寸。
- 层保留检查逐个核对源层 polygon、最终几何、唯一来源、wall owner 和连续 owner/neighbour。首层覆盖、完整请求层数覆盖、首层高度和超标比例分别报告。纯路径为 `not_requested`；fallback 为 `no_layers_retained`；缺失/改形/断连为 `incomplete`。
- 按壁面目标细分输入直线段，精确保留输入角点；细分不恢复原始折线没有表达的曲率。圆基准实际是固定 32 段多边形，不能当成精确圆的几何误差验收。法向尺寸不是经认证的 y+。
- 桌面实际能力表中，纯 Cut-cell 默认安全深度为 11、hybrid 为 8；此前笼统写为 8 不准确。高于对应默认深度仍需显式允许，质量阈值不变。本轮增加按数量的自动配置，范围和局限见下节。

## 桌面 0.3：双主题、数量配置与图片托管

- 新版提供“现代简约”与“科研终端”两套外观，独立于网格显示模式，localStorage 记住主题。背景同人插画由内置 imagegen 生成，作为淡化的全界面背景持续显示，侧栏保留角色近景；首页为大幅背景，实际几何/网格画布保持独立不透明底色。原生 details 展开诊断，显示内部拓扑、Solver 与方向连通结果；外部检查另外运行。
- 自动数量档位为约 5 千、1.5 万、5 万、15 万、30 万，以及 1 千至 50 万自定义。目标范围为 ±30%，不是精度等级或任意几何成功承诺；30 万仍标为规模试验。旧常规/更密预设保留兼容入口。
- 初值按流体面积与数量预算选择较粗的可实现背景层级，为加密带预留约一半预算；多环使用明确标记的包围盒面积初估。原生求解仍保持真实 domain-solid 语义。最多 3 次按实测数量调整带宽或尺寸；混合层回退时可在同一总次数限制下尝试首层 0.65/1.5 倍。域留白、内外流、所选方法和质量门不被数量搜索改变。
- 圆柱、翼型、喷管可一键载入既有高密案例参数，明确替换 Lref、域留白与高深度选项，转入手动检查；该入口与保持计算域的数量搜索分开。
- 界面显示初值、实际数量、是否达到范围；保留最接近目标的成功结果，后续失败不覆盖它。可把建议或实际参数转入手动，提供整体粗/细按钮。首层几何初值为壁面目标的 1/4，仍无 y+ 或流场误差驱动。
- GitHub 图床直接使用现有公开仓库的 `展示素材/` 六张 PNG。Markdown 使用编码后的 raw.githubusercontent.com 链接，六图已匿名 HTTP 200 读取且与本地 SHA-256 一致。用户额外创建的本地 `展示素材.zip` 未纳入源码。

本轮数量初值矩阵（Lref 为圆 2 m、喷管 6 m，留白 0.5，alpha=0.1，显式允许高深度）：

| 几何/方法 | 目标 1.5 万 | 目标 15 万 |
|---|---:|---:|
| 圆/纯 Cut-cell | 14,764 | 142,444 |
| 圆/hybrid | 11,211 | 114,936 |
| 喷管/纯 Cut-cell | 16,632 | 114,352 |
| 喷管/hybrid | 回退被拒绝 | 180 秒超时 |

六个成功结果均进入 ±30% 范围；喷管混合路径仍存在连接区 Solver 质量问题，不能把其纯回退输出当成混合成功。首层重试与小扰动诊断仍回退或超时，单独保留在 `outputs/desktop-budget-validation-r2/`，不抹掉初始失败。原先手动验证的 148,375 格喷管仍见下节；数量初值还不能替代每个几何的工程配置。

实际打包 App 的混合圆“约 15 万”得到 114,936 格，预览、CLI 与 ZIP 中 CM2D 的 SHA-256 完全相同。该网格与 142,444 格纯圆都通过独立读取器（含 native 方向报告核对）和真实 OpenFOAM 2606 标准 checkMesh；扩展检查均 FAIL，分别标记 7,700 / 1,360 个 concave 单元，Q1 FAIL 保留。现代主题默认约 5 千档得到 5,204 格，并实测主题持久化、切换不改变网格以及实际参数转手动。

新打包 App 使用“载入已验证高密样例参数”生成喷管 hybrid **148,375** 格，预览与导出数量相同，App/ZIP 的 CM2D 与此前精确参数 CLI 逐字节一致（SHA-256 `463850cc…4d8791`）。该按钮可复现固定成功案例，不能据此声称通用喷管自动数量配置已修复。

背景后续调整已在打包 App 中以 5,204 格小例验证：选模型后背景仍在，网格画布底色不透明，主题切换不改变网格；前端 74/74。该历史版仍带 Q1；当前已按用户要求删除。证据与截图在 `artifacts/current/desktop-wallpaper.json/png`（截图后缀为 `-welcome` / `-mesh`）。

上述桌面基线轮没有修改 C++ 网格算法和质量门；本次目标质量修复见文首。实际数量控制、打包 App、独立读取与外部检查的最终证据汇总在 `artifacts/current/desktop-ui-refresh.json`；源码导航和复现参数在 DEVELOPMENT_CN.md。原生完整 CTest 101/101、前端 74/74 通过。完整 CFD 流场未重算，本轮外部验证为网格检查。

## 修复前核心的规模证据

下列高档和中档均以当前纯 CLI `052a1d51…cfb60bd6`、hybrid CLI `0467983d…a889058` 复现。最终数量是测量值，不是强制凑数目标。

| 几何 | 纯 Cut-cell 中档 | 纯 Cut-cell 高档 | hybrid 中档 | hybrid 高档 |
|---|---:|---:|---:|---:|
| 固定圆多边形 | 55,904 | 153,208 | 59,476 | 164,970 |
| NACA2412 | 66,207 | 133,810 | 68,806 | 140,305 |
| 喷管内流 | 52,824 | 141,488 | 56,348 | 148,375 |

十二例均真实导出：纯路径三类独立读取器通过，hybrid 四类通过；方向读取器明确使用 `--report` 核对 native 测量。十二例内部 Solver 和实际 OpenFOAM 2606 **标准 checkMesh PASS**；**Q1 与扩展 checkMesh 均 FAIL**。具体凹/近共面标记、实际参数和日志分别见：

- `artifacts/current/pure-high-regression.json`
- `artifacts/current/hybrid-circle-164k.json/png`
- `artifacts/current/hybrid-high-regression.json`
- `artifacts/current/engineering-middle-validation.json`

万格纯喷管额外固定为 **11,472** 格：wall/Lref=.0025、background/Lref=.0125、band=3、Lref=6，不增加远场。三类读取器、Solver、标准 checkMesh PASS；Q1/扩展 FAIL（968 个 concave-or-planar 标记）。证据 `artifacts/current/pure-nozzle-11k.json`。当前制造解细档另验证了 14,800 格纯圆，见 CFD 部分。此前同轮纯翼型低档 17,112 的历史记录保留于 `outputs/engineering-scale/`，不冒充本次最终核心的低档重跑。

尺度阶梯的 wall/background/band 参数并非全部只有一个变量变化，尤其 band=3→40 改变近壁加密范围；这些是规模/能力验证，不能直接当成严格网格收敛序列。真实 CFD 序列另列。

## 修复前有限缩放、旋转与边界层

当前核心对三种固定几何，各测缩放 0.001、1000 与旋转 17°，**9/9 hybrid 成功**，无纯网格回退/超时。四类读取器均通过并核对 native 方向报告。

| 几何 | 缩小 0.001 | 放大 1000 | 旋转 17° | 最终层单元 |
|---|---:|---:|---:|---:|
| 圆 | 16,672 | 16,674 | 16,680 | 1,280 |
| 喷管 | 8,428 | 8,424 | 8,172 | 2,400 |
| NACA2412 | 18,575 | 18,570 | 18,558 | 932 |

均 Solver PASS、Q1 FAIL。**没有运行该九例矩阵的外部 checkMesh**，不能借用原尺度外部结果。最终数量少量变化，不能宣称缩放后逐个单元拓扑一致；更不是任意几何成功率。此前纯路径的有限缩放/平移/旋转记录仍保留，历史失败也未删除。
证据：`artifacts/current/hybrid-finite-robustness.json`；前一核心的 9 例原始记录在 `outputs/engineering-hybrid-endpoint-matrix/`。

高档实际层保留：

| 几何 | 保留/请求层单元 | 首层壁长覆盖 | 完整四层壁长覆盖 | 首层高度超标 |
|---|---:|---:|---:|---:|
| 圆 | 5,120/5,120 | 100% | 100% | 0 |
| NACA2412 | 3,380/3,400 | 99.8467% | 99.2178% | 0 |
| 喷管 | 8,768/8,816 | 100% | 98.9033% | 0 |

三个高档层来源/连续性失配为 0。圆请求 wall/Lref=.0025，最终 owner 切向跨度 P95=.002453506365、最大 .002511985635，**切向超标仍占壁长 5%**。层全部保留不代表所有方向尺寸达标。

## 修复前性能、真实 App 与导出

当前高档资源复测使用系统 `/usr/bin/time -l`，主代理从原始日志读取 RSS，并将六份新 CM2D 与已检查产物逐字节核对。以下是诊断运行；存在其他验证工作，不能当成隔离性能基准或跨硬件承诺。

| 模式/几何 | native 秒 | maximum RSS bytes |
|---|---:|---:|
| hybrid 圆 164,970 | 23.8122 | 1,128,448,000 |
| hybrid NACA 140,305 | 33.2959 | 1,077,526,528 |
| hybrid 喷管 148,375 | 60.6987 | 1,142,833,152 |
| pure 圆 153,208 | 30.0022 | 694,026,240 |
| pure NACA 133,810 | 44.3776 | 972,439,552 |
| pure 喷管 141,488 | 49.2977 | 1,011,154,944 |

macOS `peak memory footprint` 与 maximum RSS 是不同口径；该批前者最大约 1.83 GB，不可混用。完整参数、wrapper 时间和两种内存指标在 `artifacts/current/engineering-resource-measurement.json`。资源包装器的三次 pure 执行误复用了原 OpenFOAM 输出目录；原 CM2D 未覆盖，六份新旧 CM2D 哈希均相同，实际命令和此问题已保留，不虚构独立 pure 资源案例目录。其他旧测量的 time 权限失败/RSS 缺失仍保留，不能把 null 当零。

此前同参数 hybrid 圆在 300 s 超时；此次第一份合格生成 native 21.8387 s。超时不是旧版完成耗时，不能据此计算正式加速比。该次 solver 候选全域重建仅 3 次，源修复 .798747 s、最终重划分 1.555667 s。

**此前实际打包 App 验证** 已完成同一高档圆：预览、OpenFOAM、ZIP 均 **164,970** 格，CLI/App/ZIP 的最终 CM2D 逐字节一致，侧栏底部可达。native **24.2598 s**；整个 App 启动、生成、渲染、ZIP、截图和清理 **111.680827 s**，桌面端到端耗时仍有优化空间。

默认深度 8 首次正确拒绝高档；随后使用已有显式高深度选项成功，默认上限和质量门未变。此结果纠正了此前“锁屏必然不能运行 App”的推断。截图已检查；Q1 FAIL 从真实 renderer DOM 读取，该行不在本张截图可见范围，结果区可滚动。
证据：`artifacts/current/desktop-hybrid-circle-164k.json/png`。此前纯圆 153,208 和贴体喷管 148,394 的实际 App/ZIP 证据属于各自旧核心快照，不冒充本次最新运行。

安装产物：`desktop/dist/mac-arm64/CartMesh2D.app`。使用系统 clang++，未签名打包；没有声称完成第三方签名、公证或所有机器的安装验收。

## 已有 CFD 与精度边界

已使用本地 Docker 的 `opencfd/openfoam-run:2606` 实际执行，独立读取、内部 Solver、Q1、标准/扩展 checkMesh、迭代收敛和空间误差分别报告。

### 制造解

固定多边形外域，解析调和场 `T=x²-y²+.15x-.05y`。实际生成、writeCellCentres/writeCellVolumes、laplacianFoam 和误差评估均已执行。

| wall h/Lref | 最终单元 | L2 误差 | Linf 误差 |
|---|---:|---:|---:|
| .04 | 1,292 | 3.179e-3 | 1.853e-2 |
| .02 | 4,144 | 1.037e-3 | 1.328e-2 |
| .01 | 14,800 | 3.200e-4 | 5.722e-3 |

三档末次线性残差 <1e-12；标准 checkMesh/Solver PASS，Q1/扩展 FAIL。最新核心已按原精确参数重新生成三档，最终 CM2D 均与实际求解输入逐字节一致，因此复用对应已有场和误差证据。它只验证指定标量方程与多边形域，不代表湍流、传热或任意几何精度。
原始结果：`outputs/engineering-cfd/mms/`；当前拓扑关联：`artifacts/current/cfd-topology-compatibility.json`。

### 固定喷管层流

原始 `nozzle_profile.xy`、Lref=6 m、挤出 .02 m、入口 U=(.1,0,0) m/s、nu=.01 m²/s、出口运动学 p=0、无滑移壁面、前后 empty。入口/出口明确绑定源段 81/40；这是验证工况配置，**产品分段 patch UI 尚未完成**。

| 最终单元 | 收敛迭代 | 运动学压降 m²/s² | 相对流量不平衡 |
|---:|---:|---:|---:|
| 2,152 | 279 | .08624959289 | 1.09e-12 |
| 6,200 | 574 | .08760781335 | 3.58e-12 |
| 52,824 | 3,952 | .08874178 | 1.14e-12 |

最新粗/细档 CM2D 与已有 CFD 输入逐字节一致。中档从旧 6,204 变为 **6,200**，已在全新目录重新运行真实 simpleFoam，未复用旧中档场。三档末次初始残差均 ≤1e-8；最新中档 Ux=1.12e-9、Uy=9.78e-9、p=9.40e-9/2.54e-10。标准 checkMesh/Solver PASS，Q1/扩展 FAIL；扩展标记分别 148/320/496。

更新后相邻压降差 **1.5503% → 1.2778%**，统一出口区间上的归一化速度积分 L2 差 **.00227855 → .00149993**。差异在减小，**尚未建立网格无关性或通用误差保证**。此前 6,204 中档的独立内侧截面后处理未显示单调收缩；该额外后处理未对新 6,200 中档重测，不套用其旧数值。

当前合并评估：`outputs/engineering-cfd-final-r02/current-three-grid-comparison.json`；新中档场/完整命令/原始日志：`outputs/engineering-cfd-final-r02/`；粗细场：`outputs/engineering-cfd/nozzle-flow/r01`、`r03`。汇总及更新图：`artifacts/current/nozzle-flow-validation.json/png`。可复现驱动：`python3 tools/verification/run_nozzle_flow.py --output-root outputs/nozzle-flow-new`，要求新目录；子集用 `--cases r02`。

## 先前核心改动与验证

- 输入角点的算术格点归属统一登记，完整格与 Cut-cell 使用共同输入端点；格点几何预算和舍入预算分开。
- 几何长度/面积容差恢复量纲一致；拓扑面积用局部原点计算。共享输入端点直接返回原点，避免相邻层边近共线重构出末位偏差造成误停层。hair-ray 端点判定按物理长度比较。保留三尺度真实失败回归。
- 不可变轮廓诊断缓存、共享边点索引与 R1 顶点索引减少重复搜索；不改变有效性门。
- 源质量候选带真实邻格，并比较相对原补丁的改善；最终批量支持 2→2 切分和 2→1 精确合并。原备用路径允许的凹合并仍受原 concavity/angle/aspect 限制；每次提交仍经过完整拓扑和 Solver 分数门，不触碰不可变层单元。
- 保留 18 源多边形和 21 最终单元的真实局部案例。后者候选全域重建 71→15，前后都保留面积、输入身份并通过 Solver。CM2D 抽取的单元身份只是诊断跟踪，不声称恢复全部 native provenance。
- 方向连通修复与独立读取适用于未耦合、平面、均匀挤出、前后 empty 的范围；不能冒充通用三维 determinant 验证。
- 面平面独立诊断保持原 dot > -1e-6；旧 140,306 格 NACA 的 2,724 标记与原 OpenFOAM 集合完全一致，全部近共面、无 dot > +1e-6。仅解释该固定网格，原扩展 FAIL 保留；当前其他凹面提示不可据此抹掉。`--expected-set` 不匹配现返回失败。

上一轮核心交付完整 **CTest 101/101（26.63 s）**、前端 **54/54**、macOS 打包通过。该轮新增桌面功能未改变原生算法；本次修复范围见文首。日志：`outputs/concave-union-final-{ctest,npm,package}.log`。

## 修复前质量审核依据复核

用户指出 Q1 未经本人审核后，重新核对源码、成熟工具资料和真实外部结果。本次是审核依据复核，未改变 Q1 / Solver 阈值、评级或网格算法。

- Q1 是项目内部诊断；未找到完整的逐项阈值来源及工况误差校准记录，不能把既有测试通过当成标准已经成立。原始面积比硬阈值 0.05 与允许的分级裁切配置并不总兼容：细格保留面积分数 0.1、邻格宽度为其 2 倍时，面积比可为 0.025。它仍可能提示数值风险，但不能把此项失败自动解释成拓扑非法或 CFD 必然失败。边界层仍只 OBSERVED。
- 推荐以目标求解器的独立工具为主：OpenFOAM 2606 `checkMesh` 标准检查、完整拓扑/几何检查、显式 `meshQualityDict` 分开记录；保留版本、原始日志、参数和问题位置。不将 Fluent 的 skewness 数值直接搬到 OpenFOAM；公式和适用单元类型不同。
- 固定 148,375 格喷管，在输入网格未变的副本上新跑：标准 PASS；完整检查 FAIL（face-plane 检查标记 5,641 个 concave 单元，并报告 16 个短边关联点）；再启用原厂配置后 FAIL，质量配置额外标记 448 个非正交角 >65° 的面、20 个 determinant 条件面，共 468 个质量问题面。各条目的数量与定义保持分开，不能写成 468 个坏单元。
- 原厂配置用于本次有出处的对照，不等于普适工业标准。例如 `minVol=1e-13` 是有量纲、依赖尺度的条件；几何合法性、数值风险和工况适用性仍需区分。当前失败没有通过修改阈值消除。
- 后续验收应分别回答：几何拓扑是否正确；目标求解器检查发现哪些风险；指定工况是否收敛、守恒且物理量合理；在一致的加密序列中关键结果是否稳定、误差是否满足任务要求。GCI 只在条件适合时使用，不能将不一致的加密带宽序列直接包装成正式 GCI。

证据：`artifacts/current/quality-review.json`；原始日志、原厂配置、质量场和可视化问题集合在 `outputs/quality-review/`。本次未新增 CFD 求解，不借用其他案例的求解结果证明这个高密喷管满足任意工况。

资料：[OpenFOAM 自定义质量检查](https://openfoam.org/release/2-2-0/meshing-tools/)、[Fluent 的质量定义](https://ansyshelp.ansys.com/public/Views/Secured/corp/v242/en/flu_ug/tgd_user_report.html)、[Ansys 流体网格建议](https://ansyshelp.ansys.com/public/Views/Secured/corp/v252/en/discovery/UDA/user_manual/meshing/topics/r_meshquality_bestpractices.html)、[Gmsh 质量测量 API](https://gmsh.info/doc/texinfo/gmsh.html#index-gmsh_002fmodel_002fmesh_002fgetElementQualities)、[NASA 网格收敛方法](https://www.grc.nasa.gov/www/wind/valid/tutorial/spatconv.html)。

## 保留边界与下一步

1. **质量优先于继续堆数量**：当前已取消 Q1；固定喷管的官方配置超限已修复，扩展 checkMesh 的共面拆分诊断仍保留。边界层是否适合工况仍需实际流动验证。不能把标准 Mesh OK、Solver PASS 或残差收敛当成工业质量全部合格。
2. **精度**：目标尺寸与最终尺寸有偏差；折线输入本身的几何误差、边界层方向尺度、流场误差和成本需共同控制。尚无自动 y+ 或误差驱动自适应闭环。
3. **鲁棒性范围**：九例有限扰动不代表任意几何。自交/零面积/非流形/分类冲突必须显式失败；旧重叠和伪造全零 AUDIT 反例仍在 `tests/repro/core-probe.cpp`，属于待修缺陷，不能据此放宽规则。
4. **输入与边界**：旧 `tests/repro/hidden-spline-bulge.dxf` 反例本次未复现，有限复核见文首；SVG transform、物理尺寸及部分元素处理仍不全。pure 可处理部分多环，局部孔洞可能显式拒绝；hybrid 已在本次分离双实体外流小例成功，嵌套孔洞仍拒绝，不能把单个微缝成功推广为通用窄缝支持。
5. **桌面产品完整性**：保持最终求解网格预览和 ZIP；高档完整流程约 112 s，仍需定位预览/导出开销。数量自动配置已接入；分段边界条件 UI、物理精度预设、大 CAD 输入和跨机器安装还未全面验收。仅支持所列 OpenFOAM 路径，未验收所有 CFD 软件。
6. **工程纪律**：构建只用 `build/`，实验在忽略的 `outputs/`，不新增长期隐藏工作区/日期计划/代码副本。Q3/Q4 开关保留默认关闭及累积关系；不因局部失败无限追加阶段。确定性同工具链与跨编译器逐字节一致分别验收。

下一步以目标求解器的真实检查和指定 CFD 工况验证继续拓展质量修复范围，建立误差验收并完善自动选参。新增问题归入这些既有目标，不能靠换参数、删坏格或隐藏告警“通过”。
