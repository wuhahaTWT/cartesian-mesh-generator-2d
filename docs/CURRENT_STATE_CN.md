# 当前状态与接手重点

更新：2026-09-10。本文件是唯一当前状态入口；数字只适用于注明的输入、参数和工具链。

## 唯一版本

用户已确认以 `9bddfbf` 的桌面 0.2.0 为唯一产品基线。新版现在位于根目录，隐藏工作区已删除。
目录清理已提交并推送 `0ef933d`。本轮继续完善预览、导出、滚动与表单操作；本轮还修正了求解拓扑对笛卡尔矩形的处理；质量阈值未修改。
旧分支/标签仍在 Git 中，查找方式见 [开发导航](DEVELOPMENT_CN.md)。

此前 `703122a` 的旧版审计/隔离试改不能当作新版整体审计。旧新两版分叉于 `874625a`，
W1 核心修复通过等价 cherry-pick 进入新版；后来新增了尺寸场、纯路径 Q1 和重做的桌面。
新版没有自动继承旧前端的全部提交。不能再仅凭主目录名或提交日期认定版本。

## 当前能力

### 工程尺度与持续目标

用户已授权连续推进“有明确支持范围的十万级工程版本”，不再逐项审批普通实现。
范围包括统一无量纲尺寸、约 1万/5万/10万最终求解单元及有限扰动验证、桌面与导出、
解析/制造解和代表工程工况的多网格 CFD 验证。当前尚未完成该整体目标。
Astra 负责设计、核心修复和小验证，Luna 负责固定批量验证、独立读取和证据整理。
失败交回主代理判断；不放宽质量门，不把回退、缺文件或超时计为成功。

两条原生 CLI 和桌面手动设置已接入共同的尺寸场：显式参考长度 Lref（m，可省略并报告为
包围盒最长边）、壁面 h/Lref、背景 h/Lref、留白/Lref。贴体首层与挤出厚度也可按 Lref 设置。
计算域采用包住原始几何的正方形；两条路径使用相同的取整算法。
包围盒自动参考仍随旋转改变，需要旋转不变的工程参考时应显式指定。
旧 CLI 层级入口仍用于回归，桌面手动入口使用相对尺寸；自动种子转换为同一相对参数接口，
现有种子仍属于小规模设置，尚未作为十万级预设发布。

XY/CSV/TXT/SVG 按源单位换算为米，不再隐式归一到 1 m。非 DXF 的“自动”按米读取并提示；
未实现对 SVG CSS/viewBox/transform 全部物理语义的解释。指数负号坐标已修复；不完整环和
损坏坐标行显式拒绝；写出的坐标采用 17 位有效数字。输入轮廓范围改为迭代计算，避免大数组展开。

两路径生成 `.resolution.json`，在最终求解拓扑上记录实际单元数、面积等效尺寸分布、
壁面边长及 owner 切向/法向跨度，报告切向跨度超过请求值的壁长占比和边 ID。
该统计是尺寸诊断，不能替代 Solver/Q1；法向跨度不是经认证的首层高度或 y+。
边界层报告现逐个核对源层单元与最终求解 polygon 的几何及唯一来源、逐列核对 wall owner 和
连续 owner/neighbour；分别报告最终保留数、首层覆盖壁长、完整请求层数覆盖壁长、首层法向
高度及其超过请求值的壁长占比。源标签或原始 cell_count 本身不作为保留证据。
纯路径为 `not_requested`，纯网格回退为 `no_layers_retained`，缺失/改形/断连为 `incomplete`。
已通过三尺度小矩形与伪造来源、改形和断连测试：矩形最终保留 40/48 个请求层单元，首层覆盖
100%，完整两层覆盖 66.67%；不能用首层覆盖率掩盖局部停层。独立读取器重算实际 polygon、
wall 段归属及层间连接，拒绝伪造统计、来源及互换壁面端点。

进一步修复了贴体尺寸只作用于余域的问题：现在按 wall h/Lref 细分原始直线段，
精确保留输入角点，不把直线细分冒充曲率恢复。同一桌面圆例从 728 单元/128 层单元变为
1,512 单元/512 层单元；wall h/Lref=0.03125，实际 owner 切向跨度 P95=0.02549，
超标壁长从 100% 降到 0；512/512 层单元保留，首层及完整四层壁长覆盖均 100%。
真实预览与 ZIP 导出数量一致，侧栏到底可达；三种独立读取器通过。
本轮完整 CTest 97/97（89.22 s）、桌面测试 54/54 通过，读取器收紧后相关 CLI 回归再次通过。
内部拓扑/Solver PASS、Q1 FAIL；真实 OpenFOAM 2606 标准 checkMesh PASS，
扩展检查 FAIL（648 concave cells）。证据：`artifacts/current/boundary-layer-resolution-*`。
该例用于尺寸与层保留验收，不充当十万级规模证据。

本轮系统 clang++ 构建、完整 CTest 96/96（74.16 s）和桌面测试 54/54 通过。独立小矩形回归在比例
0.001/1/1000 下两路径均成功，并由 `check_mesh_resolution.py` 重算面积和方向尺寸核对；
伪造统计被独立读取器拒绝。此前固定绝对首层的错误缩放测试已纠正，不作为核心缺陷证据。
真实桌面手动圆例 3,568 单元，自动贴体圆例 728 单元，两者预览/ZIP 导出数量一致、侧栏到底可达，
内部 Solver PASS、Q1 FAIL；这两个案例不是十万级规模验收。最新手动圆例真实 OpenFOAM 2606
标准 checkMesh PASS；扩展检查 FAIL，标记 428 个 concave cells，其余检查通过。
上述旧手动圆例最终 owner 切向跨度超标壁长占比约 9.84%，
如实显示在桌面和报告中，不把请求尺寸当成已实现精度。
证据保留在 `outputs/engineering-resolution/` 和 `outputs/engineering-baseline/`。

Docker 29.7.2 已启动且本地 OpenFOAM 2606 镜像可用。固定圆例的三档 MMS 前置网格
（wall h/Lref=0.04/0.02/0.01，background=0.1/0.05/0.025，留白0.5，Lref=2）
此前都在源拓扑处出现 16 条 UnclassifiedBoundaryEdge，未进入求解。
圆极值附近内部格边端点末位不一致，如 -0.93000000000000005 与 -0.92999999999999994。
现已通过提前登记输入端点的算术格点归属，让完整单元与 Cut-cell 共享同一不可变输入顶点；
多个不同输入点落到同一算术格点时明确失败，几何 weld 预算和质量阈值不变。
保留原始失败日志于 `outputs/engineering-cfd/mms/`，四顶点菱形最小案例加入共享构造测试；
同参数旧运行时失败 16 条边，新核心通过，并验证 0.001/1/1000 三个比例。
修复后完整 CTest 96/96（91.38 s）和桌面测试 54/54 通过；同参数三档网格均通过标准
checkMesh，扩展检查仍 FAIL。

同一固定多边形外域已实际运行 OpenFOAM 2606 harmonic quadratic MMS：

| wall h/Lref | 最终单元 | L2 误差 | Linf 误差 | 标准 checkMesh | 扩展 concave cells |
|---|---:|---:|---:|---|---:|
| 0.04 | 1,292 | 3.179e-3 | 1.853e-2 | PASS | 60，FAIL |
| 0.02 | 4,144 | 1.037e-3 | 1.328e-2 | PASS | 104，FAIL |
| 0.01 | 14,800 | 3.200e-4 | 5.722e-3 | PASS | 196，FAIL |

三档均通过独立读取，内部 Solver PASS、Q1 FAIL，末次线性残差低于 1e-12。
L2 误差随加密下降；它是指定解析标量方程在实际多边形域的证据，不代表湍流/传热/任意几何精度。
脚本先执行真实 writeCellCentres/writeCellVolumes，再配置边界、运行 laplacianFoam 和重算误差；
扩展检查 FAIL 与 MMS 流程通过分开保存，overall valid 仍为 false。
代表工程流动工况及十万级规模矩阵尚未完成。

### 曲线加密（2026-09-09）

蛇形体自动更密：纯 Cut-cell 1,945 单元，约 0.43 s，壁面体长/48；
手动体长/96 可生成 3,237 单元。贴体 L7、首层 0.039、4 层请求生成
1,371 求解单元，保留 384 / 448 个请求边界层单元，总生成约 1.4 s；
同参数此前保留 212 个、约 6.7 s。完全停层列从 38 减至 4，急弯允许局部停层。
L8 此轮仍不能保证保留贴体层。输入是原始 50 顶点折线，原始轮廓保持不变。

壁面歪斜现纳入修复候选，增加壁面法向局部切分；原修复失败后再尝试保留共线连接点的凸划分。
贴体过渡失败时尝试层前沿直接连接余域，仍通过统一拓扑和质量门。
自动更密重试先保留加密层级、调整层厚。上一轮回归 95/95、桌面 51/51。
本轮外流局部层允许凹向转折继续沿法向推进，由长度、碰撞与质量约束减层；严格 H4-1 接口保持原策略。
DXF NURBS 按节点区间提取有理 Bezier 控制多边形，再按控制包络误差细分；SVG 三次曲线按有限弦段距离细分。
真实曲线按误差采样，输入 XY 折线保持其原始几何。采样达到递归上限时显式失败。
本轮 DXF 测试（含五次曲线漏采样、非夹持节点多区间案例）、桌面 52/52 通过；新蛇形通过独立读取器和
OpenFOAM 2606 标准 checkMesh，内部 solver PASS，Q1 FAIL。未重跑全套 CTest、扩展 checkMesh 或等待云端 CI。
上一轮两种加密标准 checkMesh 通过；扩展检查分别标记纯网格 270 个、贴体 320 个 concave cells，这些是旧结果。
局部停层接口的独立检查允许端点落在一条层壁面边和一条余域壁面边的交点；内部悬空端点仍拒绝。
本轮原始日志、网格与预览保留在忽略提交的 `outputs/curve-continuity/`；此前结果在 `outputs/curves/`。
CI 包含两种蛇形加密及独立读取回归。

| 需求 | 已有能力与边界 |
|---|---|
| 原生二维网格 | Cartesian / Quadtree / 真实 Cut-cell；默认外流，支持多环；局部孔洞等几何仍可能显式拒绝 |
| 密度 | 纯路径已有尺寸场、距离/曲率/间隙/尾迹和手动区域；圆例压力测试验证至 L11，但不保证所有几何成功 |
| 速度 | 新版尺寸场测量保存在 reference；必须比较同输入参数的实际单元数/尺寸/时间，不能把近均匀压力网格代替产品设置 |
| 桌面 | 新版线框、缩放拖动、手动区域、按方法着色、多字段结果解析均保留；11 个内置样例 |
| 边界层 | 共形拼接、局部停止和 fallback 已有；仍 Beta，典型圆例 L9 质量失败，界面默认上限 L8 |
| CFD | 导出 OpenFOAM case；内部 solver 门、独立读取器、外部 checkMesh 各自独立；没有验证所有 CFD 软件原生导入 |

历史 L11 圆例最低层级为 9：277,336 leaves、180,468 稳定化单元、184,380 最终求解单元。
对应的真实 checkMesh 与独立检查保留在 `artifacts/reference/pure-l11-*`。
这是一份近均匀压力网格，不是当前桌面默认密度/速度，也不是本次重新跑出的结果。
原 `size-field-survey`、`alignment-sensitivity`、`pure-quality-reachability` 保留原始内容，
用于说明局部尺寸场、对齐敏感性与质量可达性；不能推广成任意输入的保证。

## Linux 凹角修复

H4-3 的现有凹角回归在 Ubuntu 24.04 / GCC 13 上复现了 13 项 Solver 质量问题。
候选划分现与最终提交统一保留笛卡尔矩形；局部候选排序使用集中容差分箱，
减少浮点末位差异对修复顺序的影响，并保持排序传递性。最终质量验收使用原始数值与原阈值。
同输入修复后在 Linux 生成 5,451 个单元并通过内部 Solver 门；该输入保留在 H4-3 回归中。
完整云端验证记录见 GitHub Actions 对应提交，包含原生回归、独立读取与 OpenFOAM 检查。
本机完整 CTest 91/91（103.21 s），桌面测试 50/50。

## 内流边界层与自动密度更新

验证：CTest **91/91**（150.92 s），桌面测试 **50/50**，最终 App 自动生成/导出通过。

2026-09-08：贴体边界层贯通显式内流语义，包括壁面朝向、余域裁切、面积守恒、共享接口和纯路径回退。内域直接连接 Cut-cell 余域；拐角细分保留 miter 所需距离，长直边中段独立细分。余域小单元合并参数用于改变构造，Solver/Q1 判定阈值保持原值。

- 喷管常规：L6 / 最低 L5、3 层、首层 0.015、增长 1.15、留白 0.5、余域 α=0.65，1,702 单元，约 1.08 s。
- 喷管更密：L7 / 最低 L6，其余同上，3,106 单元（其中边界层 1,110），约 3.47 s。真实 App 首组选参成功，侧栏到底、预览与 ZIP 导出单元数一致。
- 独立读取器及标准 OpenFOAM 2606 checkMesh PASS；完整检查 FAIL：690 个 concave-or-planar 标记，其余检查通过。Q1 仍 FAIL。证据：`artifacts/current/nozzle-hybrid-*`、`desktop-nozzle-hybrid.png`。单环内流为当前 hybrid 范围；多环带孔保持明确拒绝，入口/出口边界条件需另行配置。
- 自动纯 Cut-cell “更密”同一批 11 个内置样例，成功数从 8/11 提升到 10/11。旧成功包含圆柱和双柱降回常规；新圆柱/厚翼型/双柱在安全深度内扩大细网格带，保持外域尺寸。蛇形体仍未过 Solver 门。该样例统计不代表任意输入成功率。
- 修正 OpenFOAM 壁面识别：一条求解网格边可由同一输入环的多段共线边完整覆盖；跨缺口及多环歧义继续拒绝。窄缝导出恢复。
- README 面向使用者介绍功能、安装、操作与输出，修改历史由 Git 保存。

## 上一轮交付与验证


实际产物与参数统一在 `artifacts/current/validation.json`；同目录保留最新三例截图、独立报告和外部日志。
CTest **89/89**（160.37 s）、桌面测试 **49/49**（本次仅参数/桌面改动；89 项原生全量是上一轮基线）。真实 App 默认自动选参成功：

| 实际输入 | 参数档 | 最终单元 | 原生生成耗时 | 内部 Solver / 独立读取 / 标准 checkMesh | 完整 checkMesh |
|---|---|---:|---:|---|---|
| NACA 2412 | 更密，壁面体长/96 | 4,445 | 约 0.50 s | 均 PASS | FAIL：539 个共平面连接单元；逐个确认全为轴对齐矩形 |
| 喷管常规 | 内流，壁面体长/128、最低 L7 | 2,068 | 约 0.77 s | 均 PASS | FAIL：148 个 concave-or-planar 标记；低 determinant 为 0 |
| 喷管更密 | 内流，壁面体长/256、最低 L8 | 6,024 | 约 2.80 s | 均 PASS | FAIL：316 个 concave-or-planar 标记；低 determinant 为 0 |
| 尖后缘 | hybrid，8/3/8、4 层、0.012、1.15、留白 1 | 3,391 | 约 3.19 s | 均 PASS | FAIL：948 个 concave-or-planar 标记 |

耗时仅代表这台机器此次输入和参数，不能作任意网格的速度保证。三例 Q1 仍 FAIL。
喷管稀疏原因是旧外流种子只有体长/16，内域最低层级为 0，自动遇到首个成功就停止。现加独立内流配置：常规 128、更密 256，最低层级始终只比壁面粗一级；回退也同步设置最低层级，不退回旧 160 格配置。内流计算域背景留白 0.5，流体轮廓与面积保持不变。手动选内置喷管也加载该配置，外流配置保留。

自动模式也检查了超椭圆 hybrid 更密档，2,363 单元首次成功；不以此宣称任意形状都会成功。

- **方格**：追溯 `4b0af3f` 的严格凸划分，发现把矩形边上的共线连接点也当成切分理由。
  现在只对精确轴对齐、四个正转角的矩形保留原单元和连接点；真实凹角仍走原修复。
  翼型远离物面检查了 3,310 条边，斜边为 0。改变的是实际求解网格，不是隐藏画线。
- **外部检查口径**：OpenCFD 的 `checkConcaveCells` 将共平面小面也纳入标记，
  [其实现](https://api.openfoam.com/2312/primitiveMeshCheck_8C_source.html) 使用 `dot > -planarCosAngle`。
  本轮保持这些诊断与原门槛，未把完整 FAIL 改成 PASS；喷管原 160 格预设的低 determinant 问题在此次新参数中不再出现。
- **内外域**：显式选择已移到几何主面板，喷管默认内流。hybrid 内流会拒绝，界面引导纯 Cut-cell，不能静默在外部生成。
- **自动操作**：默认自动，常规/更密两档；优先内置已验证种子，最多 4 组参数，每次原生进程最多 180 s。
  失败重试不更改内外域、不以 pure fallback 冒充 hybrid、不降低质量门。降密成功会明确提示。
  进度显示每组已用时间、粗略剩余时间；同一会话成功的相同参数提供后续估时依据。不是精确 ETA 或成功保证。
- **视角**：默认物体周边，主体约占可用跨度一半；保留物面和全域。相机变化不缩小实际 CFD 计算域。

目录清理/上轮界面的基线证据可从 `0ef933d` / `a149e39` 找回，不混入当前截图和计数。

## 保留的已知问题

| 方面 | 已确认的边界 / 后续工作 |
|---|---|
| 输入保形 | 坐标 `1e-3` 与不足三点环已修复；SVG defs/transform 处理不完整。DXF 隐藏样条凸起反例仍被接受 |
| 显示与导出 | 已统一最终求解拓扑并保留笛卡尔矩形；纯路径新增 `.solver.cm2d`，桌面直接读它，原 `.cm2d` 保留过程网格。两路径均按实际尺寸分档 |
| 任务与文件 | 本轮已加互斥/取消、参数锁定、每次独立临时目录、ZIP 保存、失败未确认状态；清掉旧结果显示。正常退出清缓存，异常崩溃可能留下系统临时文件 |
| 大输入 | 通用坐标轮廓范围已去除数组参数展开；SVG 仍有独立大输入路径。密网格预览和大 CAD 输入尚需分别验证 |
| 核心检查 | 重叠单元、文件自报全零 AUDIT、微尺度面积检查存在旧审计反例；相关源文件新版未变化，不能把内部 PASS 当完整保证 |
| 边界语义 | hybrid 输入、fallback 和导出 patch 身份仍有缺口；narrow_gap 的部分桌面参数会在导出阶段失败 |
| 质量与上限 | 几何/格点对齐仍会影响成败；hybrid 壁面采样与层厚限制未解决。更高 level 不靠改上限数字承诺 |
| 性能 | 每叶重复轮廓校验仍值得定向优化；旧版试改提速未合入，不作为当前产品性能结论 |

`tests/repro/core-probe.cpp` 与 `hidden-spline-bulge.dxf` 是保留的最小诊断材料，不是已修复测试。
本轮做了全目录/历史用途清点、新旧逐文件差异核对及有限反例；没有宣称所有数值路径逐行审计完成。

## 质量状态的口径

- Solver 硬门仍使用既有 policy（如 face weight 0.05、volume ratio 0.01、non-orthogonality 70°）。
- Q1 普通单元更严格；旧五个 H4 案例曾同时是 solver/checkMesh 通过、Q1 FAIL。那是历史指定参数的结果，不能冒充当前所有样例通过。
- BoundaryLayer 为 OBSERVED，不是完整合同 PASS。Q3/Q4/R1 修复有局部收益，未使所有案例达到 Q1。
- 外部检查没运行就写未运行；出现失败或仅写出部分文件不得记成整次成功。
- 历史 R1 的完成范围仅覆盖部分窄缝事务，sharp-tail 迁移未验收；Q2-B 是存档实验，不在当前主线。

## 后续按用户目标做，不再无限分阶段

当前优先按上述已授权工程目标推进：继续规模性能与几何鲁棒性修复，
完成固定规模/扰动矩阵和代表工程流动的 CFD 多网格验证。以下为之前桌面交付范围的历史记录。

1. 本次桌面交付范围已固定为免目录预览、结果包、滚动与表单修复；输入保形和大输入限制单独留在已知边界，不借此无限延长本轮。
2. 以新版尺寸场做密度/速度基准，定向减少重复计算；提高上限是有限实验，失败不自动追加修复层级。
3. 固定代表样例验证 CFD 导出与安装包；保持失败信息和原质量阈值。

完成与否按事先固定的实际产物/验收判据判断。新增问题更新本文件，不再生成日期审计或 3.1/3.2 式追加计划。
