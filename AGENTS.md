# CartMesh2D 开发规则

## 接手与唯一入口

- 当前根目录是唯一工作区，前端只看 `desktop/src/`；当前能力、版本与限制以 `docs/CURRENT_STATE_CN.md` 为准，旧版本不再充当当前基线。
- 先核对 `git status`、`git branch --show-current` 和 `git worktree list`，再读 `docs/CURRENT_STATE_CN.md`。
- 当前状态只维护该文件；代码导航在 `docs/DEVELOPMENT_CN.md`。不要新增带日期的审计、阶段计划或交接文档。
- 当前主线为网格完整性、不可压层流精度/默认稳定性及高密度提速；提速优先减少外迭代并比较完整成本，不拓展新流动形式。
- `codex/compressible-flow` 是用户明确授权的独立可压 Euler 开发线；只在该 worktree 使用自己的 `build/`、`outputs/` 和桌面 runtime，按可复现算例推进数值核心与交互，暂不合入 main。
- 重要突破完成验证后 commit 并 push；报告分支、提交和真实验证范围，不提交缓存、依赖或大批生成网格。

- `main`为通过验证的集成线，`codex/cfd-development`为后续开发线；网格里程碑标签固定。用户已授权本轮整理后验证并集成CFD，不把这项授权当作无限期自动合并规则。
- Git提交使用用户已配置的身份；不自动增加AI/bot的Co-authored-by。贡献者显示先查实际历史和GitHub API，不为刷新页面反复改写历史。

## 原生二维与物理硬约束

- 本仓库不依赖、链接或复制三维 `cartmesh` 核心，不得把三维算法压到 z=0 冒充二维。
- 核心保持 Point2D、Segment2D、AABB2D、Polygon2D、Quadtree、CutPolygon、Edge2D。
- 闭合 BoundaryLoop 默认是固体；Domain2D 是外域；流体是 `domain - solid interior`。
- Inside 是固体，不进入流体网格；Outside 是流体；Intersected 必须保留真实外侧 polygon。
- 纯笛卡尔背景模式是独立的几何网格产品：保留 Inside/Outside/Intersected 的完整单元并分类，但不标记为流体求解网格。不得将其直接接入现有绕流CFD或以保留内部格子改变Cut-cell的流体语义。
- 外流须同时具有 EmbeddedBoundary 与 DomainBoundary，并满足 `fluid_area = domain_area - solid_area`（容差内）。
- 内流只允许显式 `FluidRegion2D::Interior`。不得以单元中心采样或删除相交格子代替 Cut-cell。
- 自交、零面积、重复边、孤立边、非流形、分类冲突必须显式失败；现有漏检属于待修缺陷，不能作为放宽规则的依据。

## 开发阶段的验证范围

- 日常开发先完成当前功能目标：能构建、能运行、交互有效、数据来自真实计算，并检查本次修改直接相关的基本行为。默认只跑必要的相关测试，不自动开展全量回归、网格收敛、跨平台验收或性能极限优化。
- 开发可用、数值收敛和最终物理精度分开报告。尚未达到最终精度要求的功能可明确标为开发版或实验版；保留已完成的功能和有价值的方案，不因某个精度指标未达标而整体否定或反复重写。
- 不把 `1e-9` 等数值当作通用验证标准。新增或收紧阈值前，说明它检查的量、单位或归一化方式、依据、当前任务为何需要，以及预计验证成本。没有任务依据的严格阈值只作可选诊断，不设为开发完成条件。
- 严格精度验证在明确进行精度验收、重要交付或 main 集成时按目标组织。日常任务不自动升级为长期精度或提速研究；额外验证若超出当前任务范围，先说明收益与成本，再由用户决定。
- 降低日常验收范围不等于隐瞒错误：NaN、非法几何、损坏拓扑和失效控件仍需显式报告；迭代上限不能标为收敛，未验精度不能声称已验。以下质量和交付约束在对应资格验收时适用，不要求每次开发都完整重跑。

## 验证与修改

- 几何与拓扑正确性高于图片；不得降低 Solver 质量门、删除坏单元或隐藏拓扑/导出告警来通过验收。Q1 检测已取消，不得重新加入产品验收或用历史 Q1 结果替代当前证据。
- 边界层、终止区和余域必须进入统一共形 owner/neighbour 拓扑；pure fallback 必须明确告知。
- tolerance 集中管理；相同输入参数与工具链产生确定性拓扑和输出。跨编译器一致性必须另证。
- 算法修复保留最小失败案例。日常跑直接相关测试；重要交付完整跑 CTest 与前端测试。
- 重要里程碑生成真实网格和预览，使用独立读取器；OpenFOAM 可用时真实运行 checkMesh。
- 拓扑、Solver quality、外部 checkMesh 分开报告；未运行写未运行，不能互相替代。历史 Q1 报告只作为旧版记录，不属于当前验收。
- macOS 固定系统 clang++，避免 PATH 中 mesasdk 生成的动态库依赖导致 App 无法运行。

## CFD与交付边界

- 动量、连续性、线性残差、场变化与物理精度分别验收；达到迭代上限不算收敛。
- 初值不是物理检查点；失败候选不能替代最后接受步，非定常续算须核对网格/边界/物性/时钟。
- 任意几何的单例加速不外推为通用性能；比较同控制、同精度的完整成本，说明粗解与映射。
- 研究修复或优化通过单例后仍需通用实现与回归，不自动改默认。保留的Euler/SST/温度代码若仍有调用或测试，不按“本轮不做”删除。
- main集成须完整原生/前端回归、真实App检查和对应平台CI；未解决的精度与稳定性问题在当前状态明确列出，不能用合并替代资格证明。

## 防止目录再次膨胀

- 常规构建只用 `build/`；实验输出用忽略提交的 `outputs/` 或临时目录。
- 不在仓库内创建长期隐藏工作区、baseline-source、整份代码副本或日期命名的试改应用。
- 文档固定为 README、本规则及 docs 内三份；当前状态保留关键证据与未完成项，不逐版本追加过程。展示素材说明只解释既有素材，历史交给Git。
- 保留仍参与构建/测试的功能；删除过时入口时同步修正引用，不以减少行数为由删掉验收门。
- 少量证据可进 artifacts；大网格和安装包保留本地或使用发布附件，不塞入日常源码历史。
